# 📁 09-backtest · 回测证据链层

> **闭环位置**：进化环⑤（可量化提案的**证据前置**）。
> **原则**：无回测证据 → 只能停台账「待验证」区，不得直接落地。

## 结构
- `scripts/` 回测脚本（参数扫描/档位回测等）｜`data/` 原始K线｜`data_check/` 取数核对｜`output/` 标准三件套＋index.html＋PNG

## 约定
- 回测口径必须**契约化**（⛔ 禁字面量；参照 `stop_profit_backtest.py` v3.0 的 R1–R5 守卫）
- 结论只报「回测区间＋样本量 n＋失败条件」，⛔ 无样本披露的断言最高 C 级（手册附录E F8）
- 提案/仲裁**材料**放 `00-system/`（本目录只放回测本身）
