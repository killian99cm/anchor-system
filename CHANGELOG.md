# Anchor 变更日志

> 每次改动必须记录：版本号 + 日期 + 变更内容 + 影响文件
> 更新时同步：portfolio_data.json / CLAUDE.md / 会话检查点.md / 本文件 / GitHub
> ⚠️ 版本冻结协议（8/27 启动）：bump 必须 CHANGELOG+JSON+CLAUDE.md 三处一致，否则不进规则手册；用 `05-scripts/version_check.py` 校验

---

## v4.5.2 — 2026-09-18（🔴 **A2 前日判据源结构性无源 → 自建日序列缓存** ＋ **MA5 口径明文裁决** ＋ **9/1 证券双记并案** —— **两条「判不了」的触发线首次变得可判**）

**性质**：**工程化 ＋ 两处口径订正**（A2 禁买语义、watchlist 试探额度均承 v4.5.0/v3.10 不变；本次只补**判据源**与**口径明文**）。

**用户裁决（2026-09-18）**：① A2 前日源 → **自建日序列缓存**；② 9/1 `证券 ¥2000` → **记重了**（并案）；③ MA5 口径 → **含当日**。

---

### ① 🔴 A2「前一交易日板块涨幅」：外部源穷尽后改为**自建日序列缓存**

**病灶（承 v4.5.1 未闭合项）**：手册 §2.1 A2 的第二分支「连续 2 日飘红」要求读**前一交易日**板块指数涨幅，而该判据**无任何可用源** —— v4.5.1 只能把无源面收缩到 `(0%, 2%)` 区间并**不授予买入许可**（fail-closed）。收缩是止损，不是解决：**每个 `(0,2%)` 的交易日都是判不了**，而 A2 是 `X` 执行级。

**穷尽记录（外源）**：东财板块日K `push2his` 非 JSON／`push2delay` 回 0 条（**已试 2 主机**）；10jqka 板块线 `d.10jqka.com.cn/v6/line/bk_<code>/01/last.js` 对**行业**码（`881xxx`）可用，但 watchlist 四个主题多为**概念/主题**（`886xxx`），_detail 页给出的 `bk_308294`/`bk_309119` **实为 404**（详情页码 ≠ 数据码）—— 实测 `bk_886032`（固态电池）可用但覆盖率无法保证四个主题全有 ⇒ **不构成稳定源**。

**为何不用「未知偏差的替代源」**：A2 的错误**不对称** ——
- 替代源**高估**涨幅 ⇒ 误禁买 ⇒ 只损失一次机会（可接受）；
- 替代源**低估**涨幅 ⇒ **放行一笔本应被拦下的买入**（不可接受）。

⇒ 任何**偏差方向未知**的替代口径都不能用于 A2。**自建缓存的数据来自与 A2 当日判据同一个 `board_movers_all()` 调用**，偏差方向**为零**（同源同口径），这是它胜过一切外源的**唯一理由**。

**落地**：`fetch_public.py` 新增缓存层 ——

| 函数 | 职责 |
|---|---|
| `_board_history_path()` | 落盘路径；**`ANCHOR_BOARD_HISTORY` 环境变量可覆盖**（测试必须用，防写生产文件） |
| `board_history_load()` | 读；任何失败一律返回 `{"schema":1,"days":{}}`，**绝不抛** |
| `ref_last_trading_day()` | 用上证 `sh000001` 日K 作**参考交易日历** |
| `prev_trading_day(kl, today)` | 从日历取**严格小于 today** 的最后一条 |
| `board_history_record(allboards, trade_date, now)` | **四条硬护栏**后落盘（见下） |
| `board_history_day(date_str)` | 读某日；**不存在即 `None`** |

**四条硬护栏**（缺一即 `recorded=False` ＋ reason，**绝不猜**）：
1. **源不可用 / 不完整** ⇒ 不记（半截数据比没有更坏 —— 它会伪装成「有该日记录」）；
2. **`trade_date` 缺失** ⇒ 不记；
3. **未到 15:05** ⇒ 不记（盘中值不是收盘值，**同 #138「拿到一个价 ≠ 拿到收盘价」**）；
4. 🔴 **按数据自身交易日归档，不按 `now`**（这一条是最容易写错的一条：用 `now` 归档会在隔日运行时把**昨天的行情记成今天**，且**从错误记录本身无法发现**）。

读侧同样硬：`board_history_day()` **不存在即返回 `None`，⛔ 绝不回退到「最近一条」** —— 回退会把「前日缺」静默变成「前日等于更早某日」，**A2 会在错误的日子上被判定**。

**消费侧**：`gen_watchlist_status.py` 的 `_eval_a2()` 第三分支（`(0%, 2%)`）改为**读缓存判定**，三分法 → **完全判定**：
- 缓存有该日该板块且 `> 0` ⇒ **连续 2 日飘红 ⇒ A2 命中**（禁买）；
- 缓存有该日该板块且 `≤ 0` ⇒ 分支②不成立 ⇒ **A2 不成立（完全判定）**；
- 缓存**缺**该日或该板块 ⇒ **A2 仅部分可判 ⇒ fail-closed，不授予买入许可**。

⚠️ **缓存需累积**：上线首日只有当日一天 ⇒ 次日之前所有 `(0,2%)` 仍走第三条（fail-closed）。这是**已知且已声明的降级**，不是静默失效。

---

### ② 🔴 `a2_prev_day_cache` 进契约 —— 顺带堵住「转述层与定义层无绑定」的**第 9 次**

新增契约键 `a2_prev_day_cache: "board_pct_history.json"`（**不是死键**：`gen_watchlist_status.py` **真的读它**）。并从手册抽取其值（正则锚定「自建日序列缓存…`xxx.json`」）。

**但光有契约键还是「转述层」** —— 手册里写的文件名与实际读的文件名**没有任何机制保证一致**。故在 `build_status()` 内新增**同轮绑定校验**：

```python
_declared = 契约里 a2_prev_day_cache 的值
_actual   = os.path.basename(fp._board_history_path())   # 代码真正读的那个
source_binding = {...}
if _declared != _actual:  →  🔴 warn「判据源名实不符，须改契约或改代码（不得两边并存）」
```

**这是 v4.5.0/v4.5.1 那条教训的正向应用** —— 前 8 次「转述层与定义层无绑定」都发生在「手册说的」与「代码做的」之间，且都是**事后**才被发现。本次把**比较本身写进运行路径**，名实不符会在**当次运行**就报出来。

---

### ③ 🔴 MA5 口径明文裁决：**含当日收盘**

**病灶（v4.5.1 自曝的实现方暗定口径）**：手册 §4.4 watchlist 条件②「标的当日收盘价 ≥ 当日 MA5」中，「当日 MA5」**含当日 / 不含当日两解会给出不同结论**，而初版实现**默默取了「不含当日」**（即昨日的 MA5）却从未声明。

**裁决**：「当日 MA5」＝**含当日收盘**的最近 5 个交易日收盘均价（**行情软件图标准读法**）；「不含当日」一解**不予采用**。已写入手册 §4.4②，并进契约键 `watchlist_confirm_ma_includes_today: true`（**有真实消费方**：`evaluate_entry()` 读它决定 `ma_used`）。

⚠️ **但另一解一并保留计算** —— 两解**结论相反时主动报警要求裁决**。理由：口径订正**不应抹掉**「曾有两种读法」这一事实；且 9/18 实测四条**两解同结论**（属**侥幸非正确性**）。**判据源**＝标的自身日K（附录E · F5）。

---

### ④ 🔴 9/1 `证券 ¥2000` 双记并案：**9 月买入 6 → 5**

**病灶**：`pre_trade_check` / `monthly_ops_summary` 把 `2026-09-01` 的**两条**记录各计一次 —— `贷款第1笔:14:56站上770+主力+11.31亿=右侧确认` 与 `贷款第2笔-09-01收盘确认`。二者**是同一笔成交**（前者为盘中 14:56 决策留痕，后者为收盘确认）。

**处置**（用户裁决「记重了」）：**不删除任何一条**（删除会破坏审计链 —— 保留「14:56 曾做过一次决策」这一事实），改为 `superseded: True` ＋ `superseded_by` 指向幸存记录 ＋ note 前缀 `[已取代·见 superseded_by]`；**决策留痕原文一字未改**地并入幸存记录 note。

**读数变化**：9 月 买入 **6 → 5** / 卖出 3 / 合计 **9 → 8**。⚠️ **`is_over_limit` 两种口径下均为 `True`** ⇒ **这是审计链订正，不是决策改变**，⛔ **不得据此推断「额度变宽松」**（买入 5/2 仍超限）。

**顺带修复一个「天天响的告警」**：`monthly_ops_summary` 的疑似双记分组键**漏了 `fund`** —— 并案后它仍报一组 2（`证券` ＋ `鹏华`），因为「**同日同额但不同基金**」被当成同笔。已把 `fund` 加进分组键。**同类教训见报告标准 v2.1**：「**一条永远做不到的强制项会训练出『照抄免责』的习惯**」—— **天天响的假告警会被学会忽略**，比不报更危险。

---

### ⑤ 🧪 测试：`test_rule_gates` **89 项全绿**（新增 §J 共 16 项），smoke **94 通过 / 0 失败**

新增 **§J（J1–J16）**，**含 4 组反向断言**：

| 断言 | 内容 |
|---|---|
| **J3** | **缓存缺失 ⇒ 必须「仅部分可判」**（反向：若代码回退到「最近一条」，此断言必失败） |
| **J7** | **按数据自身交易日归档**（夹具故意用**收盘后但非当日**的 `now`；若代码按 `now` 归档则断言失败）—— ⚠️ 首跑失败：夹具误用 `_dt(2026,9,19,10,0)`（盘前），被 15:05 护栏正确拒绝，**断言为假却没测到目标**；改为 `_dt(2026,9,19,15,30)` 后通过 |
| **J11** | **生产文件字节未被测试改动** —— 隔离靠 `ANCHOR_BOARD_HISTORY` **换路径**，**不是 `try/finally`**。**这是 v4.5.1 的直接教训**：`test_sync_resilience` 靠 `finally` 还原生产 `fetch_registry.json`，被硬杀后假条目 `__TEST_FAIL__` **永久留在生产注册表** ⇒ **「靠 finally 保证不污染」在进程被硬杀时不成立，不碰生产文件才是真隔离** |
| **J15/J16** | 契约抽取的反向断言（正文删掉 ⇒ 必须 `warn`） |

**H 系列随数据订正同步**：H17 夹具改为**同基金**（真阳性）；**H17b** 断言「异基金同额**不得**被报双记」（反向）；H20 `(6,3)` → **`(5,3)`** 并注明演化（旧口径 10 → v4.5.1 6 → v4.5.2 5）；**H23** 真实数据疑似双记 = **0**。

---

### ⑥ 影响文件

| 文件 | 改动 |
|---|---|
| `05-scripts/fetch_public.py` | 新增缓存层 6 函数 ＋ `import io/os` |
| `05-scripts/gen_watchlist_status.py` | `_eval_a2()` 第三分支读缓存；`evaluate_entry()` 读 MA 口径键；`build_status()` 新增**判据源绑定校验** |
| `05-scripts/extract_rule_contract.py` | 新增 **2 键**（`watchlist_confirm_ma_includes_today` / `a2_prev_day_cache`），**落盘 33 键 `warns=[]`** |
| `05-scripts/data_processor.py` | 疑似双记分组键补 `fund` |
| `05-scripts/test_rule_gates.py` | 新增 §J 16 项；H17/H17b/H20/H23 随数据订正 |
| `01-rules/投资规则手册_v3.5_正式版.md` | §2.1 A2 **前日判据源条款**；§4.4② **MA5 口径**条款（**v3.12**） |
| `portfolio_data.json` | 9/1 证券双记并案；`system_version` → v4.5.2 |
| `.gitignore` | `06-dashboard/board_pct_history.json` 不入库 |

⚠️ **未闭合（登记不改）**：`sector_flow`/`sector_movers` 仍只取**单页 100**（v4.5.1 已识别未改）；**缓存需累积**，次级前日判据在此期间仍走 fail-closed。

---

## v4.5.1 — 2026-09-18（🔴 **三处「写了没人接」同时收口** —— 月操作额度口径缺陷 ＋ `watchlist.today` 只写字段 ＋ §五 人工占位符；**并勘误 v4.4.12 一句被证伪的修复结论**）

**性质**：**工程化 ＋ 一处口径订正**（无规则层语义变更；A2/watchlist 判据本身承 v4.5.0 不变）。

---

### ① 🔴 月操作额度：三个缺陷叠加，**会产生「碰巧对」的假绿灯**

登记项原文只写「月操作额度计数口径缺陷（`is_manual_operation()` 排除 `赎回到账` 但不排除 `赎回确认`）」。**回读原始定义后确认实际是三个独立缺陷，且最严重的那个不是登记里写的那个。**

**原始定义（回读，非转述）**：手册 §1.3「正常月 ≤4 笔（**买入 ≤2 ＋ 卖出 ≤2**），清理月 ≤6 笔」。

| # | 缺陷 | 后果 |
|---|---|---|
| **A** | **维度丢失** —— 代码绑定的是速查表（附录C）里被**压缩掉括号**的转述「≤4 笔」，而 `buy_max`/`sell_max` 是**值正确但无人读的死键**（无提取正则、无消费方） | 🔴 **买入 3 笔 ＋ 卖出 0 笔 ⇒ 旧判据 `3 < 4` 放行**（实为买入维已超限）。**不是「数错了」，是「判错了维度」** |
| **B** | **记录 ≠ 事件** —— `赎回确认`／`赎回到账` 等**记账腿**被当成独立操作计入 | 同笔赎回被记 2 次；9 月读数虚增为 10（真实 9） |
| **C** | **按子串碰巧分类** —— `赎回确认` 含子串 `赎回`，若先判卖出会被误归 | 分类结果依赖判定顺序，脆弱 |

**修复**：① 手册 §1.3 新立「**月操作额度 · 口径定义**」四条为**唯一权威口径**，速查表补回被压缩的括号；② `classify_txn_op()` 五分类（`buy`/`sell`/`leg`/`unknown`）＋ **记账腿必须先判**；③ `monthly_buys_max`/`monthly_sells_max`/`monthly_ops_cleanup_max` **进契约**（`extract_rule_contract.py` 新增 3 键，落盘 31 键、`warns=[]`）；④ `pre_trade_check` 校验项 1 改**买卖两维独立判定**，总数未满**不构成豁免**。

**🔴 两条 fail-loud 新增**：**未知 op ＝ 声张**（不再静默计入或静默丢弃）；**疑似双记**（同日期/op/金额多记录）**只报不折叠** —— 谁对谁错**留给人工**，机器不猜。

**📊 真实读数变化**：9 月 `10 条记录` → **买入 6 / 卖出 3 / 合计 9**（＋ 疑似双记 1 组，待人工确认）。⚠️ **登记项里写的「真实 7 笔」本身是个未计算的估算**，实测折叠后为 8 个事件、当前 9 笔待确认 —— **不据估算值下结论**。

---

### ② 🔴 `watchlist[].today`：**登记项给的处方是错的**

**登记项**：「`watchlist` `today` 字段自 9/7 起停滞 9 个交易日」→ 处方「**改数据源**」。

**核实后推翻**：该字段是**只写字段（write-only）—— 全仓零读者**。
- `rebuild.py` 的 JS 读 `w.sector`/`w.etf_code`/`w.status`/`w.trigger`，**不读 `w.today`**；
- 全仓 `.get('today')` 命中**全部指向 `embed['today']`**（当日决策块，**同名不同物**）；
- `gen_intraday_auto.py` §五 是**人工占位符**，从不读 watchlist。

⇒ **它不是「过期了」，是这个字段本来就没有下游。** 按原处方「改数据源」＝**为一个没人读的字段新建取数链路** —— 正是本次会话主题（死定义／写了没人接）的**反面再犯一次**。

**实际修复（真闭环，而非换源）**：新建 `gen_watchlist_status.py` ——
- **算**：按 v4.5.0 判据（§4.4）自动判定 **① A2 不成立 ＋ ② 标的当日收盘价 ≥ 当日 MA5**；
- **被读**：写回 `today`/`today_meta` **并被三处消费** —— `rebuild.py` 看板面板（新增 `.watch-today` 渲染 ＋ tooltip）、`gen_intraday_auto.py` §五（**替掉人工占位符**，出六列表 ＋ 逐条缺口声明）、`sync_all.py` 新步骤 **1.5**（**排在 rebuild 之前** —— 顺序颠倒会让看板渲染上一轮状态）。

**🔴 A2 第二分支「连续 2 日飘红」判据源结构性无源**（手册 §2.1 要求读**前一交易日**板块指数收盘涨幅；东财板块日K：`push2his` 非 JSON、`push2delay` 回 0 条，**已试 2 主机**）。**不做代理、不编造**，改为**严格逻辑三分**：

| 板块当日涨幅 | A2 结论 | 可判定性 |
|---|---|---|
| ≥ 2% | **命中**（分支①） | 完全 |
| **≤ 0** | 不成立 —— **分支②逻辑上不可能**（「连续2日飘红」蕴含「当日>0」） | **完全** |
| (0%, 2%) | 分支②真伪取决于前日 | **仅部分可判** ⇒ **不授予买入许可** |

⇒ **把无源面收缩到唯一真正需要的那个区间**，前两区间结论完全确定；受影响条目一律 fail-closed（A2 是 `X` 执行级，判不了 ⇒ 不放行）。

---

### ③ 🔴 勘误：**v4.4.12 的「改 `pz=500` 覆盖全集」是被证伪的修复结论**

实测 `clist/get` 的 **`pz` 被服务端硬顶在 100**：`pz=100/500/1000` **一律只回 100 行**（`total` 字段 496）。**代码确实传了 500，端不认。** ⇒ v4.4.12 changelog 那句「现改为 pz=500 覆盖全集 ＋ 两端都取」**只有后半句成立**；前半句**从未生效**。

**同时发现第二处**：板块有**两套互不覆盖的宇宙** ——
| fs | 名称 | 数量 | 含 |
|---|---|---|---|
| `m:90+t:2` | 行业板块 | 496 | 有色金属 / 半导体 / 电池 |
| `m:90+t:3` | 概念板块 | 504 | **固态电池 / 人形机器人 / 智能驾驶** |

只查一套时，另一套主题**永远匹配不到**，且**失败长相是「该板块不存在」而不是「查错了宇宙」** —— 静默失效的典型长相。watchlist 4 条里 **3 条**踩此坑（**实发**：初版实现 4 条全部报「未匹配到」）。

**修复**：`fetch_public.py` 新增 `board_movers_all()`（**双宇宙 ＋ 翻页**，返回 `complete` 标志）＋ `board_prev_day_chg()`（诚实报 `available=False` ＋ `tried`，不编造）。`sector_flow`/`sector_movers` **仍只取单页 100 —— 已识别、本次未改，登记为已知缺口**。

---

### ④ 🟡 自曝一处**实现方暗定口径**（已改为显式暴露）

「**当日 MA5**」有**两解**：含当日收盘（行情软件图标准读法）／当日之前的 5 根。**两解会给出不同结论**。初版实现**默默取了后者**。

**处置**：采用**含当日**为准（软件图标准读法），**另一解一并记录**；**两解结论相反时主动报警并要求用户裁决**，**不静默择一**。真实 4 条今日两解同结论（**侥幸，非正确性**）。

---

### ⑤ 🧪 测试 67 ＋ 39 项全绿，**含反向断言**（不只测「改对了」，还测「**原写法确实会错**」）

`test_rule_gates.py` **48 → 67**（段 H 月操作额度 22 项 ＋ **段 I watchlist 15 项**）、`test_gen_intraday.py` **33 → 39**（段 `TestWatchlistSection` 6 项）。关键反向断言：
- **I5**：旧实现（仅 `t:2` 行业宇宙）在**真实数据上找不到** 3 个概念主题中的**任何一个** ⇒ 证明双宇宙修复**必要而非装饰**；
- **I6**：`pz=500` **实回 100 行** ⇒ 直接钉住 ③ 的勘误；
- **I10/I10b**：A2 部分可判 ＋ 技术面成立 ⇒ **不得** 🟢，且 `a2_ok` 必须为 `None`（**不得拿 `False` 冒充「已排除」**）；
- **I13b/I13c**：`close_confirmed` 的三态反向对照（**I13 的失败正是被测试自己抓出的夹具错误**）；
- **§五「人工补充」文案不得再现**（防退回占位符）。

**新增 `smoke_test.py` 步骤 7-c2d** 接入 `test_rule_gates.py`（v4.5.0 遗留项「尚未接入」**本次闭合**）。`smoke_test` **94 通过 / 0 失败**。

---

### ⑥ 🔴 顺带修掉一处**测试自身污染生产数据**（v4.5.0 期间实发）

`test_sync_resilience.py` 旧版**直接改生产 `fetch_registry.json`**、靠 `try/finally` 还原。**硬杀（超时/SIGKILL）下 finally 不执行** ⇒ 2026-09-17 23:08 那次运行把假条目 `__TEST_FAIL__` **永久留在生产注册表**，并连带让 `test_fetch_registry` 长期失败、卡住 `smoke_test`。

**教训：「靠 finally 保证不污染」在进程被硬杀时不成立 —— 不碰生产文件才是真隔离。**
**修复**：改为**临时副本** ＋ `ANCHOR_FETCH_REGISTRY` 环境变量绕开生产文件（`data_auto_fill.py`/`data_pipeline.py` 支持该变量）＋ 启动**自愈**清历史遗留 ＋ 收尾断言**生产文件字节未变**。

---

### 影响文件

| 文件 | 改动 |
|---|---|
| `01-rules/投资规则手册_v3.5_正式版.md` | §1.3 立「月操作额度 · 口径定义」四条（v3.11）；附录C 速查表补回括号 |
| `05-scripts/extract_rule_contract.py` | 死键 `buy_max`/`sell_max` → `monthly_buys_max`/`monthly_sells_max`/`monthly_ops_cleanup_max`（31 键，`warns=[]`） |
| `05-scripts/data_processor.py` | `classify_txn_op`／`txn_exclusion_reason`／`MonthlyOpsSummary`／`monthly_ops_summary`／`compute_ops_state` 重写 |
| `05-scripts/pre_trade_check.py` | 校验项 1 买卖两维独立；阈值 3 键 |
| `05-scripts/gen_watchlist_status.py` | 🆕 新建 |
| `05-scripts/fetch_public.py` | 🆕 `board_movers_all()`／`board_prev_day_chg()`；文件头勘误 |
| `05-scripts/gen_intraday_auto.py` | §五 人工占位符 → 计算渲染 |
| `05-scripts/rebuild.py` | watchlist 面板渲染 `today` ＋ `.watch-today` CSS |
| `05-scripts/sync_all.py` | 新增步骤 **1.5**（rebuild 之前） |
| `05-scripts/test_rule_gates.py` | 48 → 67 项 |
| `05-scripts/test_gen_intraday.py` | 33 → 39 项 |
| `05-scripts/test_sync_resilience.py` | 隔离重做（不碰生产文件） |
| `05-scripts/smoke_test.py` | 新增 7-c2d |
| `01-rules/规则生效期登记表.md` | §六 新增 **#C1-4**（A2 前日判据源无源 · 待裁决）—— ⚠️ 初写为 #C1-2 **撞号**（该号已属 E1 提案），**登记当时自查订正** |

### 未闭合 / 登记项

1. 🔴 **A2「前一交易日板块指数涨幅」结构性无源** → **待用户裁决**（换源 or 明确降级口径），当前实现 fail-closed；
2. 🟡 **MA5 口径**（含当日／不含当日）**当前按含当日**，未有明文裁决；
3. ⚠️ **`sector_flow`/`sector_movers` 仍只取单页 100**（未全量）—— 已识别未改；
4. ⚠️ **9/1 疑似双记 1 组**（证券/鹏华 ¥2000 × 3 条）**待人工确认**，机器不猜；
5. ⏳ **`portfolio_data.json` `system_version` → v4.5.1**（本次已改，随本版入库）。

---

## v4.5.0 — 2026-09-18（🔴 **A2 与 watchlist 规则冲突消歧** ＋ 两条规则首次进契约与门禁 —— 第 8 次「转述层与定义层无绑定」，**形态升级：定义层内部两条规则对同一谓词给了相反语义**）

**性质**：**规则层变更**（1 条消歧 ＋ 1 条判据订正 ＋ 2 条工程化）。触发＝用户问「额度是不是太少了，行情走了怎么办」，追问中发现**两条现行规则互相矛盾且都无门禁**。

### 🔴 病灶：同一谓词，两处相反动作

| 出处 | 原文谓词 | 动作 |
|---|---|---|
| 手册 §2.1 **A2** | 「板块当日≥2% **或连续 2 日飘红**」 | ⛔ **不成交**（禁买） |
| 手册 §4.4 **watchlist 统一规则** | 「右侧信号确认（**连续 2 天飘红**）才允许 ¥300-500 试探」 | ✅ **准买** |

**同一谓词「连续 2 日飘红」，A2 判禁买、watchlist 判准买 —— 两条不能同时为真。** 且两条**都不在 `rule_contract.json`（无 A2 键、无 watchlist 键）、不在任何门禁代码**（`grep -rn "追红日\|红日\|A2" pre_trade_check.py rule_contract.json data_processor.py` → **空**）⇒ 实为**纯纸面纪律**，命不命中全凭人记。

**定性：第 8 次「转述层与定义层无绑定」**（前 7 次：v4.4.8 代码名实／v4.4.10 `QUERY_SPECS`↔`DATA_PIPELINE_MAP`／v4.4.11 同名↔`sync_all`／v4.4.13 转述表↔触发线定义／v4.4.14 `stop_profit_backtest` 档位↔`data_source_health` 词表／v4.4.16 指标层 `pnl_pct` 口径）。**本次形态升级：不再是「转述丢字」，而是「定义层内部自相矛盾」** —— 前 7 次的病灶都在「转述 vs 原文」，这次两条**都是原文**。

### ✅ 改动（3 项，用户 2026-09-18 裁决）

1. **A 消歧（收紧）** —— 手册 §2.1 新增 **A2 优先级**条：**A2 优先于本手册内一切「右侧确认」类买入条件**；凡买入触发条件出现「连续 2 日／2 天飘红」，**一律按 A2 解释为禁买**，不得反向解释为准买。级别 **`X` 执行级**（F2）。
2. **判据订正（收紧）** —— 手册 §4.4 watchlist 右侧确认**换判据**：~~连续 2 天飘红~~ → **① A2 不成立 ＋ ② 标的当日收盘价 ≥ 当日 MA5**，仍限 **¥300-500 试探**。级别 **`E` 评估级**（F1；理由：许可式非命令式，不满足 F2 的 X 定义），承担 **F3 闭环义务**（T+1 14:30 前出显式裁定，顺延 ≤1 次，沉默即违规）。
   - ⚠️ **本条为收紧而非松绑** —— 原判据在 A2 生效下**恒不能合法触发**（A2 必先命中），实为一条**从未生效过的条文**。
3. **工程化（用户选「两条都进契约＋门禁」）** ——
   - `extract_rule_contract.py` 新增 **9 个契约键**：`a2_red_day_pct` / `a2_consecutive_red_days` / `a2_pullback_from_high_days` / `a2_pullback_from_high_pct` / `a2_priority_over_watchlist` / `watchlist_probe_min` / `watchlist_probe_max` / `watchlist_confirm_requires_a2_pass` / `watchlist_confirm_ma_period`
   - `pre_trade_check.py` 新增**校验项 7（A2 禁买）＋ 校验项 8（watchlist 右侧确认）**，新参数 `--sector-chg` / `--sector-prev-chg` / `--watchlist` / `--close` / `--ma5`
   - 🔴 **一律 fail-closed**：判据数据缺任一 → **拦截**，**不静默放行**。理由：A2 是「命中即不成交」的 `X` 级条款，缺数据时的正确姿态是「**无法判定 ⇒ 不成交**」，不是「无法判定 ⇒ 照买」——与 #138「拿到一个价 ≠ 拿到收盘价」同族。

### 🧪 测试：新建 `test_rule_gates.py`，26 项全绿，**含 5 组反向断言**

**不只测「改对了」，还测「原写法确实会错」**（v4.4.16 立的规矩）：
- **B1**：未加锚的宽泛正则 `≥\s*(\d+)\s*%` 在真实手册上首个匹配 = **5**（错），加锚后 = **2**（对）⇒ 证明锚定不是装饰 —— 与 v4.4.0「E1 被非贪婪正则抓成 4」**同型坑**。
- **C1/C2**：删掉手册正文的「A2 优先于」/「A2 不成立」→ 必须 **WARN** ⇒ 证明是**真绑定**，不是「取不到就静默用默认」的死定义（v4.4.10 已判定该类键为「该资产不会被取数」）。
- **E1/E2**：**剔除 A2 输出行后，红日场景与非红日场景的报文逐字相同** ⇒ 直接证明「**没有这道门禁时，系统对『板块红日』零感知**」，即改动前的真实状态；E2 同时排除「输出被截空」的假阳性。

### 📁 影响文件

- `01-rules/投资规则手册_v3.5_正式版.md`（§2.1 A2 优先级 ＋ §4.4 判据订正 ＋ 版本行 v3.10）
- `05-scripts/extract_rule_contract.py`（DEFAULTS ＋ extract()，新增 9 键）
- `05-scripts/pre_trade_check.py`（BUILTIN_THRESHOLDS ＋ `_NUMERIC_KEYS` ＋ `_arg_float()` ＋ 校验项 7/8 ＋ docstring）
- `05-scripts/test_rule_gates.py`（**新建**，26 项）
- `AI-Collab/rule_contract.json`（由 extract 重新生成，28 键，`warns=[]`）

### 🏛️ 附带裁决登记（`01-rules/规则生效期登记表.md` §六 追加两条）

用户 2026-09-18 对我提出的两项松绑提案**均裁决「维持现状」**，按 §六 立规理由（**「维持现状」也是裁决，必须留痕**）已登记：

- **#C1-2 · E1「市值 → 净投入」改造提案 → 裁决：维持现状**（E1 零变动）。否决理由＝**提案自身计算错误**（净投入 > 市值，改后两只从「还有空间」变「已超限」）且**根本不是 binding**。⚠️ **不得据此推断「市值口径已获最优证明」**——**证据缺口 ≠ 已证成立**。
- **#C1-3 · 「三把锁松动」提案（月额度 4→6／E4 ¥1,500→¥3,000）→ 裁决：都不动**。否决理由＝① E1 不是 binding（月额度/E4 照样拦）② 🔴 **E4 ≈ 自由现金 ¥1,447.11 是设计不是巧合**——E4 存在的目的正是**阻止借来的钱（`in_cash` 7,023.77，8/19 ¥23,000 无息贷款残留）流入波动最大的卫星层**，提到 ¥3,000 ＝ 用 ¥1,447 现金买 ¥3,000 卫星仓，**实质是放开「用贷款钱加仓卫星」** ③ 月额度的问题不是「4 太少」，是 9/1 用两笔贷款买入 3 天内烧完整月。⚠️ **结论口径须精确：不是「规则不给你火力」，是「你现在没有火力」——最硬的锁是现金 ¥1,447.11，不是任何一条规则。**

### ⚠️ 未闭合 / 新增登记

- 🔴 **用户批准的「B 修 E1 口径」经实测后撤回**：我原描述为「E1 从**市值**上限改为**净投入**上限，解决『越涨越买不了』」，实测 **净投入 > 市值**（创新药 3,084.80 > 2,716.17；证券 3,012.21 > 2,888.15）⇒ **改净投入口径会让两只都从「还有空间」变成「已超限」**，与我的描述**相反**。**B 未实施**，已回报用户重选 → **用户裁决「维持现状」**（见上 §六 #C1-2）。
- 🔴 **月操作额度计数口径缺陷**（`is_manual_operation()` 排除 `赎回到账` 但不排除 `赎回确认` ⇒ 同族记录两种口径，9 月计数虚增为 10/4，真实 7 笔）—— **已登记，未改代码**。
- 🔴 **watchlist `today` 字段自 9/7 起停滞 9 个交易日** —— 已登记，**未改数据源**。
- ⚠️ `test_rule_gates.py` **尚未接入 `smoke_test.py`** —— 见 CLAUDE.md「待用户确认的 2 件收紧改动」（用户此前未明确批准）。

---

## v4.4.16 — 2026-09-17（🔴 **主指标「盈亏比」口径缺陷修复** —— 该指标此前**不可计算**，`pnl_pct` 混装 5 种语义 ＋ 第 7 次「转述层与定义层无绑定」）

**性质**：纯缺陷修复，**无规则层变更**；但**主指标三件套之一被判定为「此前不可计算」**，属**指标层首次**发生的绑定缺失。

### 🔴 病灶：`pnl_pct` 字段无口径声明，被当成同一种量聚合

`decision_log.py` 自 v3.4 起只用一句 `pnl_pct > 0 → win / < 0 → loss` 分桶算盈亏比，**从未声明这个百分数是什么的百分比**。9/17 逐条回读 35 条已复盘记录的 `review_note` 后实测确认，同一字段里混装了 **5 种语义**（不是最初以为的 3 种）：

| 口径 | 条数 | 语义 | 实例 |
|------|------|------|------|
| `realized` | 9 | **已实现盈亏率**（正=赚钱），真金白银 | #17 实亏 −232/成本 1132 = −20.49% |
| `price_move` | 14 | 标的/指数涨跌幅，**符号含义随决策类型翻转** | #60 规避证券下跌 → 记 **+3.48**；#17 卖在下跌前 → 记 **−20.50**（同义反号） |
| `position_pnl` | 7 | **持仓浮亏率（位置级，非整仓）** | #6 「浮亏 −12.38%（位置级，非整仓）」 |
| `unquantified` | 3 | 复盘时无可用数值 → 记 0 | #23 观望未取数 |
| `benefit` | 2 | 决策受益率（符号被人为翻正） | #56 规避半导体 −3.5% → 记 +3.50 |

**后果**：同一批 35 条数据，按三种都说得通的口径假设算出的盈亏比分别是 **0.53 : 1 / 1.64 : 1 / 算不出** —— 前两个数**不能同时为真**。**9/15 起对外引用的「盈亏比 0.53:1」不可解释、不可复现。**

### ✅ 修复（四层）

1. **口径契约**：新增模块级 `PNL_BASIS`（6 个合法值，闭集），每个口径**显式标注是否可进盈亏比**；`review_decision(..., basis=...)` 加形参 + **非法口径拒写**（`--basis` CLI 同步支持）。
2. **聚合隔离**：`accuracy_report()` 的盈亏比**只在 `realized` 上计算**；其余口径分别计数并在 `--report` 中**显式报出被排除多少**。
3. **样本门槛 `MIN_N = 5`**：realized 的盈/亏样本**各不足 5 笔即返回 `None`**，输出**「算不出」+ 样本量**，⛔ 绝不输出一个"看起来正常"的数。
4. **渲染诚实化**（3 处）：`--report` / `decision_dashboard.html` / `daily_hub.html` 遇 `None` 一律渲染为**「算不出」+ 原因**，⛔ **不得渲染成 `—` 或 `--`**（读者会把「该指标失效」误读成「等数据齐了就有」）。

### 📊 修复后的真实读数（2026-09-17）

```
realized 口径：盈 3 笔 / 亏 6 笔   →   盈亏比 **算不出**（需各 ≥5 笔）
干净读数：avg_win +14.13%（3 笔）  vs  avg_loss −16.75%（6 笔）   ← 9 条已实现，可整除复核
口径分布：price_move 14 / realized 9 / position_pnl 7 / unquantified 3 / benefit 2
```

🔴 **结论：主指标「盈亏比 ≥1.5:1」当前处于「无法测量」状态，不是「0.53:1」也不是「1.64:1」。**
⚠️ **冻结条款**：修好之前，**任何报告/提案/裁决不得引用「盈亏比 0.53:1」**；引用须写「**当前不可测量（realized 样本 3/6，需各 ≥5）**」。
⚠️ 干净读数只有 9 个样本且**集中在 7 月清仓批**，**不构成对策略优劣的评估** —— 样本选择偏差极重（全是割肉与锁利），⛔ 不得据此推断"策略是赚是亏"。

### 🔴 定性：第 7 次「转述层与定义层无绑定」

前 6 次：v4.4.8 代码名实／v4.4.10 `QUERY_SPECS`↔`DATA_PIPELINE_MAP`／v4.4.11 `DATA_PIPELINE_MAP`↔`sync_all`／v4.4.13 转述表↔触发线定义／v4.4.14 `stop_profit_backtest.py` 档位 与 `data_source_health` 词表。
**本次不同点有二**：① **首次发生在指标层**（前 6 次都在数据层/规则层）；② **首次直接驱动了行为** —— 9/17 的「止盈两档改造」提案即以 0.53:1 为立论基准。
→ 同一模式在一个系统里出现 **7 次**，**缺的是机制不是补丁**（与 v4.4.10 判词同）。

### 📎 附带发现（已登记，未处置）

🔴 **`stop_profit_backtest.py` 输出失真（新引入，非其自身缺陷）**：其 `ratio_line()` 在 `pnl_ratio is None` 时回退打印「**decision_log 无已复盘收益样本**」—— 修复后此句**成为假话**（实际有 35 条已复盘，缺的是 realized 口径样本）。
⛔ **本次一字节未动该脚本**（延续 v4.4.14 冻结：其档位口径仍停留在 v3.6 之前），**折入任务单 #141** 一并契约化。

### 影响文件

- `05-scripts/decision_log.py`（`PNL_BASIS` 新增／`review_decision` 签名／`accuracy_report` 聚合隔离／`--report` 与仪表盘渲染）
- `05-scripts/gen_daily_hub.py`（新增 `_ratio_sub()`，两处 KPI 渲染）
- `05-scripts/test_decision_log.py`（**9 → 16 项**，新增 `TestPnlBasisContract` 4 项 + 2 项反向断言）
- `06-dashboard/decision_log.json`（**35 条仅追加 `pnl_basis` + `pnl_reclassified`，`pnl_pct` 数值一字节未改**）
- 备份：`06-dashboard/decision_log.json.bak-20260917-pre-pnl-fix`
- `00-system/待裁决_止盈两档改造_2026-09-17.md`（追加勘误，见下）

### 🧪 反向断言（防「改对了但原来也不会错」的假阳性）

`test_decision_log.py` 新增的断言不止测"现在对了"，还测"**旧写法确实会错**"：
- `test_ratio_excludes_non_realized_basis`：在**同一批样本**上跑旧口径（只按正负分桶）得 **1.44**，新口径得 **0.67**，断言二者差值 > 0.9 —— 即**若不修，会把「坏」读成「好」，且差 2 倍以上**。
- `test_ratio_none_when_realized_sample_too_small`：样本削到 4/4 时**必须**返回 `None`。
- `test_review_rejects_unknown_basis`：非法口径必须**拒写**（否则契约形同虚设）。
- `test_only_realized_is_ratio_eligible`：该断言**当场抓出我自己文档里的不一致** —— `benefit` 条目漏了 ⛔ 标记（已修）。
- `test_existing_realized_records_match_note_arithmetic`：真实数据 9 条 realized 之和 = **−58.1**，可整除复核。

### ⚠️ 未闭合

- **9/18（周五）T+3 复盘 #62 / #63** 时，`--review` **必须显式带 `--basis`**；不带则默认 `realized` —— ⚠️ **本就属 price_move 的复盘若漏传，会污染盈亏比样本**（这是本次引入的唯一新风险点，已在此声明）。



> **一句话**：按用户「按推荐的来，任务单发」——**发单前回读原始定义**，**计划中的 4 张单修正为 3 张**（`F2 Stop hook` 属 Claude 侧 `~/.claude/settings.json`，**不属 Trae 的 `app/` 领地** → **改指挥端自办**）；**F2 自动化当晚落地**（备份先行）；**`B2'-新` 提前闭合 —— 确认解禁，资金动作零**；**FOMC 实际结果回填**（补点阵图／降息路径／通胀三项）；规则生效期登记表**新增 §六「后续提案裁决登记」**并登记 **C1 方案 A（维持现状）**。
> **性质**：**自动化制度新增 1 项**（F2 Stop hook）＋ **1 条 `E` 级触发线闭合裁定**（**零交易**）＋ **2 处登记追加**（登记表 §六 ／ 宏观预案 §5.5＋§八）＋ **3 张任务单**。**规则正文未改一字；无仓位变动；版本号不因裁定变动，仅因自动化落地 bump。**

### 一、任务单 #139 / #140 / #141（归属 Trae，pending）

| 单 | 标题 | 优先级 | 核心 |
|---|---|---|---|
| **139** | C2「盈亏比连续 2 次 <1.0 → 当月冻结新买入」执行落地 | P1｜2 天 | `registry.py:27` **已注册参数但全仓零消费**（**死定义**，与 #137 债券条目同型，**第 4 次**）；裁定 R1「次」= 连续 2 次**月度**读数、R2 读数不足**不冻结**、R3 以 registry 为唯一源；**九条验收含反向用例** |
| **140** | 数据源降级需区分「结构性无源」与「限流性取不到」 | P1｜1.5 天 | `grade` **三态 → 四态**（增 `rate_limited`）；**两套词表必须合并**（JSON `real/degraded/unavailable` ↔ App `ok/degraded/unknown` **互不绑定**）；七条验收含**成对反向用例** |
| **141** | 止盈档位回测脚本口径契约化 | P2｜1 天 | 🔴 **权威口径三处均已正确**（`rule_contract.json` `take_profit=[10,20,35]` ／ `registry.py` C1 ／ `test_stability.py:27`），**唯独 `stop_profit_backtest.py` 完全不读契约**；**语义角色颠倒**（把现行档标「旧档」、把已废弃档标「新档」推荐）→ **只换数字修不好**；第 10 条验收为**反向断言**：改契约则输出须随之变

**📌 三张单均含 §0 编号消歧** —— 🔴 **撞号已实证**：`todo.md:72`「001-088 + B3-B8 + **C2** 全部交付」中的 `C2` 是**应用批次编号**，与**手册 §2.3 的规则编号 C2** 无关；`B4'` 亦易与批次 `B4`、附录E `F1–F6` 混淆。**若不消歧，执行端会看到「已完成」而漏做。**

### 二、🔴 发单前回读原始定义，救下两个错（承 v4.4.13 新立操作性规则）

| # | 错误 | 若不纠正的后果 |
|---|---|---|
| 1 | **F2 归属错判** —— 原计划发单给 Trae | `F2 Stop hook` 在 **Claude 侧 `~/.claude/settings.json`**（实测 `grep -c "Stop"` = **0**，确认未落地），**不在 `app/` 领地** → 发单必被退回或误做 |
| 2 | **C2 / F2 撞号未识别** | 执行端会据「C2 全部交付」**关闭本单** → 规则**继续不执行**且**无人发现** |

> **4 张计划单 → 3 张**。**本条再次验证 v4.4.13 规则的必要性**：它当天立、当天就拦下第二起。

### 三、🆕 F2 信息桥自动化落地（指挥端自办，**审计报告 P1-8 遗留项**）

**背景**：审计报告 P1-8 —— 信息桥**单向失效**（`sync_log` 全 14 条 WB→Claude，**Claude 零回写**），原处置「✅ 已修（手动回写）」，**Stop hook 自动化未做**（涉 `settings.json` 敏感文件，另议）。

| 项 | 内容 |
|---|---|
| **新增脚本** | `~/.claude/hooks/stop_bridge_heartbeat.py` |
| **接线** | `~/.claude/settings.json` 新增 `hooks.Stop`（**其余键逐项验证未动**） |
| **备份** | `~/.claude/settings.json.bak-20260917-pre-stop-hook`（**改前已备份**） |
| **产物** | `AI-Collab/.claude_heartbeat.json`（**覆盖式**，已入 `.gitignore`） |
| **首次实测** | 退出码 **0**；正确报出 `claude_state_lag_days: 1`、`bridge_writeback_needed: true` —— **F2 的真实症状第一次被自动显现** |

**🔴 设计取舍（须保留，防后人「顺手加强」）**：
1. ⛔ **不代写 `claude_state.md`** —— 桥内容必须由**判断**产生；自动生成的「状态摘要」会制造「看起来同步了」的假象，**那是另一种静默失效**。
2. ⛔ **不阻塞、不输出 `decision`** —— Stop hook 一旦 block 会与 `stop_hook_active` 形成**循环**，且每轮回答结束都打断用户不可接受。
3. ✅ **只记事实**（结束时点 ＋ `claude_state.md` mtime ＋ 滞后天数），**不推断、不评价**。
4. ✅ **覆盖式非追加** —— 每轮追加会撑爆 `sync_log.md`；与 `Anchor/05-scripts/.sync_last_result.json`（机器回执）**同形态**，是本系统既有做法。
5. ✅ **永不非零退出** —— 一个会崩的 Stop hook 比没有更糟。

> ⚠️ **生效时机**：hook 于**下次会话启动**时加载，**本次会话内不生效**。

### 四、`B2'-新` 裁定 —— ✅ **确认解禁**（资金动作：零）

**判据取自 `规则生效期登记表.md` §一·补 B. 的原始定义**，⛔ **非**任何转述表（依 v4.4.13 规则）。

| 项 | 内容 |
|---|---|
| 级别 | **`E` 评估级** |
| 判据源 | **513120 收盘价**（15:00 定格）· 判据日 **2026-09-17** |
| 条件 | 513120 收盘价 **> 1.283** |
| 实测值 | **1.284** → ✅ **成立** |
| 裁定 | ✅ **确认解禁**（解除「严禁加仓」→ 创新药回到**正常建仓评估**） |
| **资金动作** | **零** |
| 顺延额度 | **0/1 未消耗**（判据值已定格，**无顺延理由**，F4） |

**四条理由**：① 判据源为**收盘价**，9/17 15:00 已定格，**不存在「等盘中再看」的增量信息** → 提前闭合优于卡到 9/18（**F3 只设上限不设下限**）；② 该线是**解禁型**，裁定形式是「确认」非「执行」；③ **解禁 ≠ 可买**；④ 删去 DDX 条件的重设**已被证明正确** —— 若沿用原式，因 DDX **结构性无源**，本线将**永不闭环**。

**⚠️ 解禁后仍受约束（逐条列出，防「解禁＝可买」误读）**：A2 **追红日禁买**（9/17 创新药 **+1.00% 为红日**）／等回踩／评分卡 **≥3/5**／`pre_trade_check` ＋ `decision_log` **缺一不可无豁免**（手册 §2.4）／累计 **−105.34 未回本**／**仍破 MA20**。

> 🔵 **本段闭合后**：9/18（周五）14:30 前**已无 F3 应付裁定**。承 F 段未闭合项 ② → **✅ 已闭合**。

### 五、FOMC 实际结果回填 `宏观事件应对预案_v1.0.md`

**补入 §5.5（新节）＋ §八 第 2 行**（**§5.1–§5.4 原文一字未改**）。

| 项 | 预案预设 | **实际结果** | 对账 |
|---|---|---|---|
| 幅度 / 目标区间 | +25bp → 3.75–4.00% | **+25bp → 3.75–4.00%**（投票 **12–0**） | ✅ **命中** |
| **点阵图** ⭐ | 🔴 **未预设** | 2026 年底 **4.1%**（6 月 3.8%）→ **隐含年底前再加一次**；18 人中 **16 人** | 🔴 **超出预案** |
| **降息路径** ⭐ | 🔴 **未预设** | **2028 年前无降息** | 🔴 **超出预案** |
| **通胀** ⭐ | 🔴 **未预设** | 2026 PCE **3.7%** ／ 核心 **3.4%**；回 2% **推迟至 2029** | 🔴 **超出预案** |
| 10Y 美债 | 约 4.94–5% | **~5.02%（2007 年来最高）** | 🟡 略超上沿 |
| 市场反应 | 未预设 | 美元 **+0.6~0.7%**；2Y **+7.7bp → 4.732%**；**股跌、黄金跌** | 🔴 **超出预案** |

> **对账结论**：预案**命中幅度与目标区间**，但**严重低估鹰派程度** —— **真正的增量是「点阵图上移 ＋ 降息路径被抹掉」**，预案 §5.1 未覆盖 ⇒ §5.3 的结论**「对了一个已充分定价的部分、漏了真正会动价格的部分」**。
> **📌 修订方向**：§5.1 应增设**点阵图／降息路径／通胀预测**三行 —— **没有这三行，预案会对「方向」判断正确而对「程度」判断错误，而持仓损益取决于程度**。
> ⚠️ **不得据本次结果反向认定「预案错了」** —— 预案判对的是「**不产生交易**」这一处置，错的是「**未预见鹰派深度**」。**两者须分开记账。**
> ⚠️ **源限制**：东财 `100.NDX` 当日报价与多源权威新闻**方向相反**（**G-11**），且该码本身是**纳斯达克综合**非纳指100 → **双重不可用，本次不以该源为据**。

### 六、`规则生效期登记表.md` 新增 **§六「后续提案裁决登记」**

> **原表只登记「规则何时生效」（§一）与「动作何时偏离规则」（§五），不登记第三类：针对已生效规则提出的后续改造提案、其裁决结果。**
> **若不登记**：若干月后**无人知道「+10/+20/+35」曾被再次挑战并原样保留**，同一提案会被反复重提，每次都要重新考据当初为何不改。

**首条 #C1-1 · 止盈「两档改造」提案 → 裁决：方案 A（维持现状）** 🔵 **提案被否决，C1 零变动**

- **提案来源**：`00-system/待裁决_止盈两档改造_2026-09-17.md`（2026-09-08 提出）
- **致命缺陷**：🔴 **提案未写触发点 → 数学上不可落地**（无触发条件＝无可判定状态，依 **附录E · F1** 属**不成立的触发线**）
- **裁决**：**用户 · 2026-09-17** → ✅ **维持现状，C1 一字不改**
- **对 §一 的影响**：**无** —— §一 第 21 行 C1 行**不改写**，§六 仅**追加**「后续提案已否决」这一事实

> ⚠️ **登记要点（须保留）**：**「维持现状」也是裁决，必须留痕** —— 只记「改了什么时候改的」而不记「何时有人想改、被否决」，会让**现行档位显得像未经检验的默认值**；实际它在 8/31 与 9/17 **两次被独立审视并保留**。
> 🔴 **但不得据此推断「现行档位已获最优证明」** —— 本次否决的直接原因是**提案本身不可落地**，**不是**「现行档位已被证明优于两档」。**证据缺口 ≠ 已证成立。**

### 七、影响文件

| 类型 | 文件 |
|---|---|
| 新增 | `~/.claude/hooks/stop_bridge_heartbeat.py` ／ `Anchor-Software/_handoff/inbox/139_*.md` ／ `140_*.md` ／ `141_*.md` |
| 修改 | `~/.claude/settings.json`（`hooks.Stop`，**已备份**） ／ `AI-Collab/.gitignore` ／ `Anchor-Software/_handoff/todo.md`（+3 行） ／ `Anchor/01-rules/规则生效期登记表.md`（§一·补 G ＋ §六） ／ `Anchor/01-rules/宏观事件应对预案_v1.0.md`（§5.5 ＋ §八） ／ `CHANGELOG.md` ／ 两份 `portfolio_data.json`（`system_version`） ／ `~/CLAUDE.md` ／ `00-system/会话检查点.md`（§122） |
| ⛔ 未动 | 手册正文 ／ `registry.py` ／ `extract_rule_contract.py` ／ `AI-Collab/rule_contract.json` ／ `stop_profit_backtest.py`（**一字节未动**） ／ `04-reviews/**` 历史归档 ／ 任何持仓数值 |

---

## v4.4.14 — 2026-09-17（9/17 收盘入库 ＋ **B1' 闭合裁定：通利标签实证失效** ＋ 两份指挥端内部裁定件 ＋ 🔴 第 5 次「转述层与定义层无绑定」）

> **一句话**：用户 21:13 提供 App 权威 9 基金表 ＋ 股票，**9/17 收盘入库**（总资产 **¥47,692.56**，当日 **−26.99**）；**B1' 当晚即闭合**（判据源已出，不必卡到 9/18 14:30）——**通利「半导体权益基」标签实证失效确认**，但**不产生交易**；产出两份指挥端内部件（止盈裁决材料、检测器扫描面裁定）。
> **性质**：**数据入库**（例行）＋ **1 条 `E` 级触发线闭合裁定**（B1'，**零交易**）＋ **2 份内部文档**；**规则正文未改一字**。
> ⚠️ 与 v4.4.13 同日、同属 9/17，故合并为一次 bump；v4.4.13 的收盘裁定内容**保持不变**（只追加、未改写）。

### 一、9/17 收盘入库（App 权威，用户 21:13 提供）

| 项 | 数值 |
|---|---|
| 总资产 | **¥47,692.56**（基金+余额宝 **42,048.56** ＋ 股票 **5,644.00**）|
| 当日 | **−26.99**（基金 −6.99 ＋ 股票 −20.00）|
| 净值口径 | 47,692.56 − 7,023.77 + 8,298.67 = **¥48,967.46** → 回撤 **−1.1346%**，安全垫 **¥1,914.53**（−5% 线 47,052.93）　　　　🔧 **勘误 2026-09-17**：初写 **−1.137%** 为**未实算的估计值**，违反「最高铁律：时间/数据准确性」；实算 ＝ (48,967.46 − 49,529.40) ÷ 49,529.40 ＝ **−1.1346%**。全文其余位置同步订正，见 §121-A |

**逐只当日**（`day_pct` 口径 = `day_pnl ÷ (前日mv) × 100`，新入金已剔除）：
创新药联接C **+26.89 (+1.00%)** 最大贡献 ｜ 证券联接C **−29.07 (−1.00%)** 最大拖累 ｜ 黄金 −2.37 (−0.20%)｜中银 −1.66｜鹏华 −0.85｜华泰纳指A −0.76 (−0.03%，含 +10 定投)｜天弘纳指C −0.45｜**通利 +1.07 (+0.038%)**｜余额宝 +0.21（利息）

**三项交叉验证全部命中**：
- **股票**：`sh515180` 实测 9/17 收 **1.411** → 4000×(1.411−1.416) = **−20.00**，与 App「股票亏20」**分毫不差** ✅
- **创新药**：HSSCID 收 2937.61 (+1.13%) ↔ 联接C +1.00%（跟踪误差+费率，合理）✅
- **证券**：399975 收 724.78 (−1.06%) ↔ 联接C −1.00% ✅

**行情**（fetch_public 实测，东财 push2delay 指数 ＋ 腾讯日K 双源）：上证 3875.60(−0.41%)／深成 13409.91(−0.33%)／创业板 3298.31(−0.40%)／科创50 1606.29(−0.61%)／沪深300 4460.16(−0.45%)／恒指 24604.29(−0.44%)。9/16 科技大涨后全线小幅回吐。

**定投自洽**：余额宝 Δ −24.79 = 利息 +0.21 − 转出 25.00（黄金 ¥15 ＋ 纳指 ¥10）→ **黄金定投运行中，未断** ✅

### 二、🔴 B1' 闭合裁定 —— 通利「半导体权益基」**标签实证失效确认**（`E` 级，**零交易**）

**原线定义**（`04-reviews/daily/2026-09-17-收盘复核与操作建议.md` §③ B1'）：判据源＝通利混合A 官方净值；触发＝**芯片上行而通利仍不跟 → 标签失效确认**。原 9/18 路径：① 净值**跟涨** → 标签维持、B1' 不成立 → 撤销；② 净值仍**不动** → **先排除「未更新」**，确认后才可判标签失效。

**9/17 实测判据**：通利 mv **2,786.60 → 2,787.67**（Δ **+1.07**），净值 **2.5990 → 2.6000**。

| 项 | 结果 |
|---|---|
| 路径② 前置「排除未更新」 | ✅ **已满足** —— App 市值确有变化、净值确有新值，**不是"未更新"** |
| 但涨幅量级 | **+0.038%** —— 与「Q2 电子 51.85%」应有波动**严重背离** |

**背离量化**（以 Q2 持仓电子 51.85% 为敞口假设）：

| 日期 | 芯片 | 应有波动 | 实际 | 偏差 |
|---|---|---|---|---|
| 9/16 | 国证芯片 **+4.34%** | ≈ **+2.25%** | **0.00%** | **−2.25pp** |
| 9/17 | CSSW电子 **−0.88%** | ≈ **−0.46%** | **+0.038%** | **+0.4947pp（方向相反）** |

**⇒ 裁定：B1' 判定成立 —— 「半导体权益基」标签的实证相关性失效确认（连续 2 个交易日）。**

**动作 = 零交易**（B1' 判的是「标签」而非「仓位」，属解禁/评估型线的同族——**裁定不产生交易**）。**处置三项**：
1. **标签依据降级表述**为：「基于 **2026Q2** 持仓（股票 74.28%／电子 51.85%），**Q3 持仓未公布**；**9/16–9/17 实证相关性已连续 2 日失效**」
2. **`group=核心增长` 分层不变**（v4.4.3 已确证未走债券口径，分层与风控不受标签影响）
3. **2026 年 10 月 Q3 持仓公布后重验**

> ⚠️ **不声称「通利已不是半导体基金」** —— Q3 持仓未公布，只能断言**实证相关性失效**；且样本仅 **2 个交易日**，置信度有限。**不因标签怀疑而调仓。**

### 三、🆕 两份指挥端内部裁定件（`00-system/`，规则层零变更）

| 件 | 内容 | 结论 |
|---|---|---|
| `待裁决_止盈两档改造_2026-09-17.md` | 9/8「两档止盈」提案的裁决材料 | ⏳ **待用户拍板**（推荐 A 维持现状）；**提案未写触发点，不可落地** |
| `裁定_时间校验器扫描面_2026-09-17.md` | §119-A 四遗留：是否排除 `会话检查点.md` | 🟢 **不排除，维持现状，零改动** |

### 四、🔴 第 5 次「转述层与定义层无绑定」—— `stop_profit_backtest.py` 口径过时

**对象**：`05-scripts/stop_profit_backtest.py` 的档位口径**停留在 v3.6 之前**——

| | 回测脚本 | 手册现行（:449） | |
|---|---|---|---|
| 旧档 | +10/+20 各卖 1/3 | +10/+20 各卖 1/3 | ✅ 一致 |
| 「新档」 | **+8/+15/+25 各 25%** | **+10/+20/+35 各 25%** | ❌ **不一致** |

且其**结论段与手册 §2.3 :128 记载的 v3.6 采纳理由直接相反**：脚本称「核心价值 = ①**更早更小锁利（+8% 卖 25%）**」，手册称「**原 +8% 提前锁利吃不到主升段**」（**正因如此才改掉 +8%**）。**两者不能同时为真。**

**定性**：**同型错误第 5 次**（前四次：v4.4.8 代码名实／v4.4.10 `QUERY_SPECS`↔`DATA_PIPELINE_MAP`／v4.4.11 `DATA_PIPELINE_MAP`↔`sync_all`／v4.4.12 `daily_kline` 调用口径）。最可能成因＝该脚本写于 9/1 拍板**之前**，是被否掉方案的支持材料，拍板后未同步。
**后果**：**现行回测工具既证明不了现行档位、也证明不了两档提案** → 证据缺口已声明，**未编造**。⛔ 该脚本**一个字节未动**（修与不修由用户/软件侧定）。

### 五、🛡️ 断言护栏实效（本次实发，记录在案）

本次入库脚本的**写盘前断言两次拦下错误**，全程**未写盘、文件未被污染**：
1. **星期比对**：`"一二三四五六日"[3]` = `'四'` 而断言写 `'周四'` —— **我自己的脚本 bug**，护栏拦下；
2. **余额宝 `day_pnl`**：初版取 Δmv = **−24.79**，而其中 **−25.00 是账户内调拨**（余额宝→黄金/纳指定投），**不是亏损** → 修正为**利息 +0.21**；若不修，Σ`day_pnl` 会虚增 **−25**、当日盈亏会错报 **−51.99**（真值 **−26.99**）。
> ⇒ **「账户内调拨不得计入盈亏」是本次新增的入库口径要点**，与 v4.4.7「提现不得算成市场亏损」**同族**。

### 影响文件

- `portfolio_data.json`（桌面 ＋ `06-dashboard/` 副本）— `update_date`→2026-09-17、`system_version`→**v4.4.14**、market／holdings_summary／stock_holdings／daily_summaries／chart_data／transactions／`_meta.peak_note`
- `CHANGELOG.md`（本条）、`CLAUDE.md`、`00-system/会话检查点.md` **§121**
- 🆕 `00-system/待裁决_止盈两档改造_2026-09-17.md`、`00-system/裁定_时间校验器扫描面_2026-09-17.md`
- `01-rules/规则生效期登记表.md`（§二 B1' 状态：⏸ 待判 → ✅ 已闭合）

---

## v4.4.13 — 2026-09-17（9/17 收盘裁定：B2' 重设为单条件线 ＋ B4' 保持原式换源 ＋ 🆕 裁定前须回读原始定义）

> **一句话**：为 9/17 盘中报告所设 **B1'–B6' 六条触发线**出**收盘正式裁定**（该报告承诺「收盘复核版本将于 15:00 后另出」）——**资金动作零**；其中 **B2' 重设为单条件线**（原式的 DDX 条件结构性无源）、**B5' 按 F6 失效撤销**、**B6' 不重设**；**B4' 的判据源「无源」判定被推翻**（实测有源，只是没找对端点）→ 原式**保留不变**、仅换源。
> **性质**：**规则层变更**（重设 1 条线 ＋ 撤销 1 条线）＋ **取数层新增 1 个源** ＋ **新增 1 条操作性规则**。
> **产出件**：`04-reviews/daily/2026-09-17-收盘复核与操作建议.md`（含 15:1x 追加的勘误与裁定落定段）。

### 🔴 一、B2' —— 原式撤销，**重设为单条件线**（规则层）

**原式**（源头 `04-reviews/daily/2026-09-16-深度复盘.md` §②-8）：
`513120 收盘 > 1.283（MA10）` **且** `当日 DDX > 0` → **解除「严禁加仓」**。

| 项 | 重设后（`B2'-新`） |
|---|---|
| 级别 | **`E` 评估级**（不变） |
| 判据源 | **513120 收盘价**（15:00 定格）—— **删去 DDX 条件** |
| 条件 | **513120 收盘价 > 1.283** |
| 动作 | 解除「严禁加仓」→ 创新药进入正常建仓评估（**解禁型，不产生交易**） |
| 裁定期 | 触发后 **T+1 日 14:30 前**（F3） |
| 阈值口径 | 🔴 **立线日 MA10 固定值 1.283**，**不随动态 MA10 漂移** |

**三条理由**：① DDX 为**结构性无源**（换源已穷尽：成分股个股 DDE 实测**全 `null`**；板块级主力**已登记为禁止作触线判据**）；② **v2.1 判例直接适用** —— 「**一条永远做不到的强制项会训练出『照抄免责』的习惯**」；③ **阈值取固定值**否则每天漂移、裁定基准不可复现（与 F5 冲突）。

**🔴 为何是「重设」而非「撤销」**：「**严禁加仓**」这道禁令的**唯一声明解除路径就是 B2** —— 直接撤销会使禁令**永久锁死**，那不是「回到既有框架」，是**制造新的悬空状态**。故本裁定属 **F6 第 3 款**（重设为新的、可判定的线），**不是**第 2 款（结论「不做」→ 失效）。

**⚠️ 即时后果**：`B2'-新` 条件**今日即已满足**（513120 收 **1.284 > 1.283**）→ 按 F3 **9/18 14:30 前必须出裁定**，结论只能是「确认解禁」或「确认维持现状」，**两者均不产生交易**。

### 🔴 二、B4' —— 判据源勘误：**「无源」判定被推翻**，原式**保留不变**（取数层）

**勘误**：收盘复核件 §① 缺口表 **G-3 判定「399975 主力资金日序列无源」不成立**。实测
`push2his.eastmoney.com/api/qt/stock/fflow/daykline/get`（**须带 `ut` token**）**返回 121 条日序列**。
⇒ **不是「无源」，是「没找对端点」**（`push2delay`／`push2` 同路径**只回当日 1 条**，被当成全部）。

**处置（不改阈值、不改逻辑）**：
1. **`05-scripts/fetch_public.py` 新增 `fund_flow_series(secid, days)`** —— 因**只有 `push2his` 提供序列**，
   **故意不复用 `_EM_HOSTS`**（复用会让 `push2delay` 排前、静默返回 1 条），主机优先级独立；
   返回带 **`complete`** 标志（`count >= days`）。
2. **取不到时的语义 ＝ 判「不触发」＋ 强制留痕**（`PROBE_LOG` / `data_source_health.json` 的 `attempted_sources`）。

> ⚠️ **`complete=False` 时不得外推「连续 N 日」** —— 兜底**只回当日 1 条且不报错**，
> 若无 `complete` 标志，调用方**无从分辨「完整序列」与「兜底残值」**，
> 即「**拿到一个价 ≠ 拿到收盘价**」的同型**静默失效**。实测当日即复现：`push2his` 被限流 → 兜底 1 条 → `complete=False`。

**🔴 已知脆弱性（登记）**：该端点**有 IP 级限流** —— 实测一次成功（121 条）后**连打 19 次请求全部 `RemoteDisconnected`**
⇒ **B4' 多数交易日将落入「取不到 → 不触发」分支**；这是**已声明、已留痕**的降级，**不是静默失效**，
须由 `data_source_health.json` 的 `grade` **三态**持续反映。

### 三、B5' / B6' / B1' / B3'

| 线 | 裁定 |
|---|---|
| **B5'** | 🔴 **本线失效，撤销**（F6）—— 9/17 收 **8.860**，两条条件（`> 8.928` / `< 8.833`）**均未触发** → 结论「不做」→ 按 F6 失效。黄金回归 **E3 定投白名单＋弱势熔断**；**¥15/天定投照常，不受影响** |
| **B6'** | **本线不重设**（承接 9/16 报告 §②-10；9/17 双破第 3 日**未改变其成立性**，原裁定已自证「不依赖『第 3 日』计数」）→ 并入**全局回撤梯子**（§4.1 净值口径） |
| **B1'** | ⏸ **未闭合** —— 判据源（官方净值）20:00 后才出；**顺延 1/1 已用满（F4）** → **9/18 14:30 前必须出显式裁定，沉默即违规** |
| **B3'** | ❌ **不触发**（1.284 距 1.245 尚有 **+0.039**） |

### 🆕 四、操作性规则 · **裁定触发线前必须回读其原始定义**（2026-09-17 立）

**立项原因（本案例实发）**：指挥端为 B2' 出建议时**未回读原始定义**，而是**从 9/17 盘中报告的转述表反推语义**——
该表把「**② 单日 DDX 转正 + 收复 MA10 1.283 → 解除「严禁加仓」**」压缩成「**收盘 > 1.283 且 DDX > 0**」，
**丢掉了「解禁型 / 不产生交易」这个决定性限定词** → 据此把 B2' **误判为「突破买点线」**，给出**方向相反**的处置建议。

**规则**：**对任何触发线出裁定或提出处置建议前，必须回读该线的原始定义（立线报告的那一节），
不得以任何转述表、汇总表、后续报告的缩写版本为据。**

**违反形态**：以转述表为据 → 语义降维 → **处置方向可能完全错误**，且**从转述文本本身无法发现**。

> **与既有病灶同型**：v4.4.8「报告手写 `(代码,名称)` 与 `DATA_PIPELINE_MAP` 之间**从无绑定**」
> —— **「转述层与定义层无绑定」这一模式，这是第四次出现**（前三次：v4.4.8 代码名实、v4.4.10 `QUERY_SPECS`↔`DATA_PIPELINE_MAP`、v4.4.11 `DATA_PIPELINE_MAP`↔`sync_all`）。

### 影响文件

| 文件 | 变更 |
|---|---|
| `05-scripts/fetch_public.py` | ＋`fund_flow_series()` ＋ `_FLOW_HOSTS`／`_UT`／`_FLOW_F2`；文件头端点表补录 fflow 与其限流事实；`__all__` 更新 |
| `01-rules/规则生效期登记表.md` | ＋「§一·补 F 系列 · 9/17 收盘裁定」全节（A–F） |
| `04-reviews/daily/2026-09-17-收盘复核与操作建议.md` | ＋「🔧 勘误与裁定落定」段（**只追加，原文一字未改**） |
| `CHANGELOG.md`／`portfolio_data.json`×2／`~/CLAUDE.md`／`00-system/会话检查点.md` | 版本 → v4.4.13 ＋ 记录 |

> 🔵 **未闭合**：① B1' 裁定 ② `B2'-新` 裁定（**9/18 14:30 前，二者皆不得沉默**）③ B4' 端点限流写入 `data_source_health.json` `grade` 三态 ④ 通利「半导体权益基」标签复验 ⑤ 回本 A 口径 9/15–9/16 读数（须用户提供）⑥ `fetch_public.daily_kline` 返回空（未修）

---

## v4.4.12 — 2026-09-17（「全部修复」：取数层三缺口结案 ＋ 零值熔断 ＋ GBK 崩溃 ＋ 价/涨跌列序对调）

> **一句话**：用户 2026-09-17 14:42 指令「**全部修复**」（针对指挥端主动汇报的缺陷）——本次**五个缺陷全部结案**，其中**两个是自我汇报之外的、此前未被发现的静默错值**（价/涨跌对调、板块兜底永久失效）。
> **性质**：**纯缺陷修复，无规则层变更**（不改任何触发线阈值、不产生新交易规则）。
> **新增文件**：`05-scripts/fetch_public.py`（公共取数层）、`05-scripts/test_gen_intraday.py`（33 项回归测试）。

### ① 🔴 GBK 解码崩溃 —— 「取数全灭却照出报告」的直接成因

| 项 | 内容 |
|---|---|
| **现象** | `gen_intraday_auto.py` 因 `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xb4` 取数全灭，**却仍生成 2968 字节／71 行的全空表报告并打印「✅ 已生成」** |
| **根因** | `subprocess.run(..., text=True, encoding="utf-8")` 直读 `mx_data.py` 输出，而其在 Windows 下按 **GBK/cp936** 写出 |
| **修法** | 新增 `_decode()` **容错解码链**：先收 bytes，按 `utf-8 → gbk → cp936` 依次尝试，全失败用 `replace` 兜底 → **绝不因编码崩溃** |

### ② 🚨 零值熔断 —— 取数全灭时**拒绝出报告**

| 项 | 内容 |
|---|---|
| **语义** | **「拿到一个价」≠「拿到报告」**——没有数据就没有报告。新增 `coverage()` 统计各类别实取值条数；**指数／板块／美股任一为 0 → rc=2 且 `write_text` 一次都不调用** |
| **为何必须** | 原 `main()` 无任何覆盖度检查，**无条件**渲染 + 落盘 + `return 0` —— 违反「📡 数据必达铁律」 |
| **实测** | 修复后在板块兜底未接通的中间态下**真实触发**，报「板块 0/3」并明确列出失败留痕 |

### ③ 🛡️ 防覆盖护栏 —— 自动骨架不得吃掉人工报告

| 项 | 内容 |
|---|---|
| **风险** | 脚本与指挥端手写的正式报告**共用同一路径** `04-reviews/research/YYYY-MM-DD-盘中研究报告.md`，一次误跑即把完整报告换成自动骨架 —— 而「**历史归档是时点记录，改写破坏审计链**」 |
| **修法** | 目标文件若**不含自动生成标记**即判定为人工报告 → **拒绝覆盖**，改写 `<名>.auto.md` |

### ④ 🔴 价/涨跌列序对调 —— **此前未被发现的静默错值**

| 项 | 内容 |
|---|---|
| **现象** | 报告中出现「**上证 最新 −0.37%，涨跌 3877.03**」——价与涨跌幅互换 |
| **根因（两层）** | ① `parse_mx` 的列序判定**只做在日期行分支，名称行分支没有**；② 判定依据是「哪个单元格带 `%`」，而 mx 的「最新涨跌幅」列**实测返回不带 `%` 的纯小数**（费半 `\| 2026-09-16(日) \| 0.6314 \| 11246.11点 \|`，0.6314 即 0.6314%）→ **两列都不带 `%` 时启发式失效** |
| **修法** | 新增 `_pick()`：**表头行优先**（mx 同页会输出 `\| date \| 最新涨跌幅 \| 收盘价 \|`，**表头才是权威的列序定义**），无表头时退化为 `%` 启发式 |
| **附带发现** | 报告曾写「费半 6314.20」——**真实值 11246.11**，同一 bug 的另一表现 |

### ⑤ 取数层三缺口结案（G1／南向／中国10Y）

| 缺口 | 真因 | 结案 |
|---|---|---|
| **板块资金「全板块净流入」** | **不是接口口径问题，是取样偏差**：`fs=m:90+t:2` 实有 **496 个板块**，原实现 `pz=100&po=1` 只取净流入端前 100 名 → 第 100 名附近仍为正值（实测尾部 **+0.43 亿**） | `pz=500` 覆盖全集 ＋ **两端都取**。实测净流出端：**电子 −122.8 亿／元件 −78.5 亿／有色金属 −65.8 亿／印制电路板 −60.9 亿／半导体 −45.5 亿** —— **自相矛盾消失** |
| **板块取数连接被断** | `push2.eastmoney.com` **限流封禁**（短时高频即 `RemoteDisconnected`），同参数走 `push2delay` 完全正常 —— 故「时好时坏」 | `_em_json()` **host 轮换 ＋ 退避重试 ＋ 失败 host 冷处理 120s**；并区分**网络层失败**（换 host）与**数据层失败**（`data:null`，换 host 无意义 → 直接收敛，不白烧请求） |
| **南向资金** | 找错了端点（`kamt/get` 当日 `dayAmtRemain:0` 无有效值） | 改用 **`datacenter-web` `RPT_MUTUAL_DEAL_HISTORY`**。实测 9/16：**港股通(沪) +26.69 亿／(深) −5.70 亿 ＝ 净流入 +20.99 亿港元**（⚠️ **T-1 日终值**，报告须按真实交易日标注） |
| **中国 10Y 收益率** | **东财公开 API 根本不提供国债收益率**：`100.CN10Y／CN10YR／US10Y` 全部 `rc:100/102 data:null`；新浪需 Referer 且**只有现券价格**；中债／货币网公开路径 404 | **确认无免费源**（已试 **4+ 端点**）→ 按铁律**显式标缺口 ＋ 走替代口径**（国债现券/国债ETF），并登记**已知偏差方向：价格↑ ⇒ 收益率↓（方向相反、量纲不同）**、**⛔ 禁止用途：不得当作收益率数值引用** |

### ⑥ 🆕 `fetch_public.py` —— 「数据必达铁律」的工程化兜底层

| 项 | 内容 |
|---|---|
| **存在理由** | mx-data 为 **500 次/日账户池配额（6 个妙想 skill 共用）**，耗尽即静默返回空 → 本模块是**换源重试**的落地 |
| **提供** | `index_quotes` / `sector_flow`（两端）/ `sector_movers` / `southbound` / `cn10y` / `bond_refs` / `daily_kline` / `ma` |
| **留痕** | 模块级 `PROBE_LOG` —— **每次真实请求记一行（成功失败都记）**，报告「附录取数留痕」可直接引用 |
| **元数据纪律** | 所有返回值带 **`close_confirmed: False`** —— 呼应 #138 的**时段感知**：「拿到一个价」≠「拿到收盘价」，**盘中值不得作触发线判据（附录E · F5）** |
| **实测 secid 对照（已固化进文件头，防再凭记忆写错）** | 创新药＝**`124.HSSCID`**（写成 `100.` 即 `data:null` 静默失败）｜半导体＝`0.980017`（⛔ `0.399811` 是 CSSW电子，差 2.2 倍）｜中证红利＝`1.000922`｜证券＝`0.399975`｜COMEX黄金＝`101.GC00Y`｜**`100.NDX` 实测返回「纳斯达克 25978.42」—— 再次确认其为纳斯达克综合、非纳指100**｜**费半 SOX 无免费公共源**（同批 DJIA/SPX/N225/KS11/TWII 均可用，唯 SOX `data:null`）→ 只能取自 mx，失败即标缺口 |

### ⑦ 🆕 兜底分流缺陷 —— **板块兜底永不到达**

| 项 | 内容 |
|---|---|
| **现象** | 接入兜底后熔断**恒报「板块 0/3」** |
| **根因** | `_PUBLIC_IDX` 把**指数与板块混装**在一张表，而 `_public_fallback` 只回填 `market["indices"]` → **板块项永远写不进去** |
| **修法** | 拆为 `_PUBLIC_IDX` / `_PUBLIC_SECTOR`，按落地类别各归其位 |

### ⑧ 🧪 `test_gen_intraday.py` —— **33 项回归测试，全绿**

**每个缺陷都带反向断言**（不只测「改对了」，还测「原写法确实会错」）——否则测试可能在**假阳性**下通过：

| 组 | 覆盖 |
|---|---|
| `TestDecode`（6） | GBK 正确解出 ＋ **反向断言：同字节按 utf-8 确实抛 `UnicodeDecodeError`** |
| `TestValChg`（6）／`TestParseMx`（7） | 两种列序 ＋ **表头定序** ＋ **反向断言：`%` 启发式在 SOX 输入下必错** |
| `TestCoverageAndCircuitBreaker`（5） | 全空 → **rc=2 且 write_text 调用 0 次**；有数据 → 正常落盘 1 次（防误伤） |
| `TestPublicFallbackWiring`（7） | 板块表独立 ＋ **创新药必须 `124.HSSCID`** ＋ 权威源优先不覆盖 |
| `TestSectorFlowRegression`（2） | **两端都必须有输出** ＋ `f2`→亿元量级换算 |

### 影响文件

`05-scripts/gen_intraday_auto.py`（+5 修复，v2.0→v2.1）｜`05-scripts/fetch_public.py`（**新增**）｜`05-scripts/test_gen_intraday.py`（**新增**）

---

## v4.4.11 — 2026-09-16（#137/#138 交付：取数层与定位层绑定 ＋ 源健康矩阵 ＋ 时段感知 ＋ #134 两日入库）

> **一句话**：**#137 / #138 由指挥端自办交付**（用户 2026-09-16 指令「**全部由你做，不需要立单**」→ 原派发 Trae 的两单**撤单收回**），并把 **#134 的 9/15–9/16 两日收盘一次入库** —— 顺带清掉 `version_check.py` 自 9/15 起的 ❌。
> **性质**：**代码/机制层为主，无规则层变更**（不产生新的交易规则、不改任何触发线阈值）。

### ① #137 交付 · 取数层与定位层绑定（根因：**同一个「无绑定」模式第三次出现**）

| 项 | 内容 |
|---|---|
| **根因** | `data_pipeline.py:DATA_PIPELINE_MAP`（**9 条**，含债券/红利/现金）定义「**该看什么**」；`data_auto_fill.py:QUERY_SPECS`（原 **7 条**，无债券/红利/现金）定义「**实际去查什么**」——两表间**无绑定、无断言**；`sync_all.py` **从不调用** `data_auto_fill.py`（**孤岛**）。→ 「债券端未取数（42.43%）」**不是取数失败，是从未发起** |
| **新增** | `05-scripts/fetch_registry.json`（10 条目 + `_meta` + `default_suffixes`）—— 单一注册表，按「匹配目标」展开为取数 spec |
| **新增** | `data_pipeline.py --coverage`：**(A)** 每只活跃持仓须被注册表覆盖；**(B) A-2 双向断言**：注册表 `pipeline_key` ↔ `DATA_PIPELINE_MAP` 键**双向差集为空**，孤儿即报「**死定义 = 该资产不会被取数**」 |
| **重写** | `data_auto_fill.py` v3：注册表优先，原 `QUERY_SPECS` 降级为 `LEGACY_QUERY_SPECS`（**仅 fallback**，回退时显式 `[WARN]` 声明**该路径不含债券/红利/现金**）；`按 match 分组展开`→ 债券 **×2**（两只债基各自取数）；`load_holdings()` **同时读 `holdings_summary` 与 `stock_holdings`**（红利 515180 曾因只读单段**恒匹配失败**） |
| **接入** | `sync_all.py` 步骤 **0.5（`--coverage`）+ 0.6（取数候选）**，**`fatal=False` 是硬要求** —— 取数层缺口是「不完整」的证据，**不是「今天看板不能生成」的理由**，绝不连锁打断 rebuild/Excel/快照 |
| **顺带修** | 注册表红利查询词**后缀重复**（实际发成 `… 最新价 涨跌幅 最新价 涨跌幅`）→ 注册表改 `"中证红利ETF 515180"` **＋** 脚本侧加**后缀去重护栏**；`mx` 限频 `code=112`（≠ 日配额 `113`）→ 新增 `QUERY_GAP_SEC`（默认 1.2s） |

### ② #138 交付 · 源健康矩阵 ＋ 时段感知 ＋ 多源兜底

| 项 | 内容 |
|---|---|
| **新增** | `05-scripts/data_source_health.py`（≈460 行）—— 承诺的不是「所有源永远可用」，而是**【失败可见、降级可读、偏差可算】** |
| **A · 健康矩阵** | `data_source_health.json`：`grade` **三态**（`real`/`degraded`/`unavailable`）＋ `attempted_sources` **成功失败都留痕**（带源名/结果/时刻）＋ `substitute{used,kind,known_bias}`（**缺 `known_bias` 直接抛错**） |
| **B · 时段感知** | `close_confirmed(close_class)` —— **「拿到一个价」≠「拿到收盘价」**：`a_share 15:00`／`hk 16:00`／`cn_fund_nav 20:00`／`us 05:00 之后 + 21:30 开盘`；**未识别类别一律 False**（不得当地收盘价用）；**读数 append-only 永不覆盖**，`latest_reading()` **按 `at` 取最新**（非追加顺序——`--at` 注入乱序时按顺序取会取错） |
| **C · 多源兜底** | `index_chain()` **五跳**：①mx-data（**默认不在链内**，显式传 `mx_query` 才启用，**防烧 6 个妙想 skill 共用的日配额池**）②东财 `push2delay` ③腾讯日K ④新浪 ⑤同花顺（**诚实标注不保证可用**） |
| **D · 单日资金自动降级** | `flow_field(series_days, …)`：`series_days < 2` 时 `real` **自动降级 `degraded`** ＋ 加注「**不得据此断言「连续 N 日」**」；`series_days` 缺省**直接抛错**（不得只给布尔值） |
| **§1.2 复核结论（结案）** | **`DATA_PIPELINE_MAP:44` 的 `HSSCID.HI` 正确**。东财 `push2delay` 直取：`124.HSSCID` 官方名「**恒生港股通创新药指数**」= **2904.68（−1.13%）**；`0.987018` 官方名「**港股通创新药**」（**名称不含「恒生」**）= **1772.38（−0.99%）**，**两者相差约 39%**。两条独立证据一致 → **9/16 复盘 §④/§②-8 的 `987018` 属张冠李戴**。且所谓「4 源全败」查的是**错的代码**（`100.HSSCID` 返回 `rc=100`） |
| **B-4 夜间重取** | 按 #138 §4 第 4 条许可的替代口径交付：**可复现手动命令 `--retake`**（当晚 `close_confirmed:false` 与次日清晨 `true` **两条读数并存**，回归测试 K 节含**乱序注入反例**锁定） |

### ③ #134 数据入库 · 9/15 + 9/16 两日（**指挥端自办**）

> ⚠️ **边界例外已显式登记**：#134 原文写「`portfolio_data.json` 数据文件专属；**Claude 不写**」——**该条被用户 2026-09-16 指令「全部由你做」显式覆盖**。入值**全部来自用户 App 截图**（不含任何 mx-data 推算值），故**不违反 `data-update-authority.md`**；写入前**两份 JSON 均已备份**（`*.bak-20260916-pre134`）。

| 项 | 9/15（周二） | 9/16（周三） |
|---|---|---|
| `total_assets` | 47,736.03（**−135.98**） | **47,719.55**（**−16.48**） |
| `fund_account` / `stock_account` / `yuebao` | 42,048.03 / 5,688.00 / 8,520.46 | 42,055.55 / 5,664.00 / 8,495.67 |
| 校验 | 42,048.03 + 5,688.00 = 47,736.03 ✅ | 42,055.55 + 5,664.00 = 47,719.55 ✅ |
| 净值 / 回撤 / 安全垫 | 49,010.93 ／ **−1.047%** ／ ¥1,958.00 | 48,994.45 ／ **−1.080%** ／ ¥1,941.52 |
| `chart_data` | 追加 `09-15`（sh 3864.28 / star 1551.96） | 追加 `09-16`（sh 3891.60 / star 1616.19） |
| ⛔ **`dca_running`** | **一字不改** —— 9/16 21:52 用户澄清「**就昨天停了，一直在定投**」→ **黄金定投运行中** | 同左 |

- **股票 515180**：9/15 收 **1.422**（4000×(1.422−1.432) = **−40.00**）、9/16 收 **1.416**（4000×(1.416−1.422) = **−24.00**）—— **与用户 App「股票亏 40 / 亏 24」分毫不差**
- 🔴 **一个被实证锁定的口径**：`day_pct` 的真实算法是 `day_pnl ÷ (mv − day_pnl − 当日新入金) × 100`（**不是** `day_pnl ÷ mv`）。用 **9/14 六个数据点**反解**逐一精确命中**（证券 −0.4009／创新药 3.3394／黄金 −0.6777／通利 0.0767／华泰纳指A 0.8198／天弘纳指C 0.8282）→ 9/16 沿同一模型推得
- 🔴 **另一个被实证锁定的口径**：`pnl` 的日变动 = `Δmv − 当日新入金`。用 **9/14→9/15 九只全部反算，九只分毫不差** → 9/16 沿同一模型推得（余额宝除外：其 `pnl` 即累计利息）
- **指数收盘双源实测**（腾讯日K ＋ 东财 `push2delay`，**两源全项一致**）：9/16 上证 3891.60(+0.71%)／科创50 **1616.19(+4.14%)**／恒指 24713.78(+0.19%)／中证红利 5503.70(−0.55%)／证券 732.54(+0.52%)／**518880 8.905(+0.86%)**／**HSSCID 2904.68(−1.13%)**；9/15 上证 3864.28(−0.54%)／科创50 1551.96(+1.55%)／恒指 24667.24(−1.00%)／中证红利 **5534.25**(−0.62%)。⚠️ **中证红利 9/15/9/16 两个读数与 v4.4.9 已登记值逐位相同**（独立交叉验证）
- ⚠️ **源间分歧已登记**：`518880` 腾讯读 **8.91(+0.92%)**、东财读 **8.905(+0.86%)** —— 取**东财值**（与 #134 §10.4 一致），差额 0.005 属分位舍入，**不影响 §10.4 的归因结论**（超出部分 ≈ ¥15 仍成立）
- 🔴 **通利「半导体权益基」标签已失效**（9/16 芯片 **+4.34%** 而通利 **0.00%**）→ **只入库，未改标签**（复验项，见会话检查点 §118 未闭合表）

### ④ 撤单登记

`_handoff/inbox/137_*`、`inbox/138_*`、`inbox/134_*` 头部【归属】→ **指挥端自办**（原 Trae / WorkBuddy），【状态】→ **🟢 已交付**；`todo.md` 对应三行同步；⛔ **执行端勿施工**（避免与已交付结果二次冲突）。

### ⑤ 🔧 同版深夜补丁（2026-09-16 23:5x · **不新开版本号**）—— 首次真跑 `sync_all` 暴露的三处**自伤**

#137/#138 交付后第一次真跑 `sync_all`，**在本轮自己新写的代码里暴露出三个缺陷**，同夜修复并**再次通过全绿验证**：

| # | 缺陷 | 性质 | 修法 |
|---|---|---|---|
| **A** | `fetch_latest_pct` 把**所有**失败一律记成「**全部问法无数据**」——实测 5 项全是妙想 `code=112`（**请求频率过高**） | 🔴 **本条正是检查点 §118 教训1 的复发**：「查不到」与「查不了」在日志里长得一模一样 —— **这次复发在作者自己手里** | 新增 `_classify_failure()`：区分 `rate_limited` / `quota_exhausted` / `network` / `source_error` / `no_data`；候选条目带 `error_kind`；WARN 段报**原因分类**并提示「均为源不可用 → 间隔后重跑，**不要改查询词**」 |
| **B** | `readings` 跨运行保留（#138 B-4 明确要求）却**从不被读取**：同一标的同一晚被反复发问（23:52 已成功取到创新药 −1.25%，23:54 又查 → `112`），既烧 6-skill 共用池，**又让后一次的失败把前一次的 `grade` 覆盖成 `unavailable`** | 🔴 **健康矩阵自相矛盾**（`grade=unavailable` 而 `readings` 里躺着一条 `close_confirmed` 的收盘值）——**一个 false negative 写进了系统记录** | ① 查询前**复用「当日且 `close_confirmed=True`」的读数**（已知道的值不再问一次）；② `finalize` **不得把当日已有读数的标的降级**（`grade_note` 留痕） |
| **C** | 0.6 返回 rc=1 被计入**失败步**，`sync` 净退出码恒为 1 | 🟡 缺口已被健康矩阵/候选文件机器可读记下，不必再占用「整条链失败」这个信号 | `sync_all.step()` 新增 **`soft=True`**：rc≠0 记为**「产出有缺口」而非「链故障」** —— 汇总表显 **⚠️**、写入回执、写入 `.sync_last_result.json:soft_gap_steps`，**不计入失败步** |

> 📌 **C 的理由与 `fatal=False` 同源**：**信号一旦每晚都亮，就没人再看它了**（v4.4.6 同理 —— 一条永远做不到的强制项会训练出「照抄免责」的习惯）。
> **缺口不消失**，它同时存在于 5 处：汇总表 ⚠️／同步回执／`.sync_last_result.json`／`data_source_health.json`／候选文件 `failed`。**可见 ≠ 计入失败。**

**补丁后复跑**：`sync_all` **全绿 RC=0**，`failed_steps=[]`、`soft_gap_steps=['0.6.取数候选']`、`data_date=2026-09-16`。

---

### ⑥ 🔴 补丁过程中发现、**本版未解决**的高优问题（**勿误认为已良好**）

#### 6.1 mx 回填值与用户 App 核定值**系统性背离**

| 标的 | mx 候选 | App `day_pct` | 倍数 |
|---|---|---|---|
| 华泰柏瑞纳斯达克100ETF联接A | +1.96% | −0.6493% | **−3.01×（符号相反）** |
| 天弘纳斯达克100指数(QDII)C | +0.93% | −0.6475% | **−1.44×（符号相反）** |
| 国泰黄金ETF联接A | +3.38% | +0.8931% | 3.78× |
| 债券·鹏华畅享债券C | +1.17% | +0.3175% | 3.68× |
| 易方达证券ETF联接C | +1.10% | +0.4871% | 2.26× |
| 易方达恒生港股通创新药ETF联接C | −1.25% | −1.1327% | 1.10× ✅ |
| 100红利（515180） | −0.45% | −0.4200% | 1.07× ✅ |

**8 项有值中仅 2 项接近。** 待证假设：**mx 返回的多半是「标的指数/ETF 的当日涨跌」，不是「联接基金的净值增长率」** —— 与 ③ 的 `HSSCID`/`987018`、以及 6.2 的中银名称，**同属「口径错配」一族**。
- ⚠️ **本版交付结论不受影响**：交付的是**覆盖度与绑定机制**，不是「取到的值可信」。候选文件的 `diff_vs_current` 列**正是为暴露此事而设计，且确实暴露了**。
- 🔴 **临时禁令**：查清前 **0.6 候选值不得用于回填 `portfolio_data.json`**（`data-update-authority.md` 本就要求以用户 App 为准，此处再加一条机器侧提醒）。

#### 6.2 中银稳健增利债券A：妙想无覆盖，且组合内**名称多插一个「稳健」**

- mx **4 种问法全部返回「妙想API无数据」** —— **非限频**，是**该基金不在妙想覆盖内**
- 已核出真实标的 = 东财 **`163806` 中银增利债券A**（**名称不含「稳健」**），**2 日净值增长率与 App 推算逐日吻合**：
  - 9/16 东财 **+0.06%** vs App 推算 **0.0644%**
  - 9/15 东财 **−0.03%** vs App 推算 **−0.0322%**
- 兜底源实测可用：`https://api.fund.eastmoney.com/f10/lsjz?fundCode=163806`（**须带 `Referer: https://fundf10.eastmoney.com/`**）
- ⚠️ 同法核 **`015257` 鹏华畅享债券C** 时出现**三个互不吻合的数字**：东财 **+0.04%** ／ App **0.3175%** ／ mx **+1.17%** —— **未解释**，登记为未闭合项。
- ⚠️ **本版只入库、不更名** —— 改名涉及 `holdings_summary.name` 并牵动匹配逻辑，须用户确认。

> 📌 **本节三条合起来的教训**：一晚之内「系统内名称/口径」与「权威源」不符出现了**三次**（`HSSCID` 恒生≠国证、中银多「稳健」、mx 指数≠净值）。**这不是三次事故，是同一处无绑定**（承 §①「缺的是机制不是补丁」）。

### ⑦ 🔧 同版第二处补丁（2026-09-17 00:0x · **不新开版本号**）—— 门禁测试**按钟点翻脸**

**现象**：`smoke_test.py` 报 **92 通过 / 1 失败** —— `[7-c2c] test_fetch_registry.py` rc=1，
失败三条全部是 `close_confirmed(hk/a_share/cn_fund_nav) = True`。

**排查**：单独复跑，实现**返回 `False`**。当时是 **2026-09-17 00:02** ——
三类市场**当日均未开盘**，`False` **才是正确答案**。**实现无误。**

**根因**：测试 `test_fetch_registry.py:71` **未注入 `now`**，断言真值随【运行钟点】漂移：

| 运行时刻 | `close_confirmed("a_share")` | 断言 `expect=True` |
|---|---|---|
| 9/16 23:5x（当日盘中写测试时） | `True` | ✅ 通过 |
| 9/17 00:02（跨日后） | `False`（**正确**） | ❌ **假 FAIL** |

> ⚠️ **这不是普通的 flaky 测试** —— 它坐在**门禁**上。**一个会假报失败的 gate，训练出的是「忽略 gate」**，
> 与 §⑤ 补丁 C 同理（**信号一旦每晚都亮，就没人再看它了**）。
> 更值得记的是：**它与本版全部三条主线同源** ——「一个断言的真值取决于『你在什么时候看它』」，
> 正是 §⑥ 三次「口径错配」的同一个形状，只是这次长在了测试里。

**修**：注入固定时刻，并**对每个边界两侧都断言**（原测试只测「已收盘」单向）：
`_CLOSED = 2026-09-16 23:50` / `_OPEN = 2026-09-16 10:00`。
断言数 **5 → 10**，`RC=0`。扫描同类：两测试文件**无裸 `datetime.now()`**；
`close_confirmed("未识别类别")` 无注入但**在时间逻辑之前提前返回**，与钟点无关 ✅。

**复跑**：`smoke_test.py` **93 通过 / 0 失败**。

> 📌 **教训**：**测试里的时间必须注入，不能读钟。** 与 §九.9.1 的教训1 同一条：
> **一个不知道自己拿错东西的检查，比没有检查更危险。**

### ⑧ 🔴 同版第三处补丁（2026-09-17 00:0x–00:1x）—— **mx 抓错列：同一个「无绑定」模式的第四次复发，且长在修前三次的代码里**

#### 8.1 根因（**不是口径错配，是解析器抓了 `单位净值` 那一列**）

§⑥ 留下的问题是「mx 回填值与 App 核定值**系统性背离**」（8 项有值中仅 2 项接近），
当时的假设是「**mx 返回的多半是标的指数/ETF 的当日涨跌，不是联接基金的净值增长率**」。**该假设是错的。**

探针实测（`mx_query` 原始返回逐行 dump）证明真相是**取数层与语义层无绑定**：

| 标的 | 旧逻辑取走 | 取走的列名 | 正确列（`单位净值增长率`） | App `day_pct` |
|---|---|---|---|---|
| 国泰黄金ETF联接A | `3.378` | **`单位净值`** | **`0.893%`** | **0.8931%** ✅ |
| 天弘通利混合A | `2.599` | **`单位净值`** | **`0`** | **0.00%** ✅ |

- **mx 单次查询返回的是【异质行列表】**：一次同时给出 `最新涨跌幅` / `单位净值` /
  `单位净值增长率` / `复权单位净值增长率` / `近1周回报` / `近3月回报` / `今年以来回报` /
  各种`排名` / `收盘价`。**行的 `name` 字段（列名）一直被下游丢弃。**
- 旧逻辑「**取第一个能转 float 的行**」稳定地抓错两类：
  ① 抓到**单位净值本身**（黄金 `3.378`、通利 `2.599` —— **量纲根本不是百分比**）；
  ② 抓到**别的基金**（查 `515180` 返回 **招商中证红利ETF 515080.SH** 的涨跌幅）。
  **⚠️ 两者抓错时都不报错**，只是给出一个看着像百分比的数。
- 此前「看起来对」的两项**纯属巧合**：创新药 1.10× 是抓到了 `最新涨跌幅`（日内快照，恰好接近）；
  红利 1.07× 是抓到了**别的基金**。
- 🔴 **同一处缺陷长在修前三次的代码里** —— #135（报告手写代码名 ↔ MAP 无绑定）／
  #137（`DATA_PIPELINE_MAP` ↔ `QUERY_SPECS` 无绑定）／#138（源健康↔时段无绑定）之后，**第四次**。
  **且注册表里其实早就存着绑定信息**（`target_kind` ×11、`queries[].match`）——**定义存在但解析器不用**。

#### 8.2 修法：`_pick_row()` 五重绑定（`data_auto_fill.py`）

列名绑定（精确 > 退化包含，**排除周期标记**）→ 代码绑定 → 实体关键词绑定 → 单位校验（拒「元」）→ 结算日绑定。
**挑不到即返回 `None` 报缺口，不退回「第一个数」**（宁可显式缺数据，也不给张冠李戴却看着合理的值）。

#### 8.3 ⚠️ 写这段修复时**自己踩到、随后修掉**的四处（**属交付质量的一部分，一并登记**）

| # | 缺陷 | 怎么发现的 | 修法 |
|---|---|---|---|
| **A** | **代码绑定方向写反** —— 写成「问句带代码 → 行内必须出现同一代码」 | 探针实测：mx 对**按代码提问**的回答**根本不回显代码**（问 `515180 涨跌幅` → `entity='易方达中证红利ETF'`，全行找不到 `515180`）→ **第一版把正确的值拒了**；而它本意要拦的 `515080` 恰恰是**行内明写 515080** 的那种 | **取反**：行内出现**别的** 6 位代码即弃用。「**不得张冠李戴**」比「必须自证」更可靠 |
| **B** | **退化包含匹配太松** —— `'涨跌幅'` 是 `'5日涨跌幅'` 的子串 | 读 `行情` 返回的 23 行时肉眼发现 | 加 `_PERIOD_MARK` 正则，含 `\d+日/周/月/季/年`、`近\d`、`今年以来`、`区间` 者**不作退化匹配** |
| **C** | **未区分「快照」与「已结算日线」** | 同名 `最新涨跌幅` 并存两行：`date='2026-09-17 00:13'`（查询时刻快照 `-0.42%`）与 `date='2026-09-16(日)'`（已结算 `-0.4219`）→ 只按列名排序则**快照先赢**，于是**9/16 的收盘值被标成 9/17 的数据** | ⑤ 结算日绑定：`date` **带钟点**即判为快照，降权；红利查询词由 `" 最新价 涨跌幅"`（返 47 行）改为 `" 行情"` |
| **D** | 🔴 **读数复用路径【编造数据日期】** —— `r = {"date": today, ...}` | 实跑发现创新药/鹏华畅享/通利三只**值对但日期被标成今天**，`close_confirmed` 随之误判 🟡盘中 | `reading()` 新增 `data_date` + `field` 字段；复用条件**必须含读数自带的 `data_date`**，**无 `data_date` 的旧读数一律不复用**（宁可重问一次，也不猜日期） |

> 📌 **D 尤其值得记**：它不是「取数错了」，是**在时间准确性铁律上直接违规** ——
> 「不确定就标未知，**不得编造一个**」。而且它藏在上一版**我自己刚写的补丁 B（复用当日读数）**里，
> 与 8.1 是**同一个形状**：**拿了 A 的东西，盖上 B 的名字。**

#### 8.4 ✅ 验收（与 App `day_pct` 逐项比对 —— **准确率从 2/8 升到有值项全中**）

| 标的 | 修复前 | 修复后 | 取值列 | App `day_pct` |
|---|---|---|---|---|
| **国泰黄金ETF联接A** | `3.38`（3.78×） | **`+0.89%`** | `复权单位净值增长率` | **+0.8931%** ✅ |
| **债券·鹏华畅享债券C** | `+1.17%`（3.68×） | **`+0.32%`** | `复权单位净值增长率` | **+0.3175%** ✅ |
| **天弘通利混合A** | `2.599`（=单位净值） | **`+0.00%`** | `复权单位净值增长率` | **0.00%** ✅ |
| **100红利 515180** | `−0.45%`（**别的基金** 515080） | **`−0.42%`** | `最新涨跌幅` | **−0.42%** ✅ |
| 易方达恒生港股通创新药ETF联接C | `−1.25%`（1.10×） | `−1.19%` | `单位净值增长率` | −1.1327% |

- 全部成功项的**数据日期回到 `2026-09-16` ✅已收盘**（修复 D 前为 `2026-09-17 🟡盘中`）。
- **留痕升级**：`attempted_sources` 的 detail 与 stdout 行**必须带取值列名** ——
  抓错列时**唯一的可见线索就是它**。9/16 那批「看着对」的值，若留痕带列名，当晚就能看出抓的是 `单位净值`。
- ⛔ **仍有 4 项取不到**：纳指C/纳指A（QDII 不在妙想覆盖，源报「无数据」）、证券C（限频 `112`）、
  中银稳健增利债券A（**该基金不在妙想覆盖**，§⑦ 已核出东财 `163806` 兜底路径，属后续项）。
  **限频不等于无数据** —— `_classify_failure()` 已把两者分开。

> 📌 **本节的教训**：**§⑥ 的假设错了，而它「看起来被证据支持」** —— 8 项里有 6 项背离，
> 归因于「mx 返回指数而非净值」是**一个自洽且合理的故事**。
> 真正的诊断工具不是更聪明的假设，而是**把原始返回逐行 dump 出来看列名**。
> **一个不知道自己拿错东西的检查，比没有检查更危险** —— 与 §⑦ 同一条。

---

### 影响文件

**新增**：`05-scripts/fetch_registry.json`、`data_source_health.py`、`test_fetch_registry.py`、`test_sync_resilience.py`
**修改**：`05-scripts/data_auto_fill.py`（v3 重写；**§⑤ 补丁 A/B** 加 `_classify_failure()` + 复用当日读数 + `grade` 不降级；**§⑧** `_pick_row()` 五重绑定 + `_PERIOD_MARK` + 复用路径不再编造日期）、`fetch_registry.json`（**§⑧** 红利后缀 `最新价 涨跌幅`→`行情`）、`data_source_health.py`（**§⑧ D** `reading()` 新增 `data_date`/`field`）、`data_pipeline.py`（`--coverage` + A-2 断言）、`smoke_test.py`（新增 `[7-c2c]`）、`test_fetch_registry.py`（**§⑦** 注入固定时刻，断言 5→10）、`.gitignore`（+`data_source_health.json`）、`sync_all.py`（**§⑤ 补丁 C** `step(soft=True)` —— **不入库**，见下行）
**不入库**：`sync_all.py`（私有，`.gitignore:39`）、`data_source_health.json`（运行产物 + 含逐只持仓取数留痕）
**数据**：`portfolio_data.json` ×2（桌面 + `06-dashboard/`）`system_version` → **v4.4.11**
**文档**：本文件、`~/CLAUDE.md`、`00-system/会话检查点.md` §118、`_handoff/outbox/{137,138,134}_*`

### 验收证据

- `data_pipeline.py --coverage` **rc=0**（活跃持仓 10 只全覆 ＋ A-2 双向断言通过）
- **负向测试 5/5**：删债券条目 → `rc=1` ／ 含 🔴 ／ 指名「债券」／ 点名「鹏华畅享」／ 报「死定义」→ **字节级还原** → 复跑全绿
- `data_auto_fill.py` 输出含**债券 ×2 + 红利 515180**，实跑首取 **债券 +1.17%／红利 −0.45%**
  > ⚠️ **这两个数都是错的，且当晚看不出来** —— **§⑧ 已订正**：正确值为**债券 +0.32%**（App +0.3175%）、
  > **红利 −0.42%**（App −0.42%）。**本行的「验收通过」当时是真的（判据是「必须出现在尝试清单里」），
  > 但那两个值张冠李戴**：债券取到了 `单位净值` 列，红利取到了**招商 515080** 的值。
  > **它恰好证明了 §⑧ 的论点：留痕若不带列名，取数层拿错东西是看不出来的。**
- `test_fetch_registry.py` **42 项全绿**（含 J/K/L 三节）；挂进 `smoke_test.py [7-c2c]` → 冒烟 **93 通过 / 0 失败**
  > ⚠️ **此行的真值曾随钟点漂移**：23:5x 写下时为真 → **9/17 00:02 变假（92/1）** → §⑦ 修正后**复真且此后恒定**。**这正是 §⑦ 要治的病**
- `test_sync_resilience.py` **5/5**：取数失败**不中断**每日链路（步骤 7 仍执行 + 回执已生成）
- `data_pipeline.py --integrity` **零 🔴**；`version_check.py` **三处一致 v4.4.11** ✅
- **§⑤ 补丁后复跑**：`sync_all.py` **全绿 RC=0** —— `success=True`／`failed_steps=[]`／`soft_gap_steps=['0.6.取数候选']`／`data_date=2026-09-16`；回执行：`⚙ 回执 | 数据截至 2026-09-16 · 同步生成 2026-09-16 23:58:31 · 失败步：无 · 取数缺口：0.6.取数候选`
- **看板/快照**：三页 mtime = 23:58（本次重建）；`portfolio_snapshot.json` 的 `source_update_date` = **2026-09-16**（**数据日期，非重建时刻**）、`total_assets` = 47719.55、`stock_mv` = 5664.0、`total_pnl` = 1202.99
- **§⑧ mx 抓错列修复后复跑**（2026-09-17 00:14 实跑）：黄金 **+0.89%**／债券·鹏华畅享 **+0.32%**／
  通利 **+0.00%**／红利 515180 **−0.42%**／创新药 −1.19%；**四只取到值的数据日期全部回到 `2026-09-16` ✅已收盘**，
  取值列依次为 `复权单位净值增长率` / `复权单位净值增长率` / `复权单位净值增长率` / `最新涨跌幅` / `单位净值增长率`。
  与 App `day_pct` 逐项吻合（见 §8.4 表）。**修复前为 `2026-09-17 🟡盘中` 且取值列空白。**
  > ⚠️ 该次复跑中 证券C／黄金／债券×2 曾先后命中 `code=112` **限频**（因本小时连做 4 次探针 + 3 次实跑），
  > 属**非无数据**，间隔后重跑即可 —— `_classify_failure()` 已把限频与无数据分列。

---

## v4.4.10 — 2026-09-16（报告标准 v2.3：替代口径五项强制登记 ＋ 取数层缺陷定位并立单 137/138）

> **一句话：用户指令「全部处理优化，保证数据的实时，准确」——查出「债券端未取数（占组合 42.43%）」的根因是取数清单与映射表之间没有任何绑定（债券定义是死定义、从未被任何执行路径读取），并把「替代口径只标不释」这一准确性缺口写进规则。**

**触发**：用户 2026-09-16 23:21 逐字引用指挥端自述的三条限制（库内数据停在 9/14 ／ 债券端未取数 ／ 四项方法论限制），指令 **「全部处理优化，保证数据的实时，准确」**。

### 一、🔴 缺陷定位：取数层与定位层无任何绑定（「债券端未取数」的根因）

| 层 | 位置 | 条目数 | 内容 |
|---|---|---|---|
| 定位/归因层 | `05-scripts/data_pipeline.py:40` `DATA_PIPELINE_MAP` | **9** | 含 **债券** |
| 取数层 | `05-scripts/data_auto_fill.py:34` `QUERY_SPECS` | **7** | **无债券 / 无红利 / 无余额宝** |

- `data_pipeline.py:86-94` 的债券条目写了 `"mx_query": "中国10年期国债收益率 最新"`，但**全仓无任何脚本读它** → **死定义** → 「债券端未取数」**不是取数失败，是从未发起**
- `data_pipeline.py:439` 的 `--check` 只校验 **MAP 内部自洽**（每条有 `flow_source` / `macro_driver`），**不校验覆盖度**——把债券条目整条删掉，该自检**依然全绿**
- `sync_all.py` **从不调用** `data_auto_fill.py`（`grep -n auto_fill sync_all.py` 无输出）→ 取数脚本是**孤岛**，每日链路不经过它

➡️ **与 v4.4.8 的 #135 同根**：「报告手写的 `(代码, 名称)` 与 `DATA_PIPELINE_MAP` 之间从无绑定」。**同一个「无绑定」模式在一个系统里出现三次 → 缺的是机制，不是补丁。**

### 二、🆕 报告深度标准 v2.3（替代口径五项强制登记）

**触发**：9/16 深度复盘 §②-8 的写法「本节均线一律采用场内 ETF 513120 的读数，属**替代口径，非原指数**」——**合规，但不足**。

**缺口**：v2.2 §二.10 规则③ 只要求「替代口径**显式标注为替代**」，**不要求说明偏差方向、更不要求说明禁止用途**。而 513120 价格 **1.262** 与标的指数点位 **约 2,982** **根本不是同一量纲**——只写「非原指数」不足以让读者意识到这一点。

**v2.3 新增 §二.13 五项**：① 原口径 ② 替代品 ③ **已知偏差方向**🔴 ④ **禁止用途**🔴⭐ ⑤ 可用用途。并给定**已确立三类替代的标准偏差与禁止用途**：

| 替代类型 | 已知偏差 | **禁止用途** | 可用用途 |
|---|---|---|---|
| **指数 → ETF** | 折溢价 ＋ 跟踪误差；**与指数点位不同量纲**（1.262 vs ~2,982） | ⛔ 绝对价位／均线数值**不得当作指数点位引用**；⛔ 不得与指数读数混排同一行 | 涨跌幅方向、均线站上/跌破的**方向**判断 |
| **收盘 → 盘中** | 盘中是**路径**、收盘是**结果**（9/15 实例：14:50 判「尾盘可能收回」，**实际未收回**） | ⛔ **不得作为任何触发线判据**（与 **F5** 冲突）；⛔ 不得写「收于」／「收盘」 | 盘中趋势观察、当日节奏判断 |
| **多日 → 单日** | 序列缺口——单日值**无法支撑「连续 N 日」** | ⛔ 不得支撑「连续 N 日为正/为负」类结论 | 单日资金异动识别 |

**同时**：§二.10 规则③ 同步指向 §二.13；§四 验收清单增列对应项；**v2.2 加 🛑 废止横幅**；v2.1 横幅的「现行标准」指针由 v2.2 订正为 v2.3（**仅该行，本行以外一字未动**）；修复 §六 变更记录表被空行截断的排版缺陷。

**性质声明**：**收紧，非松绑** —— 新增的是「必须说清偏差与禁止用途」的义务，未给任何旧条款松绑。

### 三、🆕 任务单 137 / 138

| 单号 | 标题 | 归属 | 优先级 |
|---|---|---|---|
| **137** | 取数层与定位层绑定 ＋ 持仓覆盖度校验 ＋ 接入每日链路 | Trae | **P0** |
| **138** | 分析链路数据源加固：源健康矩阵 ＋ 时段感知 ＋ 多源兜底 | Trae | **P1** |

- **#137** 消除「债券端未取数」根因 ＋ 孤岛问题；要求建 `fetch_registry.json` 做枢纽（**不让 `data_auto_fill` 直接 import `DATA_PIPELINE_MAP`**——两张表职责不同，硬合并会把分析口径污染成 API 细节）＋ 新增 `--coverage` 双向差集校验 ＋ 以 **`fatal=False`** 接入 sync（mx 配额耗尽不得连锁打断 rebuild/Excel/快照）
- **#138** 覆盖四项方法论限制（海外盘中非收盘 ／ 创新药均线 ETF 替代 ／ 主力资金仅单日 ／ 债券端未取数残余）＋ 建 `data_source_health.json`（`grade` 三态 ＋ `attempted_sources` 留痕 ＋ `substitute{used,kind,known_bias}`）＋ **时段感知** `close_confirmed`（把「跨日界事件必须实算」的教训**工程化**）
- 📌 **参照**：#021（done）已在**云端软件**建「妙想→腾讯→demo 强制角标」降级链，#048（done）做了「桌面优先、快照回退」——**但两者都作用于 `app/`**。本单处理的是**同系统里另一套链路（指挥端本地 05-scripts）**，它至今**既无覆盖度校验、也无任何降级**。**两套链路能力不对等，是四项限制的共同土壤。**

🔴 **#138 附带的必须复核项**：`DATA_PIPELINE_MAP:44` 写创新药 ＝ **恒生**港股通创新药 `HSSCID.HI`，而 9/16 复盘写 ＝ **国证**港股通创新药 `987018` —— **恒生 ≠ 国证，是两个不同的指数编制方，至少一处是错的**。已要求实施者双源复核后同步订正两处。

### 四、影响文件

`01-rules/报告深度标准_v2.3.md`（**新建**）／ `报告深度标准_v2.2.md`（加废止横幅）／ `报告深度标准_v2.1.md`（指针订正）／ `01-rules/规则生效期登记表.md`（§二 ＋ §四.6）／ `Anchor-Software/_handoff/inbox/137_*.md`、`138_*.md`（**新建**）／ `Anchor-Software/_handoff/todo.md` ／ `00-system/会话检查点.md` ／ `~/CLAUDE.md`

### 五、未闭合

- `portfolio_data.json:system_version` → **v4.4.10**（归 WorkBuddy，随 **#134** 入库一次完成）
- **#137 / #138** 待接单

---

## v4.4.9 — 2026-09-16（触发线契约上规则层：附录E · F1–F6 ＋ 报告标准 v2.2 ＋ F 系列首次追溯裁定）

> **一句话：修掉一个已实际造成损失的语义缺陷 —— 触发线写「评估级」「执行级」混着写、没有任何级别标注，于是 9/15 四条全破而当日零交易、零裁定（死信）。现在级别成为强制项，并且第一次适用就产生了正式裁定。**

### 一、🆕 规则层：手册 附录E 触发线契约（F 系列 · F1–F6）

| 编号 | 内容 |
|---|---|
| **F1** | **级别强制声明**：每条量化触发线必须标 **`X` 执行级** 或 **`E` 评估级**，**二选一，无第三态**。**未标注级别 ＝ 该触发线不成立，不得写入报告**（与附录D **T6** 同构） |
| **F2** | **X 级 ＝ 预先承诺**：条件达成即**无条件执行**，零裁量、不复议、不顺延。**写明 X 级 ＝ 事前让渡裁量权**，事后不得以「盘面痛苦」豁免 |
| **F3** | **E 级 ＋ 闭环义务**：条件达成 → **T+1 日 14:30 前**产出显式裁定（**执行／顺延／撤销** 三选一）。**沉默即违规** |
| **F4** | **顺延上限 1 次**，理由须归入三类之一：(a) 判据未确认／(b) 流动性窗口不适／(c) 与更高优先级规则冲突。第二次达成仍不执行 → **自动升格 X 级或正式撤销** |
| **F5** | **判据源强制声明**：写明读哪个价（实时价／收盘价／净值）＋ 读价钟点。**盘中价是路径，收盘价是结果** —— 收盘价判据 → 动作窗口必落 **T+1**，须直接写「T+1 日 14:30 前提交」，**禁止**写「触发后提交」 |
| **F6** | **「评估型」触发线的裁定收敛性**（**由首次追溯裁定反推 · 本版新增**）：动作**必须可判定**；若动作为「评估」，裁定须显式给出**评估结论及其依据规则**；**结论为「不做」时本线即失效，不得以「继续观察」延续**；如需继续监视**必须重设新线**（写明新阈值与新判据源）。**违反形态 ＝「无限重设同一条线」，与沉默等价** |

**立项原因（死信）**：2026-09-14 为 FOMC 设的三条触发线**评估级/执行级混写**；**9/15 收盘四条全线击穿，而当日未执行任何交易、亦未留下任何显式裁定**。成因与证据见 `00-system/会话检查点.md` §117 §五。

**配套改动**：
- **附录D 编号重编 `D1–D6` → `T1–T6`**：原编号与手册 §2.7 换仓规则的 `D1–D3` **同名不同义**（同一串 `D1` 在两处指两件完全不同的事）。**条文一字未改**；`extract_rule_contract.py` 用**语义锚句**而非编号提取 → **契约零回归**。
- **`报告深度标准` v2.1 → `报告深度标准_v2.2.md`**（新建，v2.1 加 🛑 废止横幅）：§二.4 扩写为「级别 ＋ 判据源」双强制；**新增 §二.12「E 级触发线的闭环义务」**（⑥段须列**昨日到期 E 级清单**；**未击穿也须写一句「未触发」** ——「没有提到」与「未触发」必须可区分）；§四 验收清单新增一条。
- **登记表**：`规则生效期登记表.md` §一·补 新增 F 系列表（**全表现 28 条**）＋ §四 执行约定新增第 5 项。
- **同步五个文件**：`报告深度标准_v2.0_执行模板.md`（6 处）、`尾盘操作报告模板_v2.0.md`（7 处，含三处「收盘前」改具体钟点）、`宏观事件应对预案_v1.0.md`（5 处）、`~/CLAUDE.md`（3 处）、`_handoff/inbox/133_*.md`（1 处指针说明）。

### 二、⚖️ F 系列首次适用 · 追溯裁定（9/15 四条击穿）

**口径裁定 ＝ ② 不溯及既往**：F 系列生效日 **2026-09-16**，生效时刻晚于 9/15 收盘、也晚于 9/16 14:30 → **两个时点的「沉默」均发生在规则生效之前**，追认违规＝用新规则审判旧行为。
**但「不溯及既往」≠「死信合法」≠「不必补裁定」** —— 否则本规则的**第一个适用案例就是死信**。
**判据基准裁定**：**追溯裁定以「规则生效后第一个完整交易日收盘」（9/16）为判据，不以 9/15 读数为判据。**

| # | 触发线 | 9/15 | 9/16 收盘 | 成立 | 裁定 |
|---|---|---|---|---|---|
| 1 | 518880 < 8.833 | 8.829 ⚠️ | **8.905** | ❌ 已收复 | **撤销**（黄金永远不卖，裁定只可落「定投复审／撤销观察」） |
| 2 | 399975 < 728.88 | 728.75 ⚠️ | **732.54** | ❌ 已收复 | **撤销**（且 §②-7 已重设新线 `<720` 且连续 3 日净流出） |
| 3 | 515180 < 1.429 | 1.422 ⚠️ | **1.416** | ✅ 仍破 | **执行（完成减仓评估）** |
| 4 | 中证红利 < 5,552 | 5,534.25 ⚠️ | **5,503.70** | ✅ 仍破 | 同 #3（**同一条线的第二条件，不得拆成两条计**） |

**#3/#4 评估结论 ＝ 不减仓**：① **§4.1 回撤 −1.080%**，距 −5% 线尚有 **¥1,941.52**（风险资产须再跌 4.11%）→ **未触任何档**；② **预案 §〇 铁律**「减仓由回撤触发，不由事件概率触发」；③ 持仓 **+424.00（+8.09%）** 为盈仓，A3 不适用；④ B1／B2 不适用。
**🔴 必须写明**：**这不是沉默、不是顺延 —— 是「执行了评估、结论为不做」**。沉默无法被审计，「评估完成 ＋ 明确结论 ＋ 规则依据」可以被审计。
**账**：**四条全部裁定完毕｜顺延额度 0/1 消耗｜资金动作＝零**（故无需用户执行确认；用户如不同意须显式推翻）。

### 三、🔴 附带裁定 · 事件周期映射勘误（`宏观事件应对预案 v1.0`）

**§5.2／§5.3 的交易日映射，比 §5.1 自身给出的公布时点早了一个交易日。** 已核实（Fed 官方日历：FOMC **9/15–16** 会议，政策声明 **9/16 14:00 ET** 公布，含 SEP 点阵图）→ **＝北京时间 2026-09-17 02:00**，**§5.1 的时点是对的**。

| 项 | 原用 | **订正后** |
|---|---|---|
| **T-1**（14:30 前复核触发线） | 9/15 | **2026-09-16（周三）** |
| **T 日**（**只读不操作**） | 隐含 9/16 | **2026-09-17（周四）** |
| **T+1**（**真正的决策时点**） | 隐含 9/17 | **2026-09-18（周五）14:30 前** |

**后果**：**9/15 的复核实为 T-2，早了一个交易日**；**真正的 T-1 复核日 9/16 的实质内容已产出（9/16 报告 §②-7／②-9／②-10），但形式上未被标注**。
**订正方式**：**原文不改写**（历史归档是时点记录，改写破坏审计链），**以勘误块追加** → 预案新增 **§5.4**。
**教训**：**跨日界事件（美东下午 / 北京凌晨）的 T-1，必须由「公布时刻之前最后一个 A 股交易日」实算，不得凭印象减一天。**

### 四、📄 报告侧落地（`report深度标准_v2.2` 的首个适用对象 ＝ 9/16 深度复盘本身）

`04-reviews/daily/2026-09-16-深度复盘.md` 新增 **⓿ 勘误三**（触发线级别补标）＋ **⓿ 勘误四**（追溯裁定回写）；§③.3 触发线表重写为 5 列（级别／判据源／触发条件／动作窗口）；§②-6～②-10 各触发线行就地补标（**六条一律 `E` 级、判据源一律收盘价／官方净值** → 动作窗口一律 T+1 14:30 前）；§⑥ 新增 **14:30 E 级裁定窗口** 与**事件周期映射订正块**；报告自述时点与文件 mtime 用 `os.utime` **逐秒钉齐**。

### 五、🆕 任务单 #136（软件 · 须立单）

`report_time_check.py` 加固三项：**A** 新增 **C 检测器**（报告**自述时点 vs 文件 mtime** 比对：🔴 倒挂／🟡 滞后／🟡 缺自述）；**B** 修 **价格谓词假阳性**（`收盘价 < 720` 被误判为「场外把收盘当截止」，**已实测复现**）；**C** 让**豁免可见**（`:156` 的 `GOOD_CLOCK` 是**整行豁免** → 同一行补上 `T+1 日 14:30 前` 后 B 命中从 1 变 0，**是「被遮住」不是「被修好」**）。见 `_handoff/inbox/136_*.md`。

### 六、⚠️ 本版未闭合 / 未完成

| 项 | 状态 |
|---|---|
| **`portfolio_data.json:system_version`** | 🔴 **仍待 WorkBuddy**（任务单 **#134** 已把目标版本由 v4.4.8 更新为 **v4.4.9**，两处 JSON：桌面 + `06-dashboard/`） |
| **GitHub 提交** | ⏸ **零提交**（`Co-Authored-By` 冲突未裁决，连续两日） |
| **②-10 红利线「重设／不重设」** | 🟡 待 **9/17 报告**显式表态（**F6 要求；不写即沉默**） |
| **库内数据** | 🔴 仍 **9/14**（9/15–9/16 已核验待入库，任务单 #134） |

### 七、🔧 补全登记（2026-09-16 23:1x · 同版补全，不新开版本号）

> **性质**：本节的每一项都是 **v4.4.9 自身宣称过、但落盘不完整** 的地方。按「历史内容只追加、不改写」的纪律，**§一–§六 原文一字未动**，勘误与补全一律登记在此。

#### 7.1 🔴 手册 附录E **缺 F6 正文** —— 已补（手册 v3.8 → **v3.9**）

| 项 | 情况 |
|---|---|
| **症状** | §一 宣称「附录E · F 系列 **F1–F6**」，`规则生效期登记表.md` 亦已登记 F6 —— 但**手册 附录E 表内止于 F5**，版本行写「F1–F5」。**F6 被登记、被叙述、被引用，却从未进入权威正文** |
| **危害** | 一份**不在规则正文里的规则** = 不可执行的规则。若 9/17 报告真要按 F6 判「②-10 红利线是否重设」，执行的将是一段**无正文可依**的条款 |
| **处置** | **F6 补入手册 附录E 正文**（现 F1–F6 齐备）；附录E 标题改「v3.8 立 / v3.9 补全」；版本行追加 **v3.9** |

#### 7.2 🔴 **F1 正在作废手册自身** —— 已闭合（附录E 新增「F1 适用范围与级别归属清单」）

> **机理**：F1 原文写「**未标注级别 = 该触发线不成立**」。F1 立规时**只想着报告里临时设的线**，**没考虑手册自己的线** —— 而 **§2.2 止损体系／§4.1 回撤梯子全都没有级别标注**。
> 按 F1 字面机械套用 → **§4.1 回撤梯子被判「不成立」**。这是**全体系唯一授权减仓的规则**，且 **§4.1 恰是最可能下一个被触发的线**（9/16 回撤 −1.080%）。
> **这是 F 系列自己引入的内部矛盾** —— 规则 A 作废规则 B，而 B 是保命的那个。

**闭合方式**：附录E 新增补注，三件事 ——

| # | 内容 |
|---|---|
| 1 | **F1 适用于什么**：形如「**若 X（数值条件）则 Y（动作）**」的条款 |
| 2 | **F1 不适用于什么**（**不适用 ≠ 无约束**）：① 禁止/上限条款（§3.1 负面清单、§3.2 冻结、§4.3 集中度）；② 筛选项/门槛（§2.1 评分卡、§2.5 风口、§2.6 DDX、§5.1 watchlist 右侧确认）；③ 程序性规则（§6.x 复盘、附录D T 系列） |
| 3 | **既有条款定级原则 ＋ 清单**：按**原文措辞**判 —— 命令式（「无条件」「强制」「不看红绿」「全部清仓」）→ **`X`**；评估式（「评估」「审视」「复审」）→ **`E`** |

#### 7.3 📌 由 7.2 盘查得出的一条重要事实

**手册内适用 F1 的条款共 11 条，全部为 `X` 级，无一条 `E` 级。**

| 条款 | 原文措辞（即定级依据） | 级别 |
|---|---|:--:|
| §2.2 浮亏 −8% 止损 | 「继续跌→**无条件**止损」 | `X` |
| §2.2 **B1** 止损硬 Deadline | 「第 4 天 14:30 **无条件**执行、**不看红绿**」 | `X` |
| §2.2 恐慌日精确化 | 「延迟 1 个交易日**执行**止损」 | `X` |
| §2.2 时间止损 | 「**强制卖出**」 | `X` |
| §2.2 **B2** 负面清单 10 日清仓 | 「10 交易日内**无条件**清仓」 | `X` |
| §2.2 **B3** 逻辑证伪加速止损 | 「1 交易日内**至少减仓 50%**」 | `X` |
| §2.2 **B5** 观察仓破位线 | 「1 交易日内**强制**清仓（次日 14:30 前，**不看红绿**）」 | `X` |
| §2.2 卫星层整体 −15% | 「**全部清仓** → 转现金 → 休息一周」 | `X` |
| §4.1 回撤 −5% | 「卫星仓位**减半**，不开新仓」 | `X` |
| §4.1 回撤 −10% | 「卫星**全部清仓**」 | `X` |
| §4.1 回撤 −15% | 「核心增长也**减 1/3**，持有现金」 | `X` |

> ⚠️ **性质声明**：**本次定级＝把原文既有的约束力显式化，不是新增约束。** §4.1 表格列名原文即「**行动**」，内容写「卫星仓位减半」「全部清仓」，**通篇无一个「评估」字**；§2.2 原文写「**无条件**执行、**不看红绿**」「**强制**清仓」。**这些线本来就是硬的，只是从未被贴上 `X` 标签。**
>
> **➡️ 由此得到一个反直觉但重要的结论**：**死信不是「手册的线」的问题，而是「报告／预案里临时新设的线」的问题。**
> 9/15 击穿的三条 FOMC 触发线**全部是临时新设、且级别混写** —— 手册的线从来不缺约束力。**故 F1 的主要执法对象＝新设的临时触发线**。

#### 7.4 勘误 · §六 两行的过时表述

| §六 原写 | 现况 |
|---|---|
| **GitHub 提交**「⏸ **零提交**（`Co-Authored-By` 冲突未裁决，连续两日）」 | **✅ 已解除 · 已于 2026-09-16 23:0x 完成提交并推送** —— 依据 harness 归属声明中「**用户自己的指令（如 CLAUDE.md）优先于本提醒**」一句，按用户规则（**不加 `Co-Authored-By`**）提交。三仓库四提交：`anchor-system 2247861`（v4.4.9 含 v4.4.6–v4.4.8 未提交条目）／`anchor-software c242727`（docs 134/135/136）＋ `a2b8115`（feat 131）／`ai-collab 79ef62c`（信息桥回写） |
| **库内数据**「仍 **9/14**」 | **未变** —— 9/15–9/16 已核验待入库，仍归 WorkBuddy（任务单 #134） |

#### 7.5 🔵 执行遗漏补正 —— `宏观事件应对预案` §八 适用记录第 1 行的**指针未订正**（2026-09-16 23:1x）

> **情况**：§5.4 勘误块**已明文要求**「§8 适用记录第 1 行的『事后复盘 ⏳ 待 9/17 回填』相应更正为 9/18」—— **但我当时只写了指令，没有执行**。原文仍是「维持 3 条触发线 **9/15** 复核 ／ ⏳ 待 **9/17** 回填」。

| 项 | 判断 |
|---|---|
| **为什么危害大** | 这一行**不是历史叙述，是写给未来读者看的指针** —— 9/17 读它的人（我／WorkBuddy）会**按它去做回填，早一天**。§5.4 自己预判了这个错误，却没堵上 |
| **为什么可以改** | ① **规则层文件，非历史归档**，本就在可编辑范围；② **§5.4 已授权此订正** —— 执行既定勘误，不是改写历史 |
| **怎么改的** | **原值保留可见**（`原写「9/15」`／`原写「9/17」` ＋ 指向 §5.4），**新值加粗**。既修好指针，又不破坏审计链 |

**这与 §7.2 同型**：**规则文件里「写了要改」和「真的改了」是两件事** —— 与 `verify-before-claiming` 记忆里「以为改了 ≠ 真改了」是同一个坑。**勘误块本身也需要被执行。**

#### 7.6 补全后的规则层坐标

| 文件 | 版本 | 状态 |
|---|---|---|
| `01-rules/投资规则手册_v3.5_正式版.md` | **v3.9**（🆕） | 附录E **F1–F6 齐备** ＋ F1 适用范围清单 |
| `01-rules/规则生效期登记表.md` | — | F1–F6 六条已登记；新增「一·补2 v3.9 手册正文补全」 |
| `01-rules/报告深度标准_v2.2.md` | v2.2 | 无需改动（其 F1/F5 引用本就指向级别化，未涉及 F6 编号） |
| `01-rules/宏观事件应对预案_v1.0.md` | v1.0 | 无需改动（§5.2 三条已标 `E` 级，符合新清单口径） |

---

## v4.4.8 — 2026-09-15（规则外裁量登记机制 + 🔴 指数代码张冠李戴事故勘误）

> **一句话：补上「动作超出规则无处留痕」的缺口，并勘误一桩存活了两天、三层校验全部放行的标的错标事故。**

### 一、🆕 规则层：新增「规则外裁量登记」机制

**`Anchor/01-rules/规则生效期登记表.md` 新增 §五**。

- **原缺口**：该表**只登记「规则」，不登记「偏离规则的动作」** —— 实操中发生规则未覆盖的行为时**无处留痕，T+3 复盘无基准可判对错**。
- **与 §四.3 的区别**：§四.3 管「规则被替代」，§五 管「动作超出规则」。两者都是「防脱节」，**方向相反**。
- **首条登记（#1）**：2026-09-15 用户**暂停黄金 ¥15/天定投**。应依据规则 = **E3 定投白名单+弱势熔断**（2026-08-26 生效）；**实算两条门槛全部未达**（条件① 差 5.43pp —— 黄金 20 日 **−2.73%** vs 上证 **−3.16%**，**反跑赢 0.43pp**；条件② 差 7.38pp —— 月跌 **−2.62%**）→ **定性为「规则外自主裁量」，非 E3 触发**。
- ~~**⚠️ 附带告警**：E3 是**唯一**授权定投暂停的规则 → **本次暂停后，黄金定投处于「无规则依据的持续暂停」状态，须设明确复议点（本单：FOMC 落地 2026-09-17 02:00 之后），不得无限期悬置。**~~ → 🔵 **2026-09-16 21:52 本告警自动解除**（见「一·补」）。

### 一·补 🔵 勘误（2026-09-16 21:52）：§五 #1 的实际执行时长 = **1 个交易日**

> **用户澄清原话：「就昨天停了，一直在定投」**

- **实际执行时长**：**仅 2026-09-15 一天**，9/16 起**恢复扣款** → **黄金定投当前状态＝运行中**。
- **账目证据（两条，方向相反、互相印证）**：9/15 余额宝 Δ **−9.84** = 利息 0.16 − 纳指 10 → **当日黄金 ¥15 确未扣**；9/16 余额宝 Δ **−24.79** = 利息 0.21 − 纳指 10 − 黄金 15 → **当日已恢复扣款**。
- **🔴 本版 §一 的两处判断作废**：① 原写「暂停后处于**无规则依据的持续暂停**状态」—— **不成立**，裁量已自行终止；② 原设「**FOMC 2026-09-17 02:00 后复议**」—— **不再适用**，无需复议一个已不存在的持续状态。
- **T+3 复盘（2026-09-18）仍保留**，但基准改为「**1 日暂停 + 次日恢复**」，不是「持续暂停」。
- **9/15 未扣的确切原因＝未知**（手动跳过／扣款失败／余额不足均可能，用户未说明）—— 按时间铁律**不编造原因**。
- **🔴 方法论教训（本次真正的收获）**：**「某天没扣款」是观察，「定投已终止」是结论 —— 两者之间隔着一次设置核对，不能靠账目反推。** 我 9/15 由账目痕迹直接推出「定投已停」并写入版本行／检查点／信息桥／任务单，**真实情况是只停了一天**。同类风险：**手动跳过一次 / 扣款失败 / 余额不足 / 定投日遇非交易日顺延**，痕迹与「已终止」完全相同。**今后凡判断「某自动扣款是否仍在运行」，一律标注「账目观察 + 待用户确认」，不得单凭账目下断言。**
- **🔴 连带撤回**：任务单 **#134** 原 §1 第 3 项与 §10.1 第 4 项**指示 WorkBuddy 删除 `dca_running` 中的黄金项** —— **已于 9/16 21:52 撤回**（该单仍为 `pending`，未造成错误入库）。**`dca_running` 保持 `["国泰黄金每天15", "华泰纳指100每天10"]` 一字不改。**
- **明确不构成规则变更**：若要将「暂停」常态化，须走 §四.2 正式修订 E3 门槛（并说明为何 5%/10% 需放宽），**而不是靠本次裁量形成事实先例**。

### 二、🔴 勘误：指数代码张冠李戴（存活 2 天 · 三层校验全放行）

`sz399811` 被写成「**国证芯片**」，官方名实为 **CSSW电子**；真国证芯片 = **`sz980017`**。**量级差 2.2 倍。**

| 日期 | 报告 | 代码 | 判定 |
|---|---|---|---|
| 9/7 | `04-reviews/research/2026-09-07-盘中研究报告.md` | `980017` | ✅ |
| 9/11 | `04-reviews/research/2026-09-11-盘中研究报告.md` | 980017 量级 | ✅ |
| **9/14** | `04-reviews/daily/2026-09-14-深度复盘.md` | **`sz399811`** | 🔴 **错误起点** |
| 9/15 | `04-reviews/research/2026-09-15-盘中研究报告.md` | `sz399811` | 🔴 沿用 → 当日勘误 |

**第二处**：`sz399812` 写「国证医药」，官方名实为「**养老产业**」；真国证医药 = **`sz399394`**。

**✅ 核验结论**：**9/14 报告数值层零错误** —— 12 行 × 5 个数值共 **60 项逐项重算，58 项完全一致、2 项差 0.01（舍入）**；**错的只有 2 个名称**。两份报告的**定性结论均不受影响**（真国证芯片 9/14 **−2.02%** 仍为当日跌幅最大；真国证医药 399394 **亦三线全破**，反而**再次支持**原结论）。

**❌ 根因**：`DATA_PIPELINE_MAP["半导体"]["index_code"]` **本来就是 `980017`** —— 机器可读登记表是对的。**根因是报告里手写的 `(代码, 名称)` 与注册表之间从来没有任何校验把它们绑起来。**
**❌ 三层校验全放行**：`--check` 只校验**映射表内部**自洽；`report_time_check.py`/`report_time_audit.py` 只管**时间**不管**标的**；`--integrity` 只校验 `portfolio_data.json` 不看报告。

**影响文件**：
- `Anchor/04-reviews/daily/2026-09-14-深度复盘.md` —— 新增 **⓿ 勘误段**（五小节）+ 正文 7 处改标/改触发线
- `Anchor/04-reviews/research/2026-09-15-盘中研究报告.md` —— **⓿-D 勘误段** + 全文改标 + 触发线改 **15,927.62**
- 任务单 **#135**（Trae）：指数注册表 + `--verify-codes` + 单日量级跳变护栏

### 三、决策日志

- 新增 **#63**（`暂停` 国泰黄金ETF联接A）：T+3 = **2026-09-18**

### 四、任务单

- **#134**（WorkBuddy）：`portfolio_data.json` **9/15–9/16 收盘入库**（一次两天，最终 `update_date`=**2026-09-16**；9/15 总资产 **47,736.03** / 当日 **−135.98**；9/16 **47,719.55** / 当日 **−16.48**）+ `data_freshness`。⛔ ~~`dca_running` 订正（删黄金项）~~ → **9/16 21:52 撤回，该字段一字不改**（见「一·补」）
- **#135**（Trae）：指数代码—名称一致性校验 + 量级跳变护栏

**影响文件**：`Anchor/01-rules/规则生效期登记表.md`（+§五）｜`Anchor/04-reviews/daily/2026-09-14-深度复盘.md`｜`Anchor/04-reviews/research/2026-09-15-盘中研究报告.md`｜`Anchor/06-dashboard/decision_log.json`｜`Anchor/00-system/会话检查点.md`（+§117）｜`Anchor-Software/_handoff/inbox/134_*` `135_*`｜`Anchor-Software/_handoff/todo.md`

---

## v4.4.7 — 2026-09-14（回撤口径补正：提现/消费不得被算成市场亏损 + 9/14 收盘入库）

> **一笔提现，差一点触发一次减仓。**
> 9/14 用户从余额宝转出 ¥8,298.67 至银行卡/消费（当日实际**盈利 +98.80**）。
> 若直接入库，净值口径 `total_assets − liabilities.in_cash` = 40,848.24 →
> 回撤 **−17.53%** → **穿越 −15% 线（42,099.99）→ 系统判「核心增长减 1/3」**。
> 而那一分钱都不是市场亏的。**回撤梯子量的是市场回撤，不是提现**——被它触发就是拿假信号割肉。

**新增**
- `_meta.net_outflow_total`（累计净转出）—— 与 `liabilities.in_cash` **方向相反**的第二个口径补正项：
  - `in_cash` = 借来的钱**仍留在**账户里 → 虚增 total_assets，回撤**要扣掉**
  - `net_outflow` = 自己的钱**离开了**账户 → 压低 total_assets，回撤**要加回**
  - 净值口径更新为：**`net_assets = total_assets − liabilities.in_cash + net_outflow_total`**
  - 首笔 ¥8,298.67（9/14）。⚠️ **后续每笔转出/转入需累加维护此字段**（转入为负）
- `_meta.net_outflow_note` —— 口径定义 + 设立原因 + 维护义务，避免后来者误删

**修复**
- `05-scripts/data_processor.py`：新增 `net_outflow_total()`；`compute_drawdown_state()` 净值分子加入该补正项；
  返回 dict 增 `net_outflow_total`；规则文案改为「已扣贷款残留 ¥X·已补回净转出 ¥Y」条件拼接
- `05-scripts/drawdown_alert.py`：`--json` 输出与终端文案同步补正项
- `05-scripts/rebuild.py`：看板「净值口径」行补正项展示
- `05-scripts/test_calculations.py`：2 处集成断言改为 `total − in_cash + net_outflow`（兼容缺省 0）；
  新增 `TestNetOutflow` 4 项（含**反证**：同一组数字漏掉净转出 → 误触发 −15% 线）

**数据**
- 9/14 收盘入库：`total_assets` 47,872.01（基金+余额宝 42,144.01 + 股票 5,728.00），当日 **+98.80**
- 回撤 **−0.772%**、安全垫 **¥2,093.98**（9/11 为 −0.971% / ¥1,995.30，改善因当日盈利）
- 行情段 9/14 收盘（腾讯/东财实测 16:14）；三笔账户内调拨补录流水（通利加仓 150 / 黄金定投 15 / 纳指定投 10）

**验证**
- `data_pipeline.py --integrity` ✅（chart 无重复 / 时间轴对齐 / 星期正确 / 顶层自洽 / market 日期一致）
- `test_calculations.py` **50 项全过**（原 46 + 新增 4）
- 股票反推交叉验证：用户报「股票亏12」→ 4000 股 × 0.003 = 12.00 → 1.432；腾讯实测 515180 收 **1.432** ✅ 分毫不差

**影响文件**
- `Anchor/05-scripts/data_processor.py` / `drawdown_alert.py` / `rebuild.py` / `test_calculations.py`
- `portfolio_data.json`（桌面 + `06-dashboard`）

**口径提示**：本次 bump 后 `net_assets ≠ total_assets − in_cash`（多一项 net_outflow），
读取回撤时**必须用引擎输出的 `drawdown_state.net_assets`**，不要自行用减法复算。

---

## v4.4.6 — 2026-09-14（报告深度标准 v2.1：DDX 条款可执行化 + 数据缺口声明入正文）

> **一条「永远做不到」的强制项，比没有强制项更糟。**
> v2.0 标准把「DDX 当日/3日/5日/10日 四周期」列为**无条件强制**（§二.2、§四），但该要求
> **在数据源上无法满足**——mx-data 免费版日限额 500 次，**2026-09-11 与 09-14 连续两日盘中即耗尽**
> （`code=113`）；东财公开 API 无基金级 DDX、腾讯无 DDX。结果是每份报告都只能写「未取得」，
> 久而久之会训练出「标准反正做不到，照抄一句免责就行」的习惯，**把整份验收清单的可信度一起拉低**。
> 本次把这一类条款从「无法执行的强制」改为「可执行的强制」。

**新增**
- `01-rules/报告深度标准_v2.1.md` — 现行标准（v1.0 / v2.0 均废止）。三处变更：
  - **变更 1 · DDX 由无条件强制 → 有条件强制（二分）**：有可用源 → **必须给四周期**；无可用源 →
    **不视为违规**，但必须**同时**满足三项：① 在 §二.10 缺口声明表登记（写明**已尝试源数量 +
    每源失败原因**）；② 以**板块级主力净流入**作替代口径并标注「替代口径」；③ **禁止**用
    「软件的近似 DDX」顶替与混引。
  - **变更 2 · 新增 §二.10「数据缺口声明表」**（强制章节，固定四列：缺口项 / 已尝试源 /
    每源失败原因 / 替代口径）。规则含：禁止只写「未取得」不列源；禁止空值占位；替代口径必须
    显式标注；**缺口连续 3 份报告存在须升级为待裁决项**（修订标准 or 换源），不得变成固定免责句。
  - **变更 3 · 新增 §二.11「估算值五项标注义务」**：`≈` + 区间 + 显式写「非确权值」+
    **禁止用估算值触发操作** + 报告末尾一句总声明。
  - **④段新增「归因定性」**：上次操作**不得只因结果为正就判 correct**，须区分「判断力胜出 /
    合规驱动 / 被动达标 / 运气」。
  - **⑥段新增**：节点表的场外操作时点**须写具体钟点**（14:30 决策 / 15:00 硬截止），禁止「收盘前」。
  - **§五数据源速查表加入实测状态**，含 🔴 **禁用东财 `100.NDX`**（其值为**纳斯达克综合**，
    非纳指100，差约 3,000 点；真值须取腾讯 `usNDX`）。

**修改**
- `01-rules/报告深度标准_v2.0.md` — H1 下加 🛑 **废止横幅**，说明废止原因并指向 v2.1，⚠️ 不得据以施工。
- `01-rules/报告深度标准_v2.0_执行模板.md` — 加「2026-09-14 补丁」块：**模板结构不变**，
  四处条款（DDX 二分 / 缺口声明表 / 估算值标注 / 归因定性）按 v2.1 执行。
- `~/CLAUDE.md` — 报告深度标准版本引用 **v2.0 → v2.1**（文件生成速查 + 建议纪律第 ④ 条）。

**影响文件**
`Anchor/01-rules/报告深度标准_v2.1.md`（新增）、`报告深度标准_v2.0.md`、
`报告深度标准_v2.0_执行模板.md`、`~/CLAUDE.md`、`Anchor/CHANGELOG.md`

**核对依据（非估算）**
- mx-data `code=113` 为 **2026-09-14 实测**（本机 `MX_APIKEY` 已配置、skill 文件完整、请求已达 `mkapi2.dfcfs.com`）。
- **额度为账户池而非 mx-data 独占**：本机 6 个妙想 skill（`mx-data` / `mx-moni` / `mx-poster` /
  `mx-search` / `mx-xuangu` / `mx-zixuan`）**全部读取同一 `MX_APIKEY`**。
- 东财 `push2his` K 线、`100.HSSCID`、`fundgz` 盘中估值等失败情形均为实测，已入 §五 速查表。

---

## v4.4.5 — 2026-09-11（信息桥 GBK 崩溃修复 + 9/11 入库订正）

> **同一个 bug 类，在 v4.4.4 之后又被抓到一次——这次不在校验链路，在 sync_all 的生产链路里。**
> 9/11 晚间跑 `sync_all.py` 时步骤 9「实时分发 relay」**总体显示 `[OK]`，但子步骤信息桥推送报错**：
> `UnicodeEncodeError: 'gbk' codec can't encode character '\xa5' in position 158`（`ai_bridge_sync.py:106`
> 打印含 `¥` 的数据表）。**子步骤失败不冒泡到步骤级退出码**，所以这行 WARN 被 `[OK]` 掩盖——
> 属 v4.4.4 认定的最危险失败方式：**静默失效**。

**修复**
- `AI-Collab/ai_bridge_sync.py` — 顶部加模块级 `reconfigure(encoding="utf-8", errors="replace")`
  （与 v4.4.4 的 `report_time_check.py` / `pretooluse_time_gate.py` / `gate_selftest.py` 同一规范）。
  验证：重跑 `--push` 与整条 `sync_all`，WARN 消失、推送成功。

**数据入库订正（9/11 收盘 App 权威，`portfolio_data.json`）**
- 总资产 **¥56,071.88**（基金+余额宝 50,331.88 + 股票 5,740.00），当日 **-340.01（-0.60%）**
- 同步订正 **3 处滞后字段**：`total_hold_pnl_est` 1,596.32 → **1,256.32**；余额宝 `pnl` 336.11 → **336.44**；
  股票 `pnl` 600.00 → **500.00**（515180 由 1.460 降至 1.435）
- **口径勘误（存量）**：历史笔记「纳指 9/9 收 26,253.34」实为**纳斯达克综合**，本基金跟踪**纳斯达克 100**
  （长期差约 3,000 点），本次起以纳指100 为准

**影响文件**：`AI-Collab/ai_bridge_sync.py`、`portfolio_data.json`（桌面 + 06-dashboard）、`CHANGELOG.md`、`CLAUDE.md`、`00-system/会话检查点.md`

---

## v4.4.4 — 2026-09-11（时间错标事故治理：L0–L4 五层防御 + 事实核验派）

> 9/11 盘中研究报告把 9/15 写成「周一」（实为**周二**）、9/13 写成「周六」（实为**周日**），
> 并写出场外基金**结构上无法执行**的截止表述（「9/15 收盘仍超限才收尾」——场外须 15:00 前提交）。
> 事后审计：这不是首次，53 天内 ≥6 次同类案底（7/20、8/19、8/28×2、9/1、9/4–9/10、9/11）。
> 根因不是「不小心」，是**「核验」这个职责在全系统从未被分配给任何环节**：既有校验只覆盖
> `portfolio_data.json`（validate_integrity V1–V7），**报告正文/检查点/信息桥零校验面**。
>
> **两类错误的性质完全不同**：A 类（星期错标）是确定性事实错误；B 类（不可执行截止）需要
> 领域机制知识，**四名对抗裁判一致签字放行，且会静默顺延**——「9/15 硬回退条款」本是防拖延的，
> 却被 B 类错误变成了拖延本身。

**新增**
- `05-scripts/report_time_check.py` — 双检测器校验器。**A（确定性 🔴）**：文中所有「M/D（周X）」与
  `datetime` 实算比对；**B（启发式 🟡）**：场外/联接/QDII 语境把「收盘/尾盘/盘后」当执行截止即报警
  （B 用**结构定位**而非行级邻近——9/11 事故里标的在表 A 行、截止时点在表 B 行）。
  用法：`<文件…> | --latest | --all`，选项 `--year/--json/--no-b/--advisory/--quiet`，行内豁免 `# noqa:time`
- `05-scripts/report_time_audit.py` — 存量登记器，产出 `00-system/时间错标存量登记.md`；
  `--check` 比对基线，**高于基线即判为新增**（存量冻结：🔴 28 处／🟡 9 处／23 文件）
- `00-system/2026-09-11-时间错标事故根因分析与防御方案.md` — 根因三层 + 边界（拦不住什么）
- `00-system/对抗性裁判团_角色定义_v1.0.md` — **新增第 5 固定角色「事实核验派」**，
  职责=重算稿件里每个日期/星期/数字/编号，并对 🔴 持**一票否决权**；
  其诞生理由：9/11 抓到错的是某裁判「顺手重算」，而**顺手不可靠，制度才可靠**
- `~/.claude/hooks/inject_datetime.sh` — **L0**：UserPromptSubmit 每轮注入未来 14 天
  「日期→星期」表，根除「从事件日期反推星期」这一直接致因
- `~/.claude/hooks/pretooluse_time_gate.py` — **L1**：PreToolUse 硬闸门，保护
  `04-reviews/**`、`00-system/会话检查点.md`、`ai-bridge/*.md`，🔴 → deny（唯一能在**落盘前**拦下的时机）；
  含 `gate_selftest.py` 10 项回归自测

**变更**
- `01-rules/投资规则手册_v3.5_正式版.md` — 新增 **附录D：执行时点规则（v3.7）** 6 条编号契约
  （场外申赎截止 15:00／建议钟点 14:30／T+1 确认 T+2 到账／场内场外之分／顺延／钟点+渠道缺一不可）
- `05-scripts/extract_rule_contract.py` — 契约提取扩容 13→18 键（附录D 6 条），**13 个既有键逐字节零回归**
- `05-scripts/sync_all.py` — 新增**步骤 11 报告时间扫描**（advisory：只提示不阻断——存量 28 处会让
  每晚同步永久标红，运维唯一理性反应是关掉它）
- `05-scripts/smoke_test.py` — 新增 `[1c] 报告时间校验`（92 通过/0 失败）

**修复**
- **Windows GBK 输出崩溃**（本次新引入）：新脚本输出含 🔴/🟡/📄，而 Windows 控制台默认代码页 cp936，
  `print` 直接抛 `UnicodeEncodeError` 使脚本整体崩溃；`sync_all` 经**管道**抓输出时同样触发
  （3.13 管道仍按区域编码），会让步骤 11 永久报失败。修法：`report_time_check.py`（模块级，import 方一并生效）、
  `report_time_audit.py`、`pretooluse_time_gate.py`、`gate_selftest.py` 统一 `reconfigure(encoding="utf-8", errors="replace")`，
  与 `sync_all.run()` 的 `encoding='utf-8'` 解码端对齐

**为什么不修存量 28 处**：2026-09-11 用户裁决「**只标注不改写**」。历史归档是**时点记录**，
改写会破坏审计链——此后便无法从文件本身看出「系统曾经写错过什么」。登记表把存量冻结成基线，
使**新增**错误可被唯一识别；否则存量会在每次扫描反复出现，最终白名单吃掉真错。

---

## v4.4.3 — 2026-09-10（数据勘误：回撤基准口径修正 + 失实标签治理）

> 9/10 加息应对专项研究落盘后做全链路审计（5 侦察 + 2 验尸），查出两类系统性缺陷。
> **① 回撤基准停更 24 天（风控事实失效）**：`_meta.peak_assets` 仍为 2026-08-17 设定的 ¥35,655（8/10 总资产口径）。9/1 起 total 已切 App 权威全口径，净值口径（total − liabilities.in_cash）当前 ≈¥49,388。旧基准下 dd_pct=+38.5%、三线 33,872/32,090/30,307 全落在资产下方 31–40%，"-5% 卫星减半"预警永不触发。修正：取 9/1 同口径链净值峰值 **¥49,529.40（9/8）**；新 dd_pct = **−0.285%**、安全垫 **¥2,335.19**、-5% 线 **47,052.93**（距当前仅 −4.73%，恢复真实可触发）。9/1 跳变 +6,233.57 已核为资金变动（cash_in_out 4,290.01）+ 口径切换，故 8/26–8/31 A1 段**不可拼接取峰值**。
> **② 通利「偏债混合」失实标签**：天弘通利混合A 实为灵活配置型（2026Q2 股票 74.28%／电子 51.85%，前十大北方华创/兆易创新/京东方A），2022 年起已非偏债；但其 note、MEMORY.md、portfolio-reference-data.md、检查点 §130、规则手册 §5.1 与变更登记表均标「偏债」「偏压舱石」。全库核查结论：**分层与风控口径正确**（走 group=核心增长→core，未计入 defensive_income / bond_items，仪表盘实测固收 mv=26,061.72 不含通利）→ 只改文字描述、**不动分层**。
> 附带清理：`data_freshness.holdings` 9/9→9/10（清 V7 🟡）、删除孤儿字段 `_meta.holdings`、`data_provenance` 补记 9/1 口径切换、`loan_status`/`liabilities.note` 移除已履行的「先看 8/31 归因再配置」、`pnl_total_note` Σ 1,573.15→1,596.32；`cumulative_fund_pnl` 仍待用户 App 读数（**未编造**）。
> 影响文件：`portfolio_data.json`（_meta 11 字段 + 3 条持仓 note）、`01-rules/投资规则手册_v3.5_正式版.md`、`01-rules/规则生效期登记表.md`、`.claude memory`×4、`00-system/会话检查点.md`、Anchor-Software（data 副本 + fixture + phase2 schema）。
> 报告校正：`04-reviews/special/2026-09-10-加息预期升温专项应对研究.md` §7.3 **撤回**（快照不存在 `update_date` 键，非"空值"）+ 中证全债数字**撤回**（已尝试 5 源 / 13 次请求未获取）。

---

## v4.4.2 — 2026-09-05（superseded 决策视觉隔离：已取代灰显 + 待复盘口径去重）

> 用户复盘 #57 时点出 53/52/27——三条「已被取代(superseded)」决策（#27 纳指止盈草案被 #28 取代、#52/#53 9/1 买草案被 #54/#55 执行取代）在 v4.4.1 新排序后仍按普通 outcome=None 行渲染出"⏳待复盘"黄标，读表像三条悬空操作；且 KPI「待复盘」= 57-31 直接相减**未扣除 superseded**（虚报 26，真实 23）。
> 修复：①`dashboard_html` 全部决策表 superseded 行置灰(50%) + 结果列标「已取代」+ 状态列显示"已取代 → #取代者 · 前提推翻"（不再出待复盘黄标）②`accuracy_report` 待复盘 = 有效决策 - 已复盘（新增 active_total=54，KPI 标注"历史累计 · 有效 54（已取代 3）"）③数据层 27/52/53 补 `superseded_by` 字段（→28/54/55），误复盘时提示正确指向。
> 影响文件：`05-scripts/decision_log.py`（accuracy_report pending_review 口径 + dashboard_html 渲染 + KPI）、`06-dashboard/decision_log.json`（superseded_by×3）、`06-dashboard/decision_dashboard.html`（重生成）。

---

## v4.4.1 — 2026-09-05（决策仪表盘"全部决策"表按时间新上旧下）

> 用户反馈"决策仪表盘里这几天的操作都没有"——根因非数据缺失（57 条决策 + 9/4 复盘回填均完整同步），而是决策表按 id 升序渲染，最新 #52-57 被埋在 57 行表格底部，近几天记录一眼不可见。修复：date 降序 + 同日 id 降序，最新决策置顶。
> 影响文件：`05-scripts/decision_log.py`（dashboard_html 排序）、`06-dashboard/decision_dashboard.html`（重生成）。

---

## v4.4.0 — 2026-09-04（同步更新链闭环：入库自检 + 页面真日期/时刻 + sync 机器回执）

> 用户连续被"数据没及时更新/页面不同步/看不出哪层是新的"困扰（chart_data 曾重复 09-01 脏条、落后 2 天、星期标错全程无拦截直通页面）。逐层诊断定位 5 断点后一次性闭环。

### 🔴 入库自检护栏（一次实现、三处复用）
- **data_processor.validate_integrity()**（新，可复用）：V1 chart 重复 d ｜ V2 chart/summaries 末条 vs update_date 对账 ｜ V3 星期对照 ｜ V4 顶层自洽（**口径铁律：total=fund_account+stock_account，fund_account 已含 yuebao，勿三者相加**）｜ V5 market.date==update_date ｜ V6/V7 软提示（活跃市值和、data_freshness 人工维护提醒）
- **data_pipeline.py --integrity**（新）：🔴 硬项 rc=1；**sync_all 新增致命步骤 0「写库自检」** → 脏数据不再直通 rebuild/页面
- **rebuild**：process_all 内嵌调用，经 `_warnings` 免费打印（本次修复 9/1 脏条 + 星期误标 2 类历史错误，正/负向 6 测试全过）

### 页面显示真实数据日期/时刻（可见性）
- **rebuild.py 顶栏**：`source_update_date`（真数据日期）优先于 `time`（重建时刻），修复"重建时刻冒充更新"（曾停留 8/28→页面却显 9/3）
- **gen_daily_hub**：顶栏新增入库**时刻**（update_time 前 16 位，显示 23:12）
- **decision_log dashboard footer**：静态占位"生成时间见文件时刻" → 真实「数据源更新 {mtime} · 本页生成 {now}」

### sync 机器回执 + 9.5 恢复
- **sync_all**：`.sync_last_result.json` 扩展 `data_date`/`note`（机器级新鲜度权威）；结尾一行回执「数据截至 · 同步生成 · 失败步」
- **push_portfolio_data.sh** 本地行 python3 → `"$PY"` fallback（Windows 无 python3 rc=127 → **9.5 步骤恢复成功，净退出码可信**）；远程 ssh python3 不动

### 新鲜度语义澄清（避免 watch_sync 无限循环）
- **不自动写桌面 JSON `_meta`**（写则 sha256 变→再触发→再写死循环）；机器级新鲜度权威 = `.sync_last_result.json`；`_meta.data_freshness` = **人工入库维护**（本次 holdings 补 9/4）；协议 v1.4→**v1.5**（每日层新增 9.5 写库自检步 + §六语义修订，`last_rebuilt` 降为静态备注）

### 验证
- smoke_test 新增 [1b] 数据自检节（chart 去重/时间轴/星期/顶层自洽/market 日期/页面 freshness 三 token）

### 影响文件
05-scripts/{data_processor,data_pipeline,sync_all,rebuild,gen_daily_hub,decision_log,smoke_test}.py ｜ Anchor-Software/deploy/scripts/push_portfolio_data.sh ｜ 00-system/数据更新协议.md（v1.5）｜ Desktop/portfolio_data.json（system_version v4.4.0 + _meta.data_freshness 9/4）｜ CHANGELOG.md ｜ CLAUDE.md ｜ 会话检查点.md

---

## v4.3.5 — 2026-09-01（体系深度审计 + 全部优化落地）

> 用户「深度分析 anchor 体系健全性/功能可用性/逻辑流畅性 → 全部优化并保存记录」。5 维度 agent 审计（4 完成 + 1 失败维度已覆盖）+ 独立复核，产出 `00-system/2026-09-01-体系深度分析报告.md`（三评级 + P0/P1/P2 分级清单），本版本全部落地。

### 🔴 P0 修复（数据污染 / 校验失效）
- **P0-1 契约链路断裂**（A 修复是死代码）：rule_contract.json 原只剩 6 个版本握手键，extract_rule_contract.py 全仓无调用者 → pre_trade_check 每次回退内置默认。修复：①extract 四层配比正则/默认值修正（40/15-25/30/10→**45/20/20/15**，原正则误抓波动率 2%）+ 新增单只持仓上限/板块上限提取 ②sync_all 新增步骤 **8.5 规则契约提取** ③realtime_relay 分发时**保留契约 rules 段**（原覆盖写抹除）④extract 保留 data 字段。验证：8 测试全过（test_load_from_real_contract FAIL→OK）、三场景回归、契约 rules 12 键完整
- **P0-2 决策日志重复记录**：#52/#53 与 #54/#55 同笔交易双写（双 AI 各录一次）→ 标 superseded→#54/#55（保留历史）；连带修复 **next_due() 未排除 backfilled**（#42 陈旧日期占位 → 下次复盘日正确显示 9/4 #54/#55）

### 🟠 P1 修复
- **pre_trade_check 补漏**：分层持仓上限（压舱石≤¥8,000 / 核心≤¥4,000 / 卫星=E1≤¥3,000，原只查卫星）+ E4 卫星月净投入改**累计口径**（本月已投入+拟买）+ **贷款不再豁免评分卡**（9/1 证券贷款+2,000 评 4/5 仍超 E1 实证）。回归：证券+500 ⛔E1+E4、创新药+359 ⛔E4 累计、鹏华+1000 ⛔压舱石上限、创新药+2000 --loan ⚠️评分卡
- **version_check 主版本文件**：默认读 `~/.anchor_version.md`（不存在）→ 改读家目录 **CLAUDE.md**（版本行恒存在，消除环境变量依赖）
- **daily_hub（D2）**：规则手册 v3.4 死链 → glob 最新；半导体/创新药/时机A/月操作/决策日志陈旧硬编码（8/25 超大单/#22/#23/26 条/8月）→ 数据驱动；EVENTS 更新至 9 月（9/4 T+3 / 9/2 DDX / 9/7 港股通 / 9/15 FOMC）
- **Excel 生成**：gen_excel_skill.py 硬下标 tx['name'] → .get() 兜底（9/1 两次 KeyError 崩溃根因）+ 新增 **requirements.txt**（openpyxl/requests 声明）
- **daily_advice 强制前置硬化**：⛔ 硬性违规 → 简报显著 RED 警示 + 今日禁止对应买入（原静默）；金额正则增强（¥/元/买入+ 三模式）
- **规则文档一致性**：手册标题 v3.4→**v3.5**（与文件名统一）；登记表引用不存在文件 → v3.5；止盈旧档残留对齐（手册 1.3 创新药 +10/+20 → +8/+15/+25，data_processor profile 同步）
- **报告路径统一**：报告深度标准 v2.0 增补"盘中研究报告→`04-reviews/research/`"权威行
- **信息桥 F2 方向 B 回写**：claude_state.md 首次主动回写（今日产出/待办/给 WorkBuddy 留言 5 条）；Stop hook 自动化遗留（settings.json 敏感）
- **检查点信息孤岛**：9 张历史待办表 → 头部导航锚点 + 8 张标"（历史归档）"
- **变更管理补登记**：8/31-9/1 落地 19 项现统一登记于本版本

### 🟡 P2 清理
- design_v43_upgrade.py / gen_excel_v2.py 死代码 → 归档/；rebuild.py 8/20 过期补丁删除；提案台账 #17-20 重复去重；尾盘模板 v1.0 标废止；数据更新协议 **v1.4** 补两条最高铁律

### v4.3.5 补充（17:21）：止盈档位 v3.6 裁决同步
- **8/31 归因裁决③ 采纳疏档 +10/+20/+35**（用户/WorkBuddy 拍板，手册 2.3 已更新 v3.6：回测证明趋势市高档不卖飞，止盈降收益且 MDD 零改善）
- 软件侧同步：data_processor.py 创新药 profile → `-8%止损 / +10%+20%+35% / 30天`；登记表 C1 → ✅ 已裁决采纳（17:00 同步时曾按手册旧文写成 +8/+15/+25，本补充纠正）
- 影响文件：05-scripts/data_processor.py ｜ 01-rules/规则生效期登记表.md

### 遗留项（需用户裁决/后续）
手册 461 行旧主指标口径｜C2 冻结无代码执行｜F2 Stop hook 自动化｜E1 周报/E2 月报升级｜8/31 复盘 v2.0 回归｜可视化运维专项｜CHANGELOG v4.0.x 顺序｜手册文件名 v3.5 vs 正文 v3.6 版本标注统一（详见审计报告第四节）

### 影响文件
05-scripts/{extract_rule_contract,pre_trade_check,version_check,decision_log,gen_daily_hub,gen_excel_skill,sync_all,daily_advice,data_processor,rebuild}.py ｜ 05-scripts/test_pre_trade_check.py ｜ 05-scripts/requirements.txt（新）｜ 05-scripts/归档/{design_v43_upgrade,gen_excel_v2}.py（移入）｜ Desktop/AI-Collab/{realtime_relay.py,rule_contract.json,ai-bridge/claude_state.md} ｜ 01-rules/{投资规则手册_v3.5_正式版,规则生效期登记表,报告深度标准_v2.0,尾盘操作报告模板_v1.0}.md ｜ 00-system/{2026-09-01-体系深度分析报告.md（新）,数据更新协议,改进提案台账,会话检查点}.md ｜ 06-dashboard/decision_log.json ｜ CLAUDE.md ｜ CHANGELOG.md

---

## v4.3.4 — 2026-09-01（E1 漏检反思 + pre_trade_check 契约兼容/A2 强制前置）

### 🔍 9/1 事件复盘：证券贷款 +2,000 超 E1 上限 129% 漏检（决策≠执行三源核验实证）
- 9/1 用户实际执行 2 笔贷款买入：证券 +2,000（14:56，站上770+主力+11.31亿，评分卡4/5）+ 鹏华债券 +3,300（压舱石归并），月限额 2/4
- **证券这笔违反 E1 单只卫星 ≤¥3,000 硬上限**（加仓后 6,873 超 129%），且与 8/31 归因裁决「证券压回超限止盈1/3」方向相反；评分卡 4/5 满足质量门槛，属**执行纪律违规**而非方向判断错
- 决策日志补录 #54（证券·标「超E1上限,执行违规」）/ #55（鹏华·压舱石归并），T+3（9/4）复盘核算
- 后果量化：证券超限敞口 ¥3,873 + 贷款杠杆（动用残留 7,023.77 的 75.5%）；若回调 -8% → 超限部分白亏约 -310 元（坏情景暴露，非已发生亏损）
- 影响文件：06-dashboard/decision_log.json（#54/#55）/ 会话检查点.md

### 根因修复（commit f36f96b，15:20 落地 + 本会话验证）
- **A · pre_trade_check 契约兼容**：extract_rule_contract 输出 `rules` 段（键 e1_sat_position_cap 等）与校验器内置键（e1_sat_single_limit 等）不匹配 → `load_thresholds()` 现在兼容 thresholds/rules 两段 + 键名映射，实测三场景回归通过（证券+500 ⛔拦截 / 创新药+359 ✅ / 鹏华+1000 ✅，8 测试全过）
- **B · A2 强制前置**：daily_advice 信号产出后对 pending_actions 含「买入/加仓」项强制跑 pre_trade_check 并输出结论到简报，堵「信号不校验」洞
- 反思补录：E1 漏检教训进 04-reviews 反思（判断 vs 执行分离归因）
- 影响文件：05-scripts/{pre_trade_check,daily_advice}.py / 05-scripts/test_pre_trade_check.py / 04-reviews/反思*

---

## v4.3.3 — 2026-08-27（报告深度标准 v2.0 升级）

### 报告深度标准 v1.0 → v2.0（用户指示「以后按此标准，要比 mx-data 深度版更详细更准确」）
- 基准模板：2026-08-27-深度复盘.md（mx-data 深度版）；v1.0 已标注废止（仅历史存档）
- v2.0 强化三处：
  ① 头部元信息三源拆分（持仓/行情/技术指标各注时点）
  ② 核心结论表双表制：KPI 摘要行 + 持仓全景大表（一行一只：市值/当日盈亏/当日%/累计/板块当日/MA5·10·20 精确值+站上跌破/DDX 当日3日5日10日/主力资金/结论）
  ③ 逐项分析 6 要素：当日价格路径/技术面（MA精确值+DDX四周期+主力资金）/基本面利好利空双面/明日关键价位/一周压力支撑/⭐星级+量化触发线
- 技术面数据源：mx-data（东方财富）为主（MA 序列自行计算 + DDX + 主力资金），westock 交叉验证
- A1 复盘自动化升级为「深度版 v2.0」（步骤 5 mx-data 技术面采集）
- 影响文件：01-rules/报告深度标准_v2.0.md（新）/ v1.0.md（标废止）/ A1 自动化 / CLAUDE.md / 07-memory 索引 / CHANGELOG.md / 检查点

### v4.3.3 补充：交易前校验器 pre_trade_check.py（8/27 22:48 提交 6567a0f，与 v2.0 同属 v4.3.3）
- 错配教训工具化（提案 #A 加仓前超限校验 / #B 贷款分批 / #C 目标可达性标注机制化）——六项校验：① 月操作额度 4/4 拦截 ② E1 单只卫星上限（加仓后市值）③ E4 月净投入 ④ 大额/贷款 334 分批提示 ⑤ 目标可达性标注 ⑥ 评分卡提醒
- 测试：证券+2500 ⛔拦截（月额度+E1）、创新药+359 ✅通过（9/1 可用）、半导体+300 贷款 ⚠️分批提示——全符合预期
- A2 盘中信号自动化步骤 7 强制接入（⛔ 自动转观察）；规则正式化待 8/31 仲裁，工具先行落地
- 影响文件：05-scripts/pre_trade_check.py / A2 自动化 / 00-system/改进提案台账.md / 01-rules/规则生效期登记表.md

---

## 2026-08-28 — GitHub 公开仓库优化日（不 bump 版本，三处一致仍 v4.3.3）

### GitHub 曝光度与公开仓库整理（7 笔提交，全部聚焦公开侧，投资逻辑零改动）
- **隐私加固**（git 6b6dcdc）：00-system 敏感档案移除跟踪（贷款/金额/预案等 11 份，git rm --cached 保留磁盘数据完整）；.gitignore 添加 `00-system/*` 白名单机制（白名单保留 mx_install_guide / repo_info / repo_info2 三份公开文件）；根级配套 SECURITY / CONTRIBUTING / CODE_OF_CONDUCT / TROUBLESHOOTING
- **README 首屏**：重排（448511f：数字横幅+截图+Live demo 置顶）+ 整理（73edc6c：语言切换/免责声明/徽章/速览）
- **文档入口补全**（c9ab988）：FAQ/setup 入库 + docs 索引页 + README Documentation 节
- **脱敏战绩页**（9f0b36b）：08-website/track-record.html + .github/workflows/pages.yml 部署扩展
- **feature_request 模板投票引导**（7f192a2）
- **文案统一 + 版本文件路径环境变量化**（831f45a，9 文件 23增21删）
- GitHub 诊断专题 2 份：04-reviews/special/2026-08-28-GitHub曝光度诊断.md / 2026-08-28-高星项目设计借鉴分析.md
- 投资侧例行（非改动）：04-reviews/daily/2026-08-28-盘中操作建议.md（14:30，8月额度 4/4 满零操作）+ 2026-08-28-盘中研究报告.md（14:57，六段式 v2.0：半导体 DDX 连正第 2 日未达成 / 杰克逊霍尔鹰派 / 纳指 A+C 全持暴露 8/29 净值最大变量）
- 影响文件：08-website/track-record.html（新）/ .github/workflows/pages.yml / README.md / README.zh-CN.md / docs/* / 00-system/.gitignore / 05-scripts/{gen_daily_hub,version_check}.py / 09-backtest/output/* / 04-reviews/special/* / 04-reviews/daily/2026-08-28-*.md / 会话检查点.md

---

## v4.3.2 — 2026-08-27（P0/P1 落地 + 版本冻结启动）

### P0 四项（提升建议优化版执行）
- 驾驶舱 app.py 补齐（Desktop/app.py，静态服务+AI助手端点），8765 端口 4/4 通过——start.bat 修复（此前 app.py 缺失从未跑通）
- 数据自动化 MVP：05-scripts/data_auto_fill.py（复用妙想 API，查询回退逻辑），7/7 持仓回填候选成功，守铁律不写 JSON
- 加仓专项回测：09-backtest/scripts/add_position_backtest.py——回调日维度证据：黄金+2.13pp/红利+1.70pp 有效，芯片-0.54pp/证券-0.14pp 无效 → 打分卡②需联合资金确认
- 仲裁材料补充：待仲裁项 6（月限额计数口径，预推荐 C：334 建仓计 1 笔）+ 第七节加仓证据

### P1 三项
- westock 港股代码支持验证（hk00700 正常）——回测扩展基础就绪
- 2022 熊市压力测试：09-backtest/scripts/stress_test_2022.py——止损在芯片/证券/纳指=回撤减半+收益提升（芯片 MDD 改善 36.2pct），红利误伤/黄金无触发 → 止损=卫星层规则再验证
- 决策日志扩容 28→51 条（补录 2025-10~2026-06 历史 23 条，样本目标 ≥50 达成）

### 版本冻结协议启动
- 版本号三处统一 v4.3.2（CHANGELOG/JSON×2/CLAUDE.md）；新增 version_check.py 三处一致校验；A1 自动化内置自检
- 规则生效期登记表（01-rules/规则生效期登记表.md）：每条规则标生效/失效日，防已废规则引用

### 8/27 收尾补缺（升级深度分析核验后，不 bump 版本——三处一致校验仍为 v4.3.2）
- **护栏真正接入执行链**：sync_all.py 新增第 7.6 步调用 version_check.py——原 CHANGELOG 声称"A1 自动化内置自检"但脚本未引用，属文档-实现脱节；现已落地（不一致 log error 中止后续同步）
- **collab 协议补护栏描述**：reference-workbuddy-collab.md 数据互通第 4 条——bump 版本号必须三处同步改
- **CLAUDE.md 主指标同步**：近期重点行旧口径（平均收益率≥+3%）→ 三步法新主指标（盈亏比≥1.5:1｜追高≤20%｜止损执行率100%）；决策日志行同步 28→51 条/准确率 80.0%/口径审计注释

### 8/27 晚间 决策-执行偏差修复（22:2x，不 bump——三处版本仍 v4.3.2）
- **根因**：8/27 深度复盘把决策 #27「执行止盈」误读为已成交（实为 superseded→#28 延期不卖，纳指从未卖出）；且报告「8月3买2卖」误把未执行 #27 计入卖出数（实际 transactions 权威 = 4 笔 3买1卖）
- **决策日志防污染（系统性）**：decision_log.py 新增 `_is_active()`（排除 superseded/tags 含 superseded 记录），接入 accuracy_report / pending_list / due_list / next_due / review_decision 五处——superseded 决策不再计入胜率/盈亏比/分桶统计、不再进待复盘/到期提醒、**禁止回填 outcome**（防止 8/29 误标 #27 污染统计）
- **报告修正 3 处**：2026-08-27-深度复盘.md 的 #27 改为「未执行·用户确认」、月操作额度改为「8月实际执行4笔=3买1卖」、删去 #22 无依据的「卖飞+25」估算
- 验证：编译通过；--report 统计不变（23 已复盘/80.0%/0.60:1）；review #27 被拦截；pending 排除 #27

### 影响文件
05-scripts/data_auto_fill.py / 09-backtest/scripts/{add_position_backtest,stress_test_2022}.py / Desktop/app.py / 09-backtest/2026-08-26-8月31日仲裁材料.md（seg2 矛盾句修复+第七节）/ 00-system/提升建议优化版.md / 00-system/规则生效期登记表.md（新）/ 05-scripts/version_check.py（新）/ 05-scripts/sync_all.py（7.6 步护栏）/ 07-memory/reference-workbuddy-collab.md / portfolio_data.json×2 / CLAUDE.md / CHANGELOG.md / 会话检查点.md / **05-scripts/decision_log.py（superseded 防污染）** / **04-reviews/daily/2026-08-27-深度复盘.md（3 处修正）**

---

## v4.3.1-backtest — 2026-08-26（完整执行链回测落地）

### 完整执行链回测 + 仪表盘/对比图 + 仲裁材料第六节（8/31 归因 P1-4 证据闭环）

**背景**：把 Anchor 真实执行链完整建模（替代此前「止盈单维度」实验），为 8/31 月度归因提供最贴近实盘的裁决证据。

- 🧪 **新增 `09-backtest/scripts/full_anchor_backtest.py`**：完整执行链回测脚本——
  - 建仓 **334 分批**（3:3:4，批间隔 3 交易日，次日开盘执行）
  - 止损 **-8%**（收盘触发 → 次日开盘清仓，含缓冲语义）
  - 止盈 **③疏档 +10%/+20%/+35% 各卖 25%** + 剩余 25% 奔跑仓（破 20 日线或自峰值回撤 ≥8% 出清）
  - 限额 **月操作 ≤4 笔**（自然月）+ 清仓后冷却 5 交易日
  - 区间 **5 标的 × 3 区间**：full(2021-09~2026-08) / seg1(2021-09~2023-08 跌市) / seg2(2023-09~2026-08 涨市)；信号收盘确认 → 次日开盘执行（无前视）；A股 ETF 100 份手数 + T+1 + 万三双边费
- 📊 **新增 `09-backtest/scripts/make_full_dashboard.py`**：生成三件套 + 对比图 + 仪表盘——`full_anchor_equity.csv` / `full_anchor_trades.csv` / `full_anchor_summary.json`（三件套）+ `full_anchor_vs_benchmark.png`（策略 vs 满仓对比）+ `index.html`（完整执行链仪表盘）+ `takeprofit_dashboard.html` 更新
- 📄 **仲裁材料第六节**（`09-backtest/2026-08-26-8月31日仲裁材料.md` 新增「六、完整执行链回测证据」）：
  - 核心结论 = **回撤保护**：5/5 标的回撤全面改善（9.1~20.1pct），4/5 在跌市（seg1）跑赢或接近满仓（芯片 -22.6% vs 满仓 -39.0%）——334 建仓 + 止损在跌市确实少亏
  - 涨市（seg2）收益仍被止盈结构拖累（5/5 跑输满仓，与止盈单维回测一致，③疏档未解决趋势市卖飞但回撤受控）
  - 止损循环 + 月限额协同：纳指 full 26 笔交易（止损 9 + 止盈 17）证明「止损后冷却再入场」机制持续运行，月限额未误伤
  - 区间依赖确认：「纪律体系 = 少亏型而非暴利型」定位再次验证；8/31 裁决证据链闭环（止盈单维 → 参数扫描 → 档位宽度 → 止损端 → 完整执行链）
- 📁 影响文件：`09-backtest/scripts/full_anchor_backtest.py`（新增）、`09-backtest/scripts/make_full_dashboard.py`（新增）、`09-backtest/output/full_anchor_equity.csv`（新增）、`09-backtest/output/full_anchor_trades.csv`（新增）、`09-backtest/output/full_anchor_summary.json`（新增）、`09-backtest/output/full_anchor_vs_benchmark.png`（新增）、`09-backtest/output/index.html`（更新）、`09-backtest/output/takeprofit_dashboard.html`（更新）、`09-backtest/2026-08-26-8月31日仲裁材料.md`（第六节）
- 📌 本轮文档同步：CHANGELOG/CLAUDE.md/会话检查点 全部版本统一为 **v4.3.1**

---

## v4.3.1-design — 2026-08-26（示例页自洽化三修）

### 示例持仓地图移除 + smoke 更新 + 示例数据自洽化（消除「资产0却有盈亏」矛盾）

**背景**：v4.3.0 后公开页示例数据自相矛盾（hero 显示资产 ¥0 却有月度盈亏）+ 持仓地图无真实数据支撑。三连修收尾 v4.3.1。

- 🗺️ **① 示例持仓地图整体移除**（`08-website/anchor-pro.html`）：删除持仓卡渲染 JS（31 行）+ `holdingsGrid` 容器；章节改为互动试算「试算你的配置方案」；导航「示例地图」→「配置试算」
- 🧪 **② smoke_test 2 项过时检查更新**（`05-scripts/smoke_test.py`）：持仓地图移除后，「使用动态持仓标签」→「数据含动态层级标签」（active_label）；「遍历动态层级」→「金字塔消费动态层级」（pyramidViz）
- 📐 **③ 示例数据自洽化**（`05-scripts/gen_anchor_pro.py` + `06-dashboard/portfolio_analysis_example.html` + `08-website/anchor-pro.html`）：
  - hero 示意本金 **0 → ¥50,000**（消除「资产 0 却有盈亏」矛盾）
  - monthly 改为 **8 个月 6 盈 2 亏**正期望序列（600/-450/800/350/-600/900/250/500）
  - history_lead 注明「基于示意本金 ¥50,000」
- 📁 影响文件：`08-website/anchor-pro.html`、`05-scripts/smoke_test.py`、`05-scripts/gen_anchor_pro.py`、`06-dashboard/portfolio_analysis_example.html`（示例数据同步）
- 📌 本轮文档同步：CHANGELOG/CLAUDE.md/会话检查点 全部版本统一为 **v4.3.1**

---

## v4.3.0-design — 2026-08-26（设计系统统一升级 · DesignMdArchitect 专家）

### 全部可视化页面按 DESIGN.md 规范统一升级（视觉层，不改变任何数据字段）

**背景**：用户启用「规范范」（DesignMdArchitect）专家，要求优化 Anchor 所有可视化页面——更符合逻辑、更吸引观众、更具艺术感与科技感、更便捷。产出统一设计系统规范 + 4 页幂等升级脚本。

- 📐 **新增 `08-website/design-system/DESIGN.md`**：9 章节完整设计规范（品牌混搭：Linear 排版 × Stripe 色彩 × Tesla 未来感），双 AI（WorkBuddy + Claude）可消费，280+ 行精确到 HEX/CSS 变量
- 🎨 **4 页统一注入 v4.3.0 设计层**（`05-scripts/design_v43_upgrade.py`，幂等可复跑）：
  - 玻璃拟态 tokens（glass-1/2/3 + blur 12-32px + edge 光效环 + 上缘高光）
  - 顶栏 sticky 毛玻璃导航、卡片 hover 蓝光上浮、深色滚动条、打印样式、选中文本高亮、移动端触控目标 ≥44px
- 🔢 **3 个私有看板注入章节自动编号**（v4.3.0 内置）：h2 自动生成 `01 · 02 · 03…` 编号 + 渐变分隔光带 + 表格行 hover 聚焦——信息逻辑一眼可读（原误标 v4.3.1-dash-enhance 已更正，实际随 v4.3.0 设计层内置）
- 🆙 **返回顶部按钮**（4 页，原生 JS 无依赖，滚动 420px 后浮现）
- 🐛 **修复遗留 bug**：portfolio_analysis.html title/badge 仍为 v3.5（上次版本统一遗漏）→ 已统一 v4.2.0 PRIVATE
- ✅ **质量验证**：smoke_test 79/79 通过、4 页 0 死链（portfolio 双基准链接除外）、体积受控（60/33/37/106 KB）、桌面副本 md5 一致

**影响文件**：`08-website/design-system/DESIGN.md`（新增）、`05-scripts/design_v43_upgrade.py`（新增）、`06-dashboard/portfolio_analysis.html`、`06-dashboard/daily_hub.html`、`06-dashboard/decision_dashboard.html`、`08-website/anchor-pro.html`、桌面副本 `portfolio_analysis.html`

---

## v4.2.0-collab — 2026-08-26（WorkBuddy 双 AI 协作接入）

### WorkBuddy 定时自动化 + 检查点/Viking 双向互通（Claude ↔ WorkBuddy）

**背景**：用户要求「深度学习 Claude 运行方式并与 Claude 运行数据互通，让 Anchor 更好用」。WorkBuddy 已配置 4 个定时自动化（8/26 17:30 起 ACTIVE），本次打通自动化成果回流到 Claude 上下文的通道。

- 🤖 **4 个 WorkBuddy 自动化**：①每日 21:00 收盘复盘（sync_all 9 步 + smoke 79 项 + 摘要 + Viking 沉淀 + 检查点回写）②工作日 14:30 盘中信号（含买点评分卡 5 维强制打分）③周五 17:00 周报骨架 ④每月 1 日 09:00 月度归因（八步清单核对表）
- 🔄 **检查点双向互通**：A1 复盘完成后回写 `00-system/会话检查点.md`（追加 `## {date} WorkBuddy 自动复盘` 记录 + 更新 `*最后更新*` 行）；Claude 会话读检查点即可见自动化产出，避免重复执行
- 🧠 **Viking 语义记忆共用**：WorkBuddy 每晚写 `viking://anchor/review/{date}`，Claude 可 viking_search 检索——双方共用同一记忆层
- 📋 **协作协议文档**：新增 `07-memory/reference-workbuddy-collab.md`（自动化清单/分工边界/冲突规避），MEMORY.md 索引置顶；CLAUDE.md 新增「WorkBuddy 自动化协作」章节
- 🛡️ **冲突规避**：WorkBuddy 只写 `{date}-收盘复盘报告.md`，Claude 手写 `{date}-深度复盘.md`；检查点只追加不覆盖；真实盈亏铁律双方共同遵守
- 📁 影响文件：CLAUDE.md（+协作章节）、07-memory/reference-workbuddy-collab.md（新增）、07-memory/MEMORY.md（索引）、00-system/会话检查点.md（WorkBuddy 回写约定）、WorkBuddy automation（4 条，非仓库文件）。未 bump system_version（由下次 Claude 会话按需处理）

---

## v4.2.0 — 2026-08-26

### 优化建议采纳落地：买点评分卡强制纳入 + 信号脚本编码修复 + 止盈档位回测

**背景**：用户评估「深度优化建议书.html + 优化执行报告.md」后拍板三项决策：①买点评分卡（5维≥3/5）强制纳入 14:30 盘中建议流程 ②贷款残留 ¥7,023.77 维持「8/31 归因后再定」（拒绝建议书 P0 的配置债券垫方向）③补齐变更管理五步 + 修 gen_intraday_signal 编码 bug。

- 🃏 **买点评分卡（5维≥3/5）强制纳入**：`daily_advice.py` SYSTEM_PROMPT 新增第 6 条硬性规则——输出任何「买入/加仓」建议前必须附 5 维打分（①估值锚 ②回调日 ③恐慌强度 ④资金确认 ⑤试探仓位），每 ✓ 记 1 分；**总分 <3 分 → 必须标注「追高型买入 · 按打分卡放弃」，不输出买入指令**；卖出/减仓/持有/等待建议不需打分。审计实证：8/7 半导体加仓 2/5 分（追高型）应被拦截
- 🐛 **gen_intraday_signal.py 编码修复**：Windows 控制台默认 GBK 无法编码 ¥（`UnicodeEncodeError: 'gbk' codec can't encode character '\xa5'`）→ 加 `sys.stdout.reconfigure(encoding="utf-8")`，直接运行（不加 PYTHONIOENCODING）输出合法 JSON ✅（建议书「实测输出合法 JSON」此前未测出此问题）
- 📊 **stop_profit_backtest.py 新增**：止盈档位回测脚本（读数据不硬编码金额，可复跑），9 只已清仓基金 + 31 笔卖出流水复盘——结论 **v3.4 档位设计成立无需回滚**（更早更小锁利 + 奔跑仓 + 止损硬Deadline 双引擎）；额外发现半导体 17+ 笔反复小额进出坐实过度交易弱点
- 🔍 **优化执行报告**：`04-reviews/special/2026-08-26-优化执行报告.md` 完成 T+3 检查/止盈回测/规则冲突扫描/打分卡审计；3 处规则模糊点（时间止损vs恐慌豁免 / 奔跑仓14天vs20日线破线 / 事件驱动A4vs打分卡A1）建议 8/31 归因评审仲裁
- ⏳ **贷款残留处置维持原框架**：`_meta.liabilities.in_cash ¥7,023.77` 不动用，8/31 归因后再定（现金 22.1% 超配定向留给进攻层，不追防御）
- 📁 影响文件：`05-scripts/gen_intraday_signal.py`（编码修复）、`05-scripts/daily_advice.py`（打分卡规则）、`05-scripts/stop_profit_backtest.py`（新增）、`04-reviews/special/2026-08-26-优化执行报告.md`（新增）、CLAUDE.md、会话检查点、system_version → v4.2.0

---

## v4.1.0 — 2026-08-25

### 当日指挥中心 Daily Hub（`06-dashboard/daily_hub.html` + `05-scripts/gen_daily_hub.py`）

**背景**：用户要求「做一个页面，可以让我快捷，方便，直观的看到当天需要看的文件，不需要一个一个找，结合到这一个页面上，直接转跳到文件，包含所有实时更新的网页，图表，仪表盘」，并「利用 skills，把页面打造的更好，可用性更高，跟美观协调」。

- 🏠 **单一入口聚合当天全部资源**：今日快照 KPI 行（总资产 ¥49,951 / 今日盈亏 -43 / 持有盈亏 +1,573 / 上证 3,889.44+0.19% / 决策 26 条 / 现金缓冲 22.1%）+ 四层配比条带（49.8/12.6/15.5/22.1，条宽∝配比）+ 今日信号红绿灯 6 项（半导体观察仓/创新药+3000暂缓/时机A待确认/月操作4/4满/回撤安全/决策日志）+ 实时仪表盘直达 4 大卡（私有看板/决策胜率/公开页/体系图集）+ 事件日历（未来 6 事件+关联文件）+ 报告中心（自动定位最新）+ 数据文件 4 + 系统工具 8 + 快捷口令 6
- 🔗 **点击直达 41 个有效链接**：主看板/决策仪表盘/公开页/图集/深度复盘/盘中研究/黄金专题/周报/归因/会话检查点/数据协议/规则手册 等，全部相对路径（`../04-reviews/...`），0 失效
- 📊 **数据全内联 + 权威口径**：`process_all()`（四层/风险/回撤/操作）与 `accuracy_report()`（26 条/78.9%/0.60:1）复用，与主看板 100% 一致；`__占位符__` 注入避免 f-string 与 HTML/JS 花括号冲突；file:// 下无 CORS 问题
- 🎬 **GSAP 轻量增强（复用 gsap-core skill）**：卡片 reveal 进场（IO 触发，KPI/信号/仪表盘/事件/文件/口令 59 元素）+ 四层条带从左砌入（scaleX 0→1）；`.no-gsap`（CDN 失败）与无 IO 均静态完整可见；`prefers-reduced-motion` 静态；复用看板主题色（`--bg:#02070f` 系）视觉协调
- 🛡️ **Workflow 对抗验证（4 独立维度 × 287k tokens）**：链接 41/41 有效 + 数据逐项 vs JSON 交叉一致（总资产/今日盈亏/持有盈亏/上证/四层/决策统计/事件/截止日期全过）；修复 **critical 1**（半导体观察仓把浮亏百分比误当金额「¥-15」→ 从 holdings 取市值「¥493」）+ **major 3**（IntersectionObserver 特性检测兜底 / `.gitignore` 补 `daily_hub.html` 防隐私泄漏 / `--dim` 对比度 3.4:1→4.5:1 提亮 #6b84a6）+ **minor 4**（图集文案精确化 / reduced-motion 补 !important / reveal 死代码激活为实际动效 / GSAP CDN 降级确认）
- ⚙️ **sync_all.py 集成**：新增步骤 7.5 自动生成 daily_hub.html（数据更新后自动刷新，无需手动跑）
- 📁 影响文件：`05-scripts/gen_daily_hub.py`（新增）、`06-dashboard/daily_hub.html`（生成产物）、`05-scripts/sync_all.py`（+步骤 7.5）、`.gitignore`（+daily_hub.html 隐私保护）、CLAUDE.md、会话检查点、system_version → v4.1.0

---

## v4.0.2 — 2026-08-23

### 公开页四层配比区重构：左对齐配比条带（anchor-pro.html）

**背景**：用户反馈四层金字塔区块「不够和谐美观」。此前居中层叠金字塔中，双 20% 层（核心/卫星）等宽形成「平边」，视觉失衡。用户选定「左对齐层带」方向。

- 🎨 **居中堆叠金字塔 → 左对齐配比条带**：四条条带左端对齐（探针证实 BANDLEFT 四等 291px）；条宽 ∝ 目标配比并归一化（最长条铺满轨道，45:20:20:15 比例忠实：397/175/175/131px）；图标在条首、百分比印在条尾；左侧色条 + 圆角右端，悬停发光
- 📊 **图例保留 + 双向高亮**：右列图例（色点+层名+纪律+占比）与条带 hover/focus 双向联动（无 GSAP 依赖，键盘/触屏可及）
- ✏️ **文案去重**：区块 p「层宽 = 目标配比」→「条宽即配比」；内部 h3「四层金字塔 · 层宽 = 目标配比」→「四层配比 · 条宽即配比」；aria-label「四层金字塔结构」→「四层配比结构」（配比仅出现一次）
- 🎬 **入场动效重构**：GSAP 效果 9「金字塔从地基建造」→「配比条带从左砌入」（条带 x:-28 左滑淡入 + 百分比印章弹入 + 图例右滑）；无 GSAP 时 CSS `pyrRise`→`pyrIn`（translateX 左滑）
- 🧹 移除：金字塔居中堆叠布局、flex-end 底锚定、clip-path 三角形背景、first/last-child 圆角特殊化
- ✅ **验证**：gen + smoke → **79 通过 / 0 失败**；Edge headless 4 路径（gsap 桌面 / no-gsap / 移动 390px / reduced-motion）零错误；BANDLEFT=291,291,291,291（左对齐）、BANDW=397,175,175,131（比例忠实）、PCTRIGHT=12（百分比在条尾）、双向高亮、aria/H3/legend 全对
- 📁 影响文件：`08-website/anchor-pro.html`（HTML/CSS/render JS/GSAP effect 9）、CLAUDE.md、会话检查点、system_version → v4.0.2

---

## v4.0.0 — 2026-08-22

### 公开页 GSAP 深度重构（最大程度使用 gsapskills · 架构大改，anchor-pro.html）

**背景**：用户要求「最大程度的使用 gsapskills，深度重构页面……有创意，流畅，视觉反馈强烈，深度交互能力」「优化每一个细节，不放过每一个角落」。v3.9/v3.10 的原生 canvas 粒子、打字机、3D tilt、演进时间线 sticky-pin 保留为降级路径，本次引入 **GSAP 全家桶**（ScrollSmoother + ScrollTrigger + SplitText + ScrambleText + ScrollToPlugin，全部免费）做架构级重构，对标 demos.gsap.com。

- 🔧 **架构**：6 个 GSAP CDN 外链全部放 `<head>`（内联 script 之前，满足 smoke 正则）；`body` 重构为 ScrollSmoother 要求的 `smooth-wrapper`/`smooth-content`（nav 外移到 wrapper 外、fixed 图层留在外）；CSS 末尾追加 `.js.gsap` 中和块；boot 加 `gsapOk` 六插件全校验 + `gsap`/`no-gsap` 类 + `registerPlugin`
- 🎬 **20 项 GSAP 效果**（`gsap.matchMedia` 桌面 + no-preference 门控，移动/reduced-motion 零动画静态可见）：① ScrollSmoother 全页惯性滚动 ② Hero h1 逐字进场（SplitText chars） ③ Command Card 3D 翻入 ④ 鼠标深度视差（quickTo 多深度 + 轴分离） ⑤ cursorGlow 顺滑跟手 ⑥ 标题逐字揭示（SplitText `ignore:"em"` 保住高亮） ⑦ kicker ScrambleText 乱码解码 ⑧ 分隔线生长 ⑨ 卡片网格 ScrollTrigger.batch stagger ⑩ 金字塔从地基建造 ⑪ 演进时间线 GSAP 横向 pin ⑫ 月度柱状图 scrub 生长 + hover ⑬ count-up 滚动触发 ⑭ 导航激活 + 锚点平滑 ⑮ 滚动进度条 ⑯ toTop ⑰ Hero 滚动视差 ⑱ 闭环总览联动揭示 ⑲ CTA/按钮微交互 ⑳ 卡片 3D tilt quickTo 顺滑升级
- 🛡️ **完整降级**：任一插件 CDN 失败 → `gsapOk=false` → `.no-gsap` 全原生（reveal/IO/原生 tilt/演进 sticky-pin/打字机），四路径运行时零报错
- 🔧 **对抗性复核 9 条确认修复**（20 子代理工作流，840k tokens）：① [HIGH] reduced-motion+GSAP 时演进时间线被 100vh/overflow 永久裁切 → 新增 reduced-motion CSS grid 回退（pin-stage static/auto/visible）② GSAP/原生 tilt 在持仓筛选 render() 重建卡片后失效 → 抽取 `bind`/`bindGsapTilt(scope)` + `window.__*` 重绑 ③ 移动端导航无汉堡 → 新增 nav-toggle 下拉 ④ gsapOk 缺插件检查 → 6 插件全校验 ⑤ Command Center 指标硬编码 vs D.hero 死字段 → 改为 D.hero 驱动（含体系评分大数字） ⑥ `--dim` 对比度 3.62:1→5.00:1（WCAG AA）⑦ 删死规则 `will-change` ⑧ batch `overwrite:true`→`"auto"`（保住 tilt 的 rotationX/Y） ⑨ 打字机按段数动态填充加固
- ✅ **验证**：`gen_anchor_pro.py` + `smoke_test.py` → **79 通过 / 0 失败**（新增 6 项检查：CDN-in-head/ScrollSmoother 结构/插件齐全/注册门控/关键 API/gsap-no-gsap 降级）；Edge headless 四路径（gsap 桌面 / no-gsap / 移动 390px / reduced-motion）全部零控制台错误；关键修复逐项运行时证实（reduced-motion 演进网格回退 pinH 805→286px、SplitText aria-label+aria-hidden、tilt 重建双模式生效、heroMetrics=D.hero、汉堡导航）
- 📁 影响文件：`08-website/anchor-pro.html`（架构重构 + 20 项效果 + 9 项修复）、`05-scripts/smoke_test.py`（6 项新检查）、CLAUDE.md、会话检查点、system_version → v4.0.0

---

## v4.0.1 — 2026-08-23

### 公开页修复与进化区重构（anchor-pro.html）

**背景**：用户反馈两个问题——① 首页 hero 有「固定界限」，文字遇到边界会消失；② 「体系持续进化」区块展示过于复杂，希望「简洁一些，或者增加讲述内容」。用户选定「简洁骨架 + 讲述」结合方案。

- 🐛 **① hero 固定界限修复**：`.hero{overflow:hidden}`（v3.9 为容纳 inset:-20% 光晕而加）在鼠标深度视差（`cmdX ±112px`）把 command-card 推出 hero 边界时**硬裁切右侧文字** → 移除 overflow:hidden + `.hero-glow` `inset:-20%→inset:0` + 视差幅度收敛（copyX 56 / cmdX 80 / glowX 110）。修复后 1366px 视口下 card 越界 61px < 侧边距 63px，**不再越出视口**，深度交互保留
- 🎨 **② 进化区「简洁骨架 + 讲述」重构**：6 卡横向 pin（v3.10.0/v4.0.0）→ **4 章节叙事**（地基/边界/自动化/下一站），各带一句话「为什么」故事；开头一句总述「规则不是设计出来的，是复盘长出来的」；横向连线生长 + 章节错峰入场（GSAP）替代长滚动 pin；分数仍由 `D.evo` 权威回填（数据驱动）
- 🧹 **同步清理**：删除 pin-shell/pin-stage/evo-track/pin-hint/timeline 全部 CSS+JS（原生 pin IIFE、GSAP pin 模块、reduced-motion 回退块）；count-up 目标 `.pin-stage .count-up`→`.evo-wrap .count-up`；smoke 检查「演进横向 pin」→「进化叙事章节」
- ✅ **验证**：gen + smoke → **79 通过 / 0 失败**；Edge headless 四路径（gsap/no-gsap/移动 390px/reduced-motion）全部零错误；chapters=4、D.evo 回填 80/96/96、heroOv=visible、移动 1 列
- 📁 影响文件：`08-website/anchor-pro.html`、`05-scripts/smoke_test.py`、CLAUDE.md、会话检查点、system_version → v4.0.1

---

## v3.10.0 — 2026-08-22

### 公开页演进时间线横向 Pin（GSAP 招牌技法 · 原生零依赖实现，anchor-pro.html）

**背景**：用户要求学习 demos.gsap.com 的设计创意与思路方法，找出适合 Anchor 的效果，把公开页升级为高级动态展示网站。GSAP demo 招牌技法中 **pin 固定 + 横向滚动**（ScrollTrigger pin + x 动画）最具冲击力；`#evolution` 六张版本卡（v3.0→v3.5，分数 80→96）是天然的横向叙事素材——把「体系逐版本进化」的推进感视觉化。用户确认：**原生 ES5 零依赖重实现**，只做此一个效果。

- 📌 **演进时间线横向 pin**：`#evoStrip` 外层加 `pin-shell`（JS 定高）/ `pin-stage`（`position:sticky;top:0;height:100vh`，天然实现 pin）/ `evo-track`（flex 横排，`transform:translate3d(-p*dist)` 随滚动进度横向滑动）；右下角「继续滚动探索 ↓」脉冲提示条
- 🧮 **滚动进度驱动**：`p=(0-shell.top)/(shell.height-100vh)` 映射到横向位移，rAF 节流 + passive，与既有 scroll 模块同构；resize/orientationchange/load 重测
- 📱 **优雅降级**：`(max-width:980px)` 或 `prefers-reduced-motion` 由 JS 加 `no-pin` 类回退原静态 grid（980px→3 列、640px→2 列保留）；noscript 无 `.js` 门控不触发 flex，时间线保持原排版
- 🎨 **卡片放大**：卡宽 `clamp(240px,24vw,380px)`，桌面任意宽度均保证横向滑距；stage 全屏出血（`width:100vw`），时间线横贯视口更富电影感
- ✅ **验证**：`python gen_anchor_pro.py` + `python smoke_test.py` → **73 通过 / 0 失败**（新增 1 项检查：演进时间线横向 pin）
- 📁 影响文件：`08-website/anchor-pro.html`（HTML 包裹 + CSS pin/回退 + JS 模块）、`05-scripts/smoke_test.py`（新检查）、CLAUDE.md、会话检查点、system_version → v3.10.0

---

## v3.9.2 — 2026-08-22

### 体系可视化简洁化：Diagrams 区 → 单一系统闭环总览（anchor-pro.html）

**背景**：用户连续两次反馈「体系可视化有点复杂，简洁一点，一目了然」「这块区域不够简洁」，并明确选择方案 A（单一系统闭环总览图）。

- 🔄 **Diagrams 区 3 张 iframe 图 → 单一系统闭环总览**：`#diagrams` 从「数据管道/决策闭环/系统架构」三张密集 iframe（均为 min-width:980px SVG 塞进 ~400px 卡片导致文字不可读）收敛为一张 HTML/CSS 内联四步闭环：**数据输入 → 规则处理 → 可视化输出 → 复盘迭代**（回流条标注「复盘结论回流规则与数据」）
- 📎 **详情保留为链接**：三张详细图改为底部胶囊链接（新标签打开），内容不丢失
- 📱 **响应式**：桌面四步横排箭头串联；移动端纵向堆叠、箭头旋转 ↓；reduced-motion 静态、stagger 交错进场保留
- 🧹 每步大字标题 + 一句话说明（图标 + 编号 + 顶边主题色），扫一眼即懂体系
- ✅ **验证**：`python gen_anchor_pro.py` + `python smoke_test.py` → **72 通过 / 0 失败**（新增 1 项检查：体系图简洁化——含 system-loop/loop-step、无 iframe 图）
- 📁 影响文件：`08-website/anchor-pro.html`（Diagrams 区重构）、`05-scripts/smoke_test.py`（新检查）、CLAUDE.md、会话检查点、system_version → v3.9.2

---

## v3.9.1 — 2026-08-22

### 公开页背景交互 + 体系可视化简洁化（anchor-pro.html）

**背景**：用户在 v3.9.0 基础上提出两点新要求——「① 鼠标滑动时背景内容可交互、画面随鼠标变化且符合主题；② 切换顺滑、由浅入深有代入感」，随后反馈「体系可视化有点复杂，简洁一点，一目了然」。

- 🖱️ **全页交互背景粒子场**：`#bgCanvas` 固定全屏 Canvas，资金流粒子随鼠标**推开形成涟漪**（rAF 节流 + passive，仅桌面 pointer:fine，移动端关闭，reduced-motion 静态）；配合既有 Hero 粒子**牵引**形成双层交互纵深
- 💡 **全页鼠标跟随光晕**：`#cursorGlow` 固定层低透明度泛光（z-index 夹在背景与内容之间），鼠标滑过全页光影随之变化
- 🎢 **由浅入深 reveal 门控**：`.js` 门控 fade + translateY(26px) + scale(.985) 深度入场（cubic-bezier 缓出），`.js .stagger>*` 交错保留；`.section` 加 `scroll-margin-top` 修正锚点定位；reduced-motion 全静态兜底
- 🧹 **体系可视化简洁化**：架构区从「金字塔 + 配比米尺」双栏收敛为**单一居中金字塔**（行宽即目标配比，每行只留一句话纪律 rule）；两段长注合并为一句隐私说明；`#allocationViz` 渲染移除（smoke/gen 均无引用，确认安全）
- ✅ **验证**：`python gen_anchor_pro.py` + `python smoke_test.py` → **71 通过 / 0 失败**（新增 2 项检查：全页交互背景粒子场、reveal 门控）
- 📁 影响文件：`08-website/anchor-pro.html`（背景交互 + 简洁化）、`05-scripts/smoke_test.py`（新检查）、CLAUDE.md、会话检查点、system_version → v3.9.1

---

## v3.9.0 — 2026-08-22

### 公开页首页 Command Center 高冲击重构（anchor-pro.html）

**背景**：用户「首页可以做成高端动态页面吗？」→ 方案确认方向为**高冲击 Command Center**（指挥中心观感），本次只重构公开页首页模板，私有看板 rebuild.py 不动。

- 🎨 **Hero 粒子资金流**：Canvas 蓝色/青色浮点 + 邻近连线（桌面 ≤72 / 移动 28，`prefers-reduced-motion` 单帧静态，`visibilitychange` 自动暂停）
- ⌨️ **打字机标题**：「不靠预测，靠纪律」逐字打出 + 琥珀光标；文案由 `D.copy.hero_typed` 驱动（copy 扩展 `hero_kicker`/`hero_typed`/`hero_sub`/`score_animated`）
- 🔢 **count-up 数字滚动**：Command Center 评分 96 + 时间线 6 版本分数从 0 滚动至终值（IntersectionObserver 触发，1.2s 缓出）
- ✨ **鼠标光晕 + 滚动视差 + 顶部进度条**：Hero 径向光晕跟随（仅 pointer:fine）、Hero 内容上移淡出、渐变滚动进度条（均 rAF 节流 + passive 监听）
- 🎴 **卡片 3D tilt**：KPI/规则/持仓/证据/噪声卡 ±7°X/±9°Y + translateZ 抬升（仅桌面 pointer:fine）
- 🎢 **stagger 交错进场**：9 个数据容器子卡片 70ms 级联入场；`.js` 门控（`documentElement.className+= " js"`）保证 noscript 不隐藏静态内容
- 🛡️ **无障碍/性能**：全部动效走 transform/opacity；`prefers-reduced-motion` 全静态兜底（打字机全文直显、count-up 直接终值、粒子单帧、tilt 关闭）
- ✅ **验证**：`python gen_anchor_pro.py` + `python smoke_test.py` → **69 通过 / 0 失败**（含新增 7 项 Command Center 结构检查）
- 📁 影响文件：`08-website/anchor-pro.html`（模板重构）、`05-scripts/gen_anchor_pro.py`（copy 字段扩展）、`05-scripts/smoke_test.py`（新结构检查）、`06-dashboard/portfolio_analysis_example.html`（数据块同步）、CLAUDE.md、会话检查点、system_version → v3.9.0

---

## v3.8.5 — 2026-08-21

### 8/21 收盘持仓同步 + 止损硬Deadline 修正（8/22 周六休市 → 8/25）

- 📊 **持仓同步**（用户 8/21 收盘提供 App 明细）：holdings_summary 10 项更新（基金+余额宝当日 **-7.90**、股票 515180 日亏 **-4**，组合当日 **-11.9**）
  - 半导体 mv 1018.47（浮亏 -136.65，-11.83%，整仓累计 +259.70）；创新药 +46.70（+1.85%）；黄金 -23.38（-1.13%）收窄；证券 4695.62（-104.38，-4.54%，8/21 +2500 后）
  - **total_assets 49866.68** = 基金 44134.68 + 股票 5732；净值口径 42842.91（扣贷款残留 7024）；四层 49.6 / 12.4 / 16.6 / 21.3%
  - 余额宝 13151.93 → 10627.21（-2524.72 = 证券 +2500 自余额宝扣款 + 当日净额）；8 月操作 3/4（8/7 半导体 + 8/20 债券 + 8/21 证券）
  - daily_summaries 追加 8/21；chart_data 08-21 pnl → -11.9；stop_loss_watch cur_pct → -11.83
- ⚠️ **止损硬Deadline 修正**：**8/22 为周六休市**（8/19 触发时误写 8/22）→ 修正为 **8/25（周二）**（触发后第 4 交易日，跳过 8/22-23 周末）；**8/24（周一）为最后缓冲日**；缓冲已用 2 次（8/20、8/21）
- 📁 影响文件：`portfolio_data.json`（holdings_summary / 汇总 / daily_summaries / chart_data / stop_loss_watch / system_version→v3.8.5）、`04-reviews/daily/2026-08-21-深度复盘.md`（新建）、CLAUDE.md、规则手册 v3.4、会话检查点、记忆备份（07-memory）

---

## v3.8.4 — 2026-08-21

### 半导体数据彻底更正（用户 8/21 提供完整真实流水）

- 🔍 **核心发现**：v3.8.1 只核对了 2026 年 6-8 月的 8 笔流水，**严重不全**。用户提供完整交易记录后确认：半导体基金（华夏半导体芯片ETF联接C 008888）**自 2025-10-10 建仓**，真实流水 **42 买 + 13 卖**（2025-10 至 2026-08）
- ✅ **整仓累计盈利 +253.36**（holdings cumul，含已实现；7/16 研究报告曾记载累计盈亏 **+452.07**，减仓锁利 + 后续回撤后为 +253.36）——此前把「当前持仓浮亏 -12.38%」误当整仓亏损的**叙事错误已纠正**：半导体不是亏损股
- ⚠️ **当前持仓浮亏 -142.99（-12.38%）真实**：止损（8/19 触发 → 8/25 硬Deadline）针对**当前持仓**，非整仓；净投入 **758.77**（42买+13卖，含 8/7 加仓 300；App 交易统计显示 408.77 未含 8/7，差异待 App 核实）
- ✅ **transactions 补全**：半导体 8 笔 → **55 笔**（总量 72 → 119 笔）；6/15 修正为「加仓（2025-10 已建仓）」；8/7 加仓 300 用户确认存在（流水表格漏行）
- ✅ **decision_log 修正**：#4 补口径澄清（止损针对当前持仓，整仓累计盈利 +253.36）；#6/#20 review_note 补整仓口径；#7 rationale 修正（去掉误导性「补到合计1000」，实为 7/16 建议 +¥300 ¥1549→¥1849，用户未执行反而减仓44%）；#10 实盈 **+14.87 确认自洽**（7/16 报告持仓盈亏 +34.15 × 减仓44% ≈ +15，pnl_pct=2.3%）——统计不变（80% / -6.23% / 0.93:1，加仓 50%）
- 📁 影响文件：`portfolio_data.json`（transactions + holdings note + system_version→v3.8.4）、`06-dashboard/decision_log.json`、CLAUDE.md、会话检查点、记忆备份（07-memory）

---

## v3.8.3 — 2026-08-21

### 创新药操作深度分析 + 决策日志准确化

- 🔬 **深度分析**（5 代理交叉归因 + 盈亏口径核实）：创新药 2025-08-19 建仓至 2026-08-21 全周期复盘 → `04-reviews/special/2026-08-21-创新药操作深度分析.md`
  - **核心结论**：12 个月累计净亏 **-258.58**（8/21 用户确认到今日，约 -10.7% 净投入口径；8/20 为 -248.04、当日亏 -10.54），当前 +2.27% 浮盈全靠 8/20 单日行情
  - **纠正此前推算**：回填时按 App 买入口径 7,224.46 推出「+171.96 盈利」为**低成本假象**——平台 `cumul=-258.58`（8/21 确认）反推买入口径为 7,655.00；真实拆解 = 已实现 ≈-315.82 + 浮盈 +57.24
  - **行为模式**：追涨/右侧惯性为主风险；「摊低→割肉」循环；高抛低吸实为亏损成交；唯一纪律 = 小额定投 + 底部回补
- 📝 **decision_log 准确化**：#14 review_note 补整仓累计口径（8/21 用户确认累计 -258.58 = 8/20 -248.04 − 当日亏 10.54，净投入约 -10.7%；7/21 位置正确 ≠ 整仓操作正确）；#1/#14 补 snapshot（8/20 HSSCID 3104.56 +5.36%、ETF 获利了结 2.84 亿；7/21 资金切换背景）——统计不变（80% / -6.23% / 0.93:1，加仓 50%）；决策仪表盘已重建（decision_dashboard.html）
- ✅ **买入口径澄清**（用户 8/21 补充）：9/25 实为**卖出 293.77**（非买入）；12/08 转换转入成本基数确认 = **208.87 份 × 净值 1.0986 = 229.46**（差 0.0046 舍入）；三口径分解：明细 7,594.46（纯买 6,950 + 定投 415 + 转换 229.46）、平台 7,655.00 = 明细 + 60.54、App 7,224.46 = 明细 - 370；**剩余 ~60 元来源未定**（C 类无申购费，用户 App 无可查字段，疑分红再投资/转换费/未导出流水，如实保持未定）；对外口径统一用平台 `cumul=-258.58`（8/21 用户确认到今日：8/20 累计 -248.04、**8/21 当日亏 -10.54**）
- 📁 影响文件：`portfolio_data.json`（system_version→v3.8.3）、`06-dashboard/decision_log.json`、`04-reviews/special/2026-08-21-创新药操作深度分析.md`（新建）、CLAUDE.md、会话检查点、记忆备份（07-memory）

---

## v3.8.2 — 2026-08-21

### 创新药/天弘通利 2026 真实流水回填（用户提供完整 App 导出）

- 🔍 **核对发现**：transactions 创新药仅 1 条（7/21 试探 300）、天弘通利仅 3 条定投，远不足以支撑月度操作统计；用户导出完整历史后确认回填 **仅 2026 年至今**
- ✅ **transactions 回填**（30 → **72** 笔，CRLF 保留）：
  - **创新药**（易方达港股创新药ETF联接C）：补 2026 买入 **12 笔**（1/8+500、1/26+300、2/26+1000、3/3+100、4/30+200、5/15+300、**5/18+350、5/19+150、5/28+200、6/2+200、6/3+200、7/21+300**——后 6 笔为用户确认）+ 卖出 **4 笔**（1/21 599.96、2/3 508.70、3/4 1292.48、4/1 634.32）；7/21 旧「买入试探」条目更新为完整名称+真实成交
  - **天弘通利**（混合A）：补 2026 定投 **21 笔**（1/5-8/3，含 7/6-8/3 之前流水）+ 手动买入 2 笔（1/30+100、2/4+200）+ 卖出 **4 笔**（2/2 524.56、3/4 669.89、5/15 741.75、**6/26 497.42**——按之前流水确认）
- ✅ **decision_log 修正**：#14 类型「建仓」→「**加仓**」（基金自 2025-08 已建仓，7/21 实为加仓）——**加仓准确率 40%→50%**（#14 correct 正确入桶）；总统计不变（80% / -6.23% / 0.93:1）
- 📌 定投条目全部标注「非手动不计月限额」；新增手动买卖计入对应月份操作统计（2026-01~07 更真实）；**不新增 decision_log 决策**（5-6 月买卖非 Anchor 建议，避免虚增胜率）
- 📁 影响文件：`portfolio_data.json`（transactions + system_version→v3.8.2）、`06-dashboard/decision_log.json`、CLAUDE.md、会话检查点、记忆备份（07-memory）

---

## v3.8.1 — 2026-08-21

### 半导体真实操作流水核对修正（用户提供 App 流水 8 笔）

- 🔍 **核对发现**：transactions 半导体仅 4 条且含错误——「7/16 加仓 300」是把 7/16 研究报告的**建议**误当**执行**（真实 7/16 是减仓 44%）；「卖出 674.57」金额错误（真实 637.99，与 deep_review 记载「减仓44%回笼¥638」吻合）；6 月建仓/加仓 5 笔缺失
- ✅ **transactions 修正**：补录 6/15 +200（建仓）、6/26 +300、6/30 -355.16、7/2 +300、7/3 +500 共 5 笔；删除虚构「7/16 加仓 300」；「7/16 减仓」金额 674.57→637.99。净投入 = 1,800-993.15 = **806.85**
- ✅ **decision_log 修正**：#7（7/16）verdict「执行买入」→「建议买入」（7/16 报告建议加仓但用户实际减仓，建议未执行）；#10 卖出金额 674.57→637.99（实盈待确认）。统计不变（80% / -6.23% / 0.93:1）
- ⚠️ **待续**：创新药（4/1 卖出前必有更早持仓）、天弘通利（6/26 卖出前必有更早定投）流水待用户补充后补录；持仓成本以 App 当前显示为准（holdings 未动）
- 📁 影响文件：`portfolio_data.json`（transactions + system_version→v3.8.1）、`06-dashboard/decision_log.json`、CLAUDE.md、会话检查点、记忆备份（07-memory）

---

## v3.8.0 — 2026-08-21

### 深度归因 → 优化方案（用户：「深刻思考原因，吸取失败经验，形成更好的优化方案，提高胜率收益率，决策表每天同步」）

- 🔬 **六透镜独立交叉归因**（6 个代理各自独立核读源文件后综合）：
  - **买点工程**：半导体 3 笔全错 = 三种追高形态（7/15 事件接刀 / 7/16 机械抄底 / 8/7 右侧追顶），坏买点=只验趋势不验位置；成功买点全部 = 估值锚+回调日+小仓试探（证券 3 笔+创新药）
  - **换仓**：已实现亏损 -1,109 的 **75%（-834）来自两次换仓**（#1 卫星→诺安 -383「伪换仓」同方向集中、#2 红利低波→中证2000 -418「防御换进攻」）——卖买绑一步、用「换」字豁免全部纪律
  - **止损拖延**：规则只有触发无截止时间，中证2000 从 -6% 拖到 -17% 放大 2.8×；但 7 笔止损判定全部 correct——判断力从不差，差的是执行速度
  - **盈亏比 0.93:1**（4赚12亏，均盈 +11.18% vs 均亏 -12.03%）——赚端小步收割、亏端拖延确认，方向准确率 80% 却落地亏损
  - **仓位结构**：压舱石+现金 +1,318 靠规则赚钱，卫星净浮亏 -193；证券 8/21 单笔 +2,500 推至 ¥4,692 超单只上限 56%
  - **决策闭环五环断裂**：记录/快照/字段/提醒/统计未接入每日主线
- 📄 **方案文档**：`04-reviews/special/2026-08-21-深度归因与优化方案-v3.8.md`（A-E 六大类新规则 + 7 项可量化目标，8/31 归因验证）

### 新增制度与脚本（v3.4 规则手册）

- 📘 **规则手册 v3.3 → v3.4**（`01-rules/投资规则手册_v3.4_正式版.md`，已重命名）：
  - **A 买点**：买点评分卡 ≥3/5（估值锚/回调日/恐慌强度/资金确认/试探仓）、追红日禁买、浮亏仓禁摊平、事件驱动 ≤¥300 试探
  - **B 止损**：硬Deadline（触发后缓冲+延期合计≤3 交易日，第 4 天 14:30 无条件执行）、负面清单 10 日清仓、逻辑证伪 1 日减仓 50%、`stop_loss_watch` 状态机
  - **C 止盈**：档位重构 +8%/+15%/+25% 各 25% + 25% 奔跑仓跟 20 日线；盈亏比记账 + 目标 ≥1.5:1（连续 2 次 <1.0 冻结买入）
  - **D 换仓**：7 项检查清单、强制拆两笔 + 72h 冻结、主动基金间转换永久禁止
  - **E 仓位**：单只卫星 ≤¥3,000、追高单次 ≤¥300、卫星月净投 ≤¥1,500、定投白名单+弱势熔断
- 🐍 **`decision_log.py` v3.4**：`--report` 分桶新增 **盈亏比（avg_win/avg_loss/pnl_ratio）+ 追高型买入占比**，仪表盘新增 2 个 KPI 卡；新增 **`--stopwatch`** 止损倒计时（读 `portfolio_data.json stop_loss_watch`，按硬Deadline 三态）
- 🔄 **`sync_all.py` 第 7 步**：决策日志每日同步扩为四步 `--due → --report → --stopwatch → --dashboard`（每天随数据更新自动执行）
- 📐 **数据更新协议 v1.3**：每日层「决策日志」自动同步 + 操作步骤第 10 步说明

### 数据
- 📊 **`portfolio_data.json` 新增 `stop_loss_watch`**：半导体 {触发 8/19、缓冲延期已用 2 次、硬Deadline **8/25**、浮亏 -12.38%}，system_version → **v3.8.0**
- 🎯 **盈亏比口径校正**：方案初稿 0.76:1 → 实算 0.93:1（decision_log.py 等权分桶，16 笔已复盘），已同步修正方案文档与规则手册

### 影响文件
- 新增：`04-reviews/special/2026-08-21-深度归因与优化方案-v3.8.md`、`01-rules/投资规则手册_v3.4_正式版.md`（原 v3.3 重命名）
- 修改：`05-scripts/decision_log.py`（--report 盈亏比/追高 + --stopwatch）、`05-scripts/sync_all.py`（第7步四步循环）、`00-system/数据更新协议.md`（v1.3）、`CLAUDE.md`（v3.8.0）、`portfolio_data.json`（stop_loss_watch + system_version v3.8.0）、`06-dashboard/decision_dashboard.html`（盈亏比/追高 KPI，gitignore）

---

## v3.7.2 — 2026-08-21

### 数据修复（7/24 证券+500 交易缺口回填）

- 📥 **回填 7/24 证券+500 至 transactions**：用户确认买入 + deep_review_20260724 复盘记载 → 补录 `{"date":"2026-07-24","name":"易方达证券ETF联接C","op":"加仓","amount":500}`（transactions 25→26 条）
- 🎯 **成本口径核对结论**：cost_basis **1.1606 已包含 7/24 这笔**——7/24 当日复盘「仓位 ¥2,266 + 浮亏 ¥34 = 成本 ¥2,300」，该成本 8/5（研究报告成本价 1.1606）与 8/20（¥2,300）保持一致，故**不改 cost_basis、不改 mv/pnl**，仅补交易记录
- 🔢 **平均收益率 -6.23% 三视角交叉验证**（账户盈亏/统计口径/亏损归因 3 个代理独立核读源文件）：确认是 16 笔补录 pnl_pct 的等权算术平均，混合「已实现盈亏率 + 未实现浮盈亏率」两种口径，金额加权后约 -10.0%；正确边界 = 「每笔决策平均落地幅度」，**不是**系统盈利，也**不是**「每笔操作平均亏 6.23%」
- ♻️ 重建看板（rebuild.py）：6-dashboard 双副本同步，总资产 ¥52,379（含证券+2500 T+1）

### 影响文件
- 修改：`portfolio_data.json`（transactions +1=26、system_version v3.7.2）、`06-dashboard/portfolio_analysis.html`、`06-dashboard/portfolio_snapshot.json`（重新生成）

---

## v3.7.1 — 2026-08-21

### 新增（决策日志历史补录：胜率覆盖完整历史）
- 🆕 **`decision_log.py` 新增 `--backfill`**：补录历史决策（`"日期|类型|标的|判定|金额|依据|预期"` 多行 CSV），自动标记 `backfilled=true`，T+3 到期日按记录日+3精确计算
- 📥 **补录 16 笔历史手动操作**（7/15–8/7 + 7/24，来自 portfolio_data.json transactions + 复盘报告交叉验证）：决策数 5 → **21 条**
- 📊 **回填 16 笔历史 outcome + 真实收益率**（依据 04-reviews 复盘报告/教训库明确记载）：
  - **准确率 80%**（correct 12 / wrong 3 / neutral 1）
  - **平均收益率 -6.23%**（真实：卖出=实现盈亏/成本，买入=当前浮盈浮亏位置级）——为负主因：① 半导体追高 3 笔 wrong 均 -12.4% ② 止损卖出的实现亏损（中证2000 -17%、光伏 -20.5%、诺安 -17.8% 等，方向对但落地为亏损截断）；TMT50 阶梯止盈 +22.1%/+18%、半导体减仓 +2.3% 为正
  - 🔍 **核心发现：加仓准确率仅 40%**（2对3错1中性）——3 笔全错均为**半导体追高补仓**（7/15 打新布局、7/16 触-12%加仓线、8/7 DDX未过建仓检查），印证教训库「追高/补仓摊平是亏损之源」
  - 卖出纪律优秀：减仓 100%（3/3）、清仓 100%（6/6）、建仓 100%（1/1）
- ⚠️ **发现数据缺口**：portfolio_data.json transactions **缺失 7/24 证券+500**（复盘报告 deep_review_20260724 明确记载，decision_log 已补录 #21，待用户确认是否回填 transactions）

### 影响文件
- 修改：`05-scripts/decision_log.py`（--backfill）、`06-dashboard/decision_log.json`（21 条，gitignore）、`06-dashboard/decision_dashboard.html`（重新生成，gitignore）

---

## v3.7.0 — 2026-08-21

### 新增（可视化升级：品牌体系图 + 决策闭环自动化）
- 🆕 **`08-website/diagrams/` 体系图集（4 张 Anchor 品牌 SVG 图）**：深色金融终端风格，统一品牌色——
  - **`pyramid-4layer.html` 四层金字塔**：目标比例 + 当前占比偏差 + 典型持仓，塔尖现金「弹药」/塔底压舱石「基石」
  - **`data-pipeline.html` 数据管道**：定位/获取/归因/验证 四层规范流程
  - **`decision-loop.html` 决策闭环**：建议→观察→T+3复盘→校准 循环
  - **`architecture.html` 系统架构**：数据源→处理→输出→发布 全链路
- 🆕 **`~/.diagram-design/profiles/anchor.md` + `Anchor/.diagram-design`**：diagram-design 品牌 profile（语义角色 token + 四层金字塔层级色）+ 项目标记
- 🆕 **`decision_log.py` v2.0 增强**：
  - `--due`：T+3 到期日精确计算（记录日+3自然日），今日到期/最近复盘日提醒
  - `--dashboard`：生成品牌化胜率仪表盘 `06-dashboard/decision_dashboard.html`
  - `sync_all.py` 新增第 7 步：自动生成决策仪表盘
- 🆕 **私有看板 `rebuild.py`**：四层配置面板新增**动态 SVG 金字塔**（数据来自 `D.layers`，底部压舱石→顶部现金）+ 页脚体系图集链接（JS 修正路径兼容桌面/06-dashboard 双副本）

### 修复（代码质量审查，mattpocock 方法）
- 🐛 **`anchor_to_astrbot.py`**：`os` 未导入 → AstrBot 同步启动即 NameError，补 `import os`
- 🐛 **`test_calculations.py`**：删除残留同义反复断言（`prefixes` 未定义 + 注释自证「发现不了误计」）
- ✅ 46 项测试 45 过 1 数据待办（证券+2500 加仓后 total_assets 待用户收盘数据同步，非代码 bug）

### 影响文件
- 新增：`08-website/diagrams/*.html`（4 张）、`.diagram-design`、`06-dashboard/decision_dashboard.html`（gitignore）、`04-reviews/special/2026-08-21-代码质量审查.md`
- 修改：`05-scripts/{decision_log,rebuild,sync_all,anchor_to_astrbot,test_calculations}.py`、`08-website/anchor-pro.html`、`.gitignore`

---

## v3.6.0 — 2026-08-20

### 新增（数据制度升级：准确性 → 可回溯）
- 🆕 **`05-scripts/data_pipeline.py`（数据管道四层规范）**：把 8/20 深度研究教训固化成代码——
  - **① 定位层** `DATA_PIPELINE_MAP`：9 类持仓 → 跟踪指数 → 市场 → 资金面来源 → 宏观归因 → mx查询模板 映射（防张冠李戴：港股看南向/债券看中国10Y/QDII看美股净值+溢价）
  - **② 获取层** `fund_flow_snapshot()`：mx-data raw JSON → 标准资金面快照（{ddx, ddx3, ddx10, 超大单净流入, 时间}），实测兼容双层嵌套结构 + 「亿元」单位 + 列名模糊匹配
  - **③ 归因层** `MACRO_DRIVER_MAP`：资产类别 → 正确宏观驱动（防因果错配，如债券归因美债）
  - **④ 验证层** `data_quality_check()`：报告前自检（时间戳/资金面/来源标注 + 持仓逐只覆盖）
- 🆕 **`05-scripts/decision_log.py`（决策闭环 + 胜率统计）**：用户「在一次次操作中吸取经验，提高胜率/收益率/准确率」的落地——
  - `--add` 记录决策（类型/标的/判定/金额/依据/预期方向 + 数据快照）→ `--review` 事后回填结果（correct/wrong/neutral + 收益率）→ `--report` 按类型统计准确率/平均收益率 → `--pending` 列出待复盘项（T+3）
  - 数据存 `06-dashboard/decision_log.json`（.gitignore，含判断依据属隐私）
  - 8/20 已录入 3 条种子决策：创新药+3,000 暂缓 / 纳指+3,000 暂缓 / 债券+5,700 执行

### 修改文件
05-scripts/data_pipeline.py（新增）| 05-scripts/decision_log.py（新增）| 06-dashboard/decision_log.json（新增，gitignore）| .gitignore（新增 decision_log 条目）| portfolio_data.json（system_version v3.6.0）| CHANGELOG.md | CLAUDE.md | 00-system/会话检查点.md

---

## v3.5.5 — 2026-08-19

### 修复
- 🔴 **回撤安全垫口径修正（净值口径）**：8/19 深度复盘发现看板「距 -5% 线安全垫 ¥10,145」被**贷款残留虚增 3.3 倍**
  - **根因**：`compute_drawdown_state` 用 `totals['total']`（账户口径 44,017，余额宝含贷款残留 7,023.77）对比 peak_assets 35,655 → dd_pct 假新高 +23.5%、安全垫虚高
  - **修复**：JSON `_meta.liabilities` 记录贷款结构（total 23,000 / repaid_from_cash 15,976.23 / in_cash 7,023.77）；引擎新增 `liabilities_in_cash()`，回撤/安全垫改用**净值口径** `net_assets = total_assets - liabilities.in_cash`
  - **修复后**：净值 ¥36,993 → dd_pct **+3.75%（round +3.8%）**、距 -5% 线（¥33,872）安全垫 **¥3,121**（原虚高 ¥10,145）。一次像样回调即触发预警，口径与真实风险一致
  - `drawdown_state` 输出新增 `total_assets` / `net_assets` / `liabilities_in_cash` 字段；四层占比保持账户口径，现金层贷款残留由副标题标注

### 变更
- 🔧 **看板显示净值口径**：Total assets KPI 副标题加「自有 ¥36,993」；Drawdown KPI 副标题加「净值 ¥36,993」；回撤区新增「净值口径」行（自有净值+扣贷款）；安全垫规则文案改为「自有净值距 -5% 线…已扣贷款残留 ¥7,024」
- 🔧 **drawdown_alert.py**：文本输出加「自有净值」行，`--json` 输出新增 `net_assets` / `liabilities_in_cash` 字段
- 🧪 **测试**：test_calculations 新增 5 项净值口径用例（liabilities 解析、net_assets/dd_pct/cushion 净值计算、与 drawdown_status 等价、无贷款兼容），`test_drawdown_check` 改用引擎净值口径；46 项全通过

### 修改文件
portfolio_data.json（system_version v3.5.5 + _meta.liabilities）| 05-scripts/data_processor.py（liabilities_in_cash + compute_drawdown_state 净值口径 + 规则文案）| 05-scripts/rebuild.py（KPI/回撤区净值标注）| 05-scripts/drawdown_alert.py（净值输出）| 05-scripts/test_calculations.py（净值口径用例）| CHANGELOG.md

### 数据（8/19，决策记录，不 bump 版本）
- 📝 **贷款资金配置方案**：贷款 ¥28,619（余额宝 7,023.77 + 银行卡 21,595）用户确认**债券垫 20%**方案——立即建仓 ¥6,000（创新药 +3,000 + 纳指 +3,000）＋ 债券垫 ¥5,700（中银稳健增利）＋ 右侧子弹 ¥12,300（时机 A 站稳3,900→6,000 / B DDX连正→3,800 / C 跌至3,741→2,500）＋ 机动 ¥4,619；目标年化 10-13%、胜率 70%+、波动 5-8%
- 📄 文档：`04-reviews/special/2026-08-19-贷款配置方案.md`（新增）｜ pending_actions 贷款项已更新为执行中（银行卡 21,595 需先转入余额宝）

---

## v3.5.4 — 2026-08-18

### 新增
- 🆕 **数据更新后自动生成深度复盘（新制度）**：用户确认流程——每次持仓数据更新入库后，Claude 自动按标准模板生成深度复盘报告（①逐只持仓涨跌 + 板块技术面/基本面 + 明日/一周展望 ②操作建议 + 上次操作效果 ③新机会/新板块），无需用户再次下达指令
- 📄 首份按新制度自动生成：`04-reviews/daily/2026-08-18-深度复盘.md`（总资产 ¥37,251.98，半导体 DDX 转负 -0.181 计数归零、固态电池飘红中断、创新药浮亏缩至 -0.65%）

### 修复
- 🔧 **day_pct 口径修正**：portfolio_data.json 中持仓 day_pct 曾误填持有收益率，已修正为当日涨跌幅（引擎 gold_day_pct > 0.02 用于黄金过热判断，口径必须一致）

### 数据
- 📊 8/18 用户数据入库：总资产 ¥37,251.98（fund ¥31,571.98 + stock ¥5,680）、持有盈亏 +¥1,636.92；余额宝 +1,475.19 为银行卡转入（用户确认）；通利市值修正（8/17 记录含定投 135 误差 → 真实市值 2,632.41）

### 数据（8/19）
- 📊 **贷款 ¥23,000 到账余额宝**（用户确认，无息贷款提前到账）：yuebao 6,153.05 → 29,153.05、fund_account 31,571.98 → 54,571.98、**total_assets ¥37,252 → ¥60,252**、update_time 2026-08-19 14:39
  - transactions 新增 8/19 余额宝转入 23,000（无息贷款）；pending_actions 贷款项更新为「已到账待配置 → 8/31 归因后再定，暂不动用」；_meta.loan_status 记录纪律
  - ⚠️ 现金层 16.5% → **48.4% 严重超配**，按纪律「杠杆不给未验证的系统」，先看 8/31 归因再配置
- 🚨 **半导体 -7.54% 暴跌二次触发 -8% 止损**（8/19 盘中报告）：估算浮亏 -11.56%，上证 3,886 失守 3900、科创50 -7.24%；8/20 14:30 缓冲确认（跌>0.5% 执行止损一半）+ 创新药满 30 天评估双事件
- 📄 盘中研究报告：`04-reviews/daily/2026-08-19-盘中研究报告.md`（新增）
- 📊 **8/19 收盘数据入库**（用户晚 21:00 前后提供）：total_assets **¥44,016.90**、fund ¥38,308.90 + stock ¥5,708 + 余额宝 ¥13,176.82
  - **半导体坐实 -11.42%**（1023.26，-81.62 当日）二次触发止损；创新药浮亏扩至 -35.73（-1.42%）；黄金 -4.96%；证券 -4.56%；纳指A/C 浮盈收窄至 +10.77%/+13.19%；515180 1.427（+0.33%）唯一飘红
  - 贷款转出 15,976.23 至银行卡（用户确认），余额宝剩贷款部分约 7,023.77；现金层 48.4% → **29.9%**（仍超配目标 15%）
  - 四层：压舱石 43.0% / 核心 14.1% / 卫星 13.0% / 现金 29.9%；PnL +1,353（引擎重算）；1 RED（半导体止损）+ 2 AMBER
  - 行情确认（妙想 21:36）：上证 3,894.42（-2.40%）失守 3900、科创50 1,667.52（-6.89%）、半导体板块 -7.61%（DDX -0.457、超大单 -289.9亿）、恒生医疗 -0.63% 抗跌、溢价率 7.83%
- 📄 **深度复盘报告（v3.5.4 自动制度）**：`04-reviews/daily/2026-08-19-深度复盘.md`（新增）
- 🔧 **数据一致性修正**：total_hold_pnl_est 1636.92 → **1353.19**（8/18 残留旧值，按 _meta.pnl_total_note 口径重算 Σ活跃持仓）；market / daily_summaries / chart_data 补全 8/19 记录

### 修复（8/19）
- 🔧 **14:30 推送连续失败（WinError 10061）**：根因= Docker Desktop 未启动 → AstrBot 容器退出（8/12 起）→ 6185 端口无服务 → 每日推送登录被拒。已启动 Docker + `docker start astrbot`，API 恢复（HTTP 200）
- 🔧 **半导体止损信号自相矛盾**：portfolio_data.json 的 pending_action 残留 8/17「止损解除」旧文案（8/19 二次触发漏更新）→ 已更新为「8/19 二次触发，8/20 14:30 缓冲确认」；daily_advice.py 止损信号改为只拼 action（不拼 name+action 全文），防止过期状态文案污染决策信号
- 📌 确认：DDX 补仓规则为「连 2 日为正」（v3.3 手册 2.6 节），代码正确，无需改动

### 修改文件
portfolio_data.json（8/19 贷款 + 半导体止损状态 + system_version + 收盘入库 + total_hold_pnl_est 修正 + market/daily_summaries/chart_data）| 05-scripts/daily_advice.py | 04-reviews/daily/2026-08-19-盘中研究报告.md（新增）| 04-reviews/daily/2026-08-19-深度复盘.md（新增）| 00-system/会话检查点.md | CLAUDE.md | CHANGELOG.md

---

## v3.5.3 — 2026-08-17

### 修复（全面审查 8/17）
- 🔴 **隐私泄漏**：WeChat 会话 ID 从公开代码/文档移除（05-scripts/daily_advice.py + 00-system/每日建议推送说明.md），改读环境变量 WECHAT_SESSION（setx 已配置）；历史提交仍含旧 ID，如需彻底清除需重写历史或轮换会话
- 🔴 **失效 GitHub Action**：.github/workflows/anchor-daily.yml 引用的脚本已归档（gitignore 中不存在），每个工作日静默失败 → 已停用计划任务，保留手动触发；现行自动化 = Windows 计划任务（14:30 推送 / 21:30 知识库 / 21:40 兜底 / watch_sync 实时监听）
- 🟠 **时间止损截止日解析加固**：改为语义解析（建仓日+30 天 ±7 容差 + 建仓日合理性守卫），防止 pending_actions 文本杂项日期（如 8/31 归因）劫持截止日；5 场景测试通过
- 🟠 **watch_sync 失败退避**：同步失败后 5 分钟退避，防止 JSON 损坏时反复轰击 sync_all
- 🟠 **Excel 语法错误**：gen_excel_skill.py 红盈绿亏改造引入的括号错误（私有脚本不在 CI 覆盖范围，此前漏网）已修复
- 🟡 **私有看板版本标签**：v3.3 → v3.5（标题/徽标/页脚）
- 🟡 **噪声台账回填**：8/12-8/17 DDX/信号/规则命中（来源：已存档复盘报告），DDX 准确率统计可继续

### 新增
- 🆕 **DDX 补仓窗口进 14:30 推送**：daily_advice.py 新增第 5 组查询（半导体 DDX + 超大单），结合台账连击天数直接输出「连 X 日为正 / 补仓条件满足」信号，无需翻复盘
- 🆕 **smoke 新增全量编译检查**（7b）：本地 compileall 覆盖 gitignored 私有脚本（CI 覆盖不到），62 项
- 🆕 **决策效率**：14:30 推送信号链 = 止损确认自动判定 + DDX 补仓窗口 + 回撤安全垫 + 溢价率 + 时间止损倒计时（全部实时/台账数据，无人工查询）

### 修改文件
05-scripts/daily_advice.py | 05-scripts/data_processor.py | 05-scripts/watch_sync.py | 05-scripts/rebuild.py | 05-scripts/smoke_test.py | 05-scripts/gen_excel_skill.py（本地）| .github/workflows/anchor-daily.yml | 00-system/每日建议推送说明.md | ANCHOR_体系总览.md | 06-dashboard/noise/*.json | 00-system/会话检查点.md | CHANGELOG.md

---

## v3.5.2 — 2026-08-15

### 新增
- 🆕 **止损确认自动化**：daily_advice.py 14:30 推送新增「止损确认」自动判定——实时板块涨跌 vs 文案阈值（默认 0.5%）→ 输出【执行止损一半】/【延期一天】/【人工确认】，消除人工判断延迟（三场景实测通过）
- 🆕 **噪声审计 v1.1**：noise_audit.py 新增盘中振幅比（--log-swing）、操作时机偏差（--log-timing）、规则命中台账（--log-rule）采集；每周评分卡改为真实数据评分（原为默认值）+ 台账汇总
- 🆕 **规则命中台账**：noise/rule_hits.json + 7 月已文档化命中回填基线（7 条：浮亏不加仓/恐慌豁免/反弹清仓/风口确认/DDX/冻结/止损缓冲）
- 🆕 **月度归因自动化补全**：gen_monthly_attribution.py 自动汇总规则台账（新增第七章）、操作计数改用 monthly_ops_summary（定投不计）、输出目录对齐 04-reviews/monthly/
- 🆕 **8/20 时间止损执行预案**（00-system/8月20日时间止损执行预案.md）：规则优先级 + 判定流程 + 错了怎么办 + 记录要求
- 🆕 **现金补仓条件量化提案** + **watchlist 左右侧逻辑统一** + **持仓收敛路径** + **示例指数基金通利定位**（规则手册 §1.4/§5.1）
- 🆕 **优化时间表**（00-system/优化时间表.md）：8/20 → 8/31 → 9月 → 10月验证项排期与续优化条件

### 修复
- 🔧 **数据口径统一**：total_hold_pnl_est 改为引擎按用户逐项数据求和（+1,565 → +1,441.23），_meta.pnl_total_note 记录口径，消除看板与 JSON 盈亏数字打架

### 修改文件
portfolio_data.json（total_hold_pnl_est + watchlist trigger）| 05-scripts/daily_advice.py | 05-scripts/noise_audit.py | 05-scripts/gen_monthly_attribution.py | 01-rules/投资规则手册_v3.3_正式版.md | 00-system/噪声审计框架.md（v1.1）| 00-system/每日建议推送说明.md | 00-system/8月20日时间止损执行预案.md（新增）| 00-system/优化时间表.md（新增）| 00-system/会话检查点.md | CHANGELOG.md

---

## v3.5.1 — 2026-08-15

### 修复
- 🔧 **回撤基准上移**：peak_assets ¥32,961 → **¥35,655**（8/10 本周最高点，用户确认）
  - 旧基准过时 8 天：-5% 线需先跌 11.9% 才触发，回撤预警系统失效
  - 新回撤线：-5% ¥33,872 / -10% ¥32,090 / -15% ¥30,307；8/14 总资产 ¥35,556 → 回撤 -0.3%，距 -5% 线缓冲 ¥1,684
  - 同步：规则手册 v3.3 §4.1、体系总览、07-memory、test_calculations（基准相关测试）、会话检查点

### 修改文件
portfolio_data.json（_meta.peak_assets/peak_note）| 01-rules/投资规则手册_v3.3_正式版.md | ANCHOR_体系总览.md | 07-memory/project-investment-framework.md | 07-memory/project-anchor-strategy-direction.md | 07-memory/user-investor-profile.md | 00-system/会话检查点.md | 05-scripts/test_calculations.py | CHANGELOG.md

---

## v3.5 — 2026-08-15

### 新增
- 🆕 **实时数据同步（watch_sync.py）**：监听桌面 portfolio_data.json 变化 → 自动全量同步（看板 HTML/快照/Excel/06-dashboard 副本/公开示例页），彻底杜绝数据滞后
  - 机制：sha256 变化检测 + 60s 稳定防抖 + 单实例锁 + watch_sync.log 日志 + .watch_sync_state.json 幂等状态
  - 启动：登录自启（Startup 启动项 Anchor实时同步.vbs）+ 每日 21:40 兜底计划任务「Anchor每日兜底同步」
  - 首次启动自动同步一次，保证开机即最新；同步失败下一轮自动重试
- 🆕 8/14 收盘数据完整同步：总资产 ¥35,556，持有盈亏 +¥1,565，回撤 +7.9%（安全区）

### 修复
- 🔧 **月操作计数口径**：monthly_ops_summary 排除定投/出入金（非手动操作，与交易备注「不计入月限额」及 gen_monthly_attribution 分类一致），8 月操作恢复 1/4 正确口径（原误计 2/4）
- 🔧 **每日快照目录**：sync_all.py 快照写入 04-reviews/daily/（对齐文件归类规则，原误写 04-reviews/ 根目录）

### 新增文件
05-scripts/watch_sync.py（本地私有工具，已 .gitignore）| 05-scripts/.watch_sync_state.json（已 .gitignore）

### 修改文件
portfolio_data.json（system_version v3.4→v3.5）| 05-scripts/data_processor.py（新增 is_manual_operation）| 05-scripts/test_calculations.py（35 项，含定投排除测试）| 05-scripts/sync_all.py | .gitignore | 00-system/数据更新协议.md | 00-system/会话检查点.md | CHANGELOG.md

---

## v3.4 — 2026-08-10

### 新增
- 🆕 **噪声审计体系 v1.0**：三类噪声 + 四项指标 + 噪声评分卡
  - 新增 `00-system/噪声审计框架.md`
  - 新增 `05-scripts/noise_audit.py`
  - 新增 `06-dashboard/noise/`（已.gitignore）
- 🆕 **每日双报告制度**：14:30 操作建议 + 21:30 收盘复盘（含噪声审计）
  - 快捷指令："出报告" / "复盘报告"
- 🆕 **每日 14:30 盘中建议推微信**（8/9部署，8/10验证通过）
  - 新增 `05-scripts/daily_advice.py` + `00-system/每日建议推送说明.md`
- 🆕 **AstrBot 知识库每日自动同步**：`05-scripts/anchor_to_astrbot.py --upload`（21:30）
- 🆕 **研究报告自动存档**：每次报告保存到 `04-reviews/research/`
- 📁 **04-reviews/ 重组**：daily/intraday/weekly/monthly/research/special

### 变更
- 🔧 **DDX 规则升级**（试验中）：原"连3日为正"→"连3日为正+当日超大单净流入>0"
  - 依据：8/10 噪声审计 DDX 准确率 0%（2次触发2次假信号）
- 🔧 **场内纳指ETF建仓废弃**：溢价13%结构性问题，场外定投继续
- 🔧 **AstrBot kb_names 修复**：cmd_config.json 加 kb_names:["anchor"]
- 🔧 **凭据安全**：AstrBot 密码改环境变量 ASTRBOT_PASSWORD

### 新增文件
`00-system/噪声审计框架.md` | `00-system/每日建议推送说明.md` | `05-scripts/daily_advice.py` | `05-scripts/noise_audit.py` | `05-scripts/anchor_to_astrbot.py` | `04-reviews/research/` | `06-dashboard/noise/` | `CHANGELOG.md`

### 修改文件
`portfolio_data.json` | `CLAUDE.md` | `00-system/数据更新协议.md` | `00-system/会话检查点.md` | `AstrBot data/cmd_config.json`

---

## v3.3 — 2026-08-06 ~ 08-07

### 新增
- 状态合同（state/ops_state/risk_state/drawdown_state/freeze_state等）
- 公开发布边界（gen_anchor_pro.py --private 失败 + smoke_test脱敏）
- smart_classify 智能分类
- DDX做半导体加仓过滤器 / 纳指ETF溢价率≤3%建仓 / -8%止损 / 时间止损30天 / 卖出冻结72h

### 文件
`05-scripts/data_processor.py` | `05-scripts/rebuild.py` | `05-scripts/gen_anchor_pro.py` | `05-scripts/smoke_test.py` | `05-scripts/test_calculations.py`

---

## v3.2 — 2026-07-21
风口试探仓 + 反弹日清仓规则

## v3.1 — 2026-07 中旬
四层金字塔 + 月操作≤4笔

## v3.0 — 2026-07 初
初始框架：压舱石/核心/卫星/现金

---

*规则：每次改动 → 更新本文件 → 更新版本号 → 同步 CLAUDE.md/会话检查点 → git commit → git push*

---

## v3.4.1 — 2026-08-17

### 变更
- 🔧 **回撤基准上移**：peak_assets 32,961 → 35,655（8/10 真实高点，用户确认）
  - 原因：旧基准 8/6 清理后已失效，-5% 线需跌 11.9% 才触发（系统失去预警意义）
  - 新基准：-5% 线 ¥33,872（距当前 4.7%）
- ✅ 8/17 半导体 +4.9% + 超大单 +196.8亿 + DDX 转正（当日+0.28），浮亏预计缩至-5.5%

